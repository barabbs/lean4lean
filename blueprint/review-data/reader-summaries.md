## theory-syntax.json

The group is the object language of the whole verification: universe levels (VLevel), de Bruijn expressions (VExpr) with their lift/inst/Lift/Subst calculus, declarations (VDecl), environments (VEnv), quotients, the Lean.Expr translator (Meta), and an original coNP-hardness result for level equivalence (LevelSat, untouched master, no thesis counterpart). Contributed: iota added VEnv.pats + addPat (the schematic ι-rule registry), VRecRule/VRecursor/VInductDecl.recs, and ~200 lines of total telescope/application-spine readers (mkApps, piBinders, getAppFn, headConst?, RecHeaded/CtorHeaded and their quantifier-free prefix biconditionals); trproj added a subst-first weakening/instantiation section (lift'_eq_subst, subst_instN, lift'_instN_hi, liftN_subst_liftN) plus stability of spines, Π-telescopes and CtorHeaded under subst/lift'/inst/instL. Quality is high on specification design and documentation (VRecRule.ctorParams and the subst-first section docstring explain *why*, not just *what*; nothing here is sorry-dependent), but hygiene is weak: about ten contributed declarations have no consumer, and trproj's relocation/re-proof of master's lift'_inst_hi orphaned four upstream declarations (lift_r_one, Subst.lift_r_comm, Subst.trunc, Subst.Depth.one) without removing them. The iota block is noticeably tighter than the trproj block.

- Lean4Lean/Theory/VExpr.lean:919 — trproj moved+re-proved master's `lift'_inst_hi` (to :1018), leaving `lift_r_one` with no consumer and `Subst.lift_r_comm` (:911), `Subst.trunc` (:908), `Subst.Depth.one` (:885) alive only to feed it
- Lean4Lean/Theory/VExpr.lean:1232 — `CtorHeaded.forallE` is unused, proved `:= h` (definitionally trivial), and its docstring states a different proposition than the lemma
- Lean4Lean/Theory/VExpr.lean:1046 — section docstring still claims "with their decidability" after the head-shape instances were relocated to Lean4Lean/Tests/ShapeDecide.lean
- Lean4Lean/Theory/VExpr.lean:753 — `Lift.liftVar_consN_lt` (:753), `liftVar_consN_succ` (:756) and hence `consN_fixes` (:745) are dead; only `consN_cons` of that trproj block is used
- Lean4Lean/Theory/VExpr.lean:1256 — `CtorHeaded.instL` and its helper `getAppFn_instL_const` (:1248) are dead symmetry additions to the used `inst` pair
- Lean4Lean/Theory/VExpr.lean:1009 — `subst_instN` subsumes master's `subst_inst` (:952) and `liftN_subst_liftN` (:1032) subsumes `lift_subst_lift` (:947), but neither is re-derived; docstring says "m binders" while the statement binds `n`
- Lean4Lean/Theory/VDecl.lean:51 — `VRecursor.getFirstIndexIdx` is never used; relatedly `VEnv.addRecRule` (Theory/Inductive.lean:260) inlines the major index instead of calling `getMajorIdx` despite its docstring
- Lean4Lean/Theory/VEnv.lean:44 — `addPat` is total (no freshness/consistency check, unlike `addConst`); conflicting rules for one pattern are ruled out only later, with no comment saying so

## experimental-b.json

Group experimental-b covers 12 research modules (5253 lines) in Lean4Lean/Experimental: two clusters, a VExpr-side Church-Rosser/stratification development (NormalEq = the thesis's proof-irrelevance relation ≡_p, ParallelReduction = ≫_κ/⋙_κ with the triangle lemma and Church-Rosser, Stratified/StratifiedUntyped = the ⊢_n stratification of unique.tex §sec:unique, Stronger = an abandoned level-annotated attack on unique typing), and an SExpr-side normalization cluster (SExpr, UniqueTyping, StepIndexed, MoreStepIndexed, DomainTheory, Thierry, Thierry2) corresponding to normalization.tex, which is a 22-line stub ending in "UNFINISHED". The contribution is 27 lines, all from the iota branch, all compile-keeping after VEnv gained `pats` and VEnv.IsDefEq gained `pat`: a `Typing.pat_env` field (NormalEq:97), two `pat` proof cases (ParallelReduction:857,909), the `IsDefEq1.pat`/`IsDefEqU1.pat` constructors plus their two/one proof cases and an `Ordered → OrderedStrong` hypothesis strengthening (Stratified, StratifiedUntyped), and `pats _ _ := False` in VEnv'.out (Stronger:28). Quality verdict: the one genuine design decision — stratifying the ι rule's premises so the typing premise goes to the abstract `hasType` while the side conditions stay recursive — is made correctly and is validated by a clean round trip through `IsDefEq1.induction`; everything else is minimal, correct-looking, undocumented, and largely redundant with the real ι work in Theory/Typing/ChurchRosser.lean, in files upstream has marked "TODO: remove". The main substantive gap is that the entire SExpr-side development still has no ι rule at all, so the two halves of the project now model ι differently.

- Lean4Lean/Experimental/ParallelReduction.lean:857 and NormalEq.lean:97 — the ι adaptations duplicate byte-for-byte what the branch already added to Theory/Typing/ChurchRosser.lean:32,1390, in two files headed 'TODO: remove, this is now part of ChurchRosser.lean'; neither copy cross-references the other
- Lean4Lean/Experimental/SExpr.lean:608 — the whole SExpr cluster (Params :23, IsDefEq :590, UniqueTyping, ShapeLogRel*, LogRel, Step*Indexed) still has no ι/pat rule; reduction rules enter only via env.defeqs plus the global `axiom Params.extra_pat` (:614), so the branch left the two halves of the project modelling ι differently
- Lean4Lean/Experimental/Stratified.lean:79 (and StratifiedUntyped.lean:59) — exported hypothesis silently strengthened `Ordered env` → `OrderedStrong env` (forced by the new IsDefEq.strong, harmless since VEnv.WF implies it) with no explanatory comment
- Lean4Lean/Experimental/UniqueTyping.lean:138 — file has zero textual `sorry` but is entirely conditional on `SExpr.IsDefEq.strong` (SExpr.lean:679, `:= sorry`); a sorry-grep would mislabel it complete
- Lean4Lean/Experimental/DomainTheory.lean:223 — `Dom.out` is sorryAx-backed via the `stop` tactic (= `repeat sorry`) despite no literal `sorry`; same at LogRel.lean:320/361/364/369/377 and ShapeLogRel.lean:1703
- Lean4Lean/Experimental/Thierry.lean:9 and Thierry2.lean:9 — `axiom mySorry : α` proves False; these scratch files are deliberately inconsistent and nothing may depend on them
- Lean4Lean/Experimental/StratifiedUntyped.lean:51 — the added `IsDefEqU1.pat` is never eliminated (its consumer is commented out at :121-142) and its `chk` triples' third components are wholly unconstrained in the erased setting, where `Check.OK` would say the same more directly
- Lean4Lean/Experimental/Stronger.lean:28 — `pats _ _ := False` in `VEnv'.out` silently restricts every theorem about erased environments to ι-free ones, closing off any future reconnection of the level-annotated experiment to the ι development

## verify-typechecker.json

The group is the soundness bridge between the executable Lean4Lean kernel and the VEnv/VExpr model: MLCtx/VContext/VState scaffolding plus Hoare predicates M.WF/RecM.WF/Methods.WF in Basic.lean, then per-function .WF theorems for reduce, whnf, inferType and isDefEq, tied off by fuel induction and re-exported in TypeChecker.lean (mechanising typesys.tex's "algorithmic equality implies definitional equality" and soundness.tex's consistency corollary). Contributed material is small and concentrated: the iota branch adds inductiveReduceRecCore.WF (WHNF.lean:17-145), a careful 129-line proof that the kernel's iota step agrees with the registered iota rule (axioms.tex sec:iota), and trproj adds inferProj.WF_struct plus a corrected restatement of inferProj.WF (InferType.lean:388-410). The iota proof is the only substantive new proof and is well designed, well commented and (in its own script) complete; the trproj piece is statement-level only - both theorems are sorry - but it repairs two genuine defects in master's inferProj.WF placeholder and documents that the general statement is unprovable without extending the model. Overall quality is good but the payoff is thin relative to the supporting infrastructure: the iota lemma has no consumer (reduceRecursor.WF is still sorry), inferProj.WF_struct is referenced only from docstrings, and the projection-side obligations reduceProjCore.WF and tryEtaStructCore.WF were left untouched despite trproj closing five of master's seven TrProj sorries elsewhere. Note the blame files mis-attribute WHNF.lean's 141 lines to trproj; they are byte-identical to the iota commit (trproj carries a duplicate of 7a68882 as 6fd8a1d).

- Lean4Lean/Verify/TypeChecker/InferType.lean:400 - inferProj.WF is documented as 'not provable as stated' yet is consumed by inferType'.WF:479, so checkType.WF/inferType.WF' (TypeChecker.lean:204,215) are known-unprovable for projections outside TrProj's scope
- Lean4Lean/Verify/TypeChecker/WHNF.lean:17 - inductiveReduceRecCore.WF has no consumer anywhere in the repo; reduceRecursor.WF (WHNF.lean:149) is still sorry, so the new lemma is dead code
- Lean4Lean/Verify/TypeChecker/WHNF.lean:24 - hypothesis hmaj requires the unreduced spine element to already be the constructor application, but the kernel calls inductiveReduceRecCore on a major produced by whnf/K/struct-eta (Inductive/Reduce.lean:115); the bridging step is neither provided nor documented
- Lean4Lean/Verify/TypeChecker/WHNF.lean:23 - hsat (exact saturation) is assumed with the docstring conceding it follows from well-typedness, but that is proved nowhere; it also silently excludes over-applied constructor applications, the very case where the two slicings disagree
- Lean4Lean/Verify/TypeChecker/InferType.lean:392 - inferProj.WF_struct is sorry and unused (only docstring references in Tests/ProjInhabit.lean:15 and Verify/Typing/Expr.lean:125); its hypotheses are kernel metadata facts, not TrProjCtor, contrary to its own docstring
- Lean4Lean/Verify/TypeChecker/WHNF.lean:17 - #print axioms reports sorryAx for inductiveReduceRecCore.WF, inherited via TrExpr.rebuild_mkAppList from the still-open TrProj.uniq/TrProj.weak'_inv (Verify/Typing/Lemmas.lean:995,747)
- Lean4Lean/Verify/TypeChecker/Reduce.lean:143 - reduceProjCore.WF and tryEtaStructCore.WF (IsDefEq.lean:227) are the projection/structure obligations the enlarged TrProj metatheory was built for, and neither was attempted on trproj
- Lean4Lean/Verify/TypeChecker/Reduce.lean:21 - reduceBinNatOpG duplicates kernel executable code inside a proof file (master, not contributed); minor maintenance hazard

## verify-level-and-utils.json

The group covers the universe-level algorithm verification (Verify/Level.lean, 3880 lines), the normLt sort order (NormLt.lean), a vendored Array.qsort verification (QSort.lean, Lean FRO / Kim Morrison, from leanprover/lean4#14658) and the isDefEq memo cache (EquivManager.lean) — 4967 lines, 100% master, with zero iota/trproj contribution and no active sorry. Verify/Level.lean verifies a canonical form for universe levels following Géran's paper (not the thesis, which only gives the inequality rules in axioms.tex and the valuation semantics in soundness.tex): normalize_eval (:1214), Théorem-39 soundness NormLevel.le_eval (:1237), the separation converse (:2674), canonicity normalize_complete (:3721), and the reconstruction chain toTree_eval (:2217)/reify_eval (:1404)/normalize'_complete (:3849). Quality is high: excellent explanatory docstrings, semantically correct statements against VLevel.eval, and one genuinely nontrivial hand-written theorem (separation). The main weaknesses are that a quarter of the file (the reconstruction/canonicity layer supporting normalize') is consumed by nothing but tests, three mkData lemmas plus three EquivManager lemmas are dead code, two stale docstrings contradict what is actually proved, and all main results factor through two Std.TreeMap axioms.

- Lean4Lean/Tests/Level.lean:8 — docstring claims canonicity of normalize' and completeness of isEquiv'/geq' are 'not proved'; all three are proved at Verify/Level.lean:3849, :3861, :3870
- Lean4Lean/Verify/Level.lean:41 — mkData_depth/mkData_hasParam/mkData_hasMVar unused project-wide; sole consumers of the Lean.Level.mkData_eq axiom and need allowUnsafeReducibility (:38-39)
- Lean4Lean/Verify/Axioms.lean:10 — every main theorem of Verify/Level.lean factors through the axioms Std.TreeMap.all_eq_all_toList / any_eq_any_toList (NormLevel.le, addable and BEq are all defined via TreeMap.all/any)
- Lean4Lean/Verify/Level.lean:3822 — normalize'_eval, normalize'_complete, isEquiv'_complete, geq'_wf, geq'_complete are unused; only isEquivList_wf (:3837) reaches the verified type checker, so the ~1000-line reconstruction layer supports a function the kernel never calls
- Lean4Lean/Verify/LevelStd.lean:155 — stale comment says the qsort facts are 'currently unproved' and names qsort_perm_toList/pairwise_qsort_normLt, which do not exist (real ones at :228, :234, on top of Verify/QSort.lean)
- Lean4Lean/Verify/EquivManager.lean:41 — RelevantEq.symm (:41), RelevantEq.trans (:58) and M.WF.bind_le (:205) are never used anywhere
- Lean4Lean/Verify/Level.lean:1222 — invariant sprawl: end-to-end theorems thread 5-7 separate normalize_* side conditions by hand (geq'_complete at :3870 takes six)
- Lean4Lean/Verify/QSort.lean:34 — vendored code declares 4 lemmas at _root_ into List/Array/Vector and registers global grind patterns (:40-55, :105-106), plus carries upstream TODOs and commented-out experiments into this repo

## kernel-impl.json

The kernel-impl group is the executable Lean re-implementation of the Lean 4 kernel (18 files, 4040 lines): type checker (inferType'/whnf'/isDefEqCore'), inductive add + recursor reduction, quotients, primitives recognizer, level normalization, and the replay CLI. Of 4040 lines exactly 22 are contributed: 21 on trproj in Lean4Lean/Inductive/Reduce.lean (the ι step extracted verbatim into the pure `inductiveReduceRecCore`, lines 71-89 plus the call site at 115) and 1 on iota in Lean4Lean/Quot.lean:24 (`if info.isUnsafe then fail` in `checkEqType`). Both changes are correct, minimal, motivated by a specific proof obligation, and documented in the commit message and in divergences.md; I diffed the Reduce.lean split against master and confirmed it is behaviour-preserving, and the Quot.lean line genuinely discharges the former `Environment.EqSafe` assumption, making `addQuot.WF`/`addDecl.WF` unconditional. Quality verdict: exemplary restraint and documentation on the contributed kernel code; the only blemishes are cosmetic (duplicated docstring, misleading section-variable placement) plus one unstated scope caveat about `quotInit` short-circuiting on imported environments.

- Lean4Lean/Quot.lean:24 — the new unsafe-Eq check never runs on imported environments: addQuot returns early when env.quotInit is true and Environment/Basic.lean:124 sets quotInit := !imports.isEmpty, so the removed EqSafe assumption is only replaced for from-scratch environments
- Lean4Lean/Inductive/Reduce.lean:71 — inductiveReduceRecCore sits inside the section whose variables (env, whnf, inferType, isDefEq, [Monad m]) it uses none of; arity is right today but a future use of `env` would silently change the signature the WF theorem is stated about
- Lean4Lean/Inductive/Reduce.lean:91 — inductiveReduceRec's untouched master docstring still describes the ι slicing now performed in the extracted core, so the file documents the same algorithm twice
- Lean4Lean/TypeChecker.lean:946 — TypeChecker.etaExpand has no call site anywhere; if it is meant to be the thesis's rec-normal-form preprocessing (unique.tex sec:kappa) that is undocumented, and it bears directly on the `hsat` saturation hypothesis of inductiveReduceRecCore.WF
- Lean4Lean/ForEachExprV.lean:25 — Expr.forEachV/forEachV' appear to be dead code: imported by TypeChecker.lean but never called
- Lean4Lean/TypeChecker.lean:593 — quickIsDefEq destructures TypeChecker.State positionally (.mk a1 … a7 (eqvManager := m)); adding a field silently changes what it matches
- Lean4Lean/Inductive/Add.lean:469 — AddInductive.run computes `isUnsafe` twice (also at 453), the second shadowing the first
- Lean4Lean/Level.lean:160 — the new Géran-style level normalization has no correctness statement anywhere in the repo; soundness rests on the informal argument in the isEquiv' docstring plus divergences.md

## theory-typing.json

The `theory-typing` group is the abstract type theory of the thesis (`IsDefEq` = axioms.tex §2.1-§2.2 as one judgment), environment well-formedness (`VDecl.WF`/`VEnv.WF`), and the structural metatheory (weakening, substitution, regularity, Π/sort inversion). No line in this group comes from trproj; all 381 contributed lines are iota. The contribution is (a) a fourteenth `IsDefEq` rule, `pat`, modelling ι-reduction over a `pats` registry, with admissibility predicates `PatTyped`/`PatWF` (Basic.lean:60,92,104), (b) `pat` cases in all ten inductions over `IsDefEq` plus `Ordered.pat`/`Ordered.patWF`/`patHeaded` (Lemmas.lean, 74 lines), and (c) a 237-line rebuild of the strong-system bootstrap from `VEnv.WF` rather than `VEnv.Ordered` (`PatsStrong`, `foldlM_addConst_strong`, `addQuot_strong`, `addInduct_strong`, `WF.strong`, `WF.orderedStrong`). Quality is high: every contributed declaration is docstringed, the docstrings name their thesis section and their own limitations, the `TemplateHeaded` half of `PatWF` is a genuinely clever device that keeps the Π/sort inversion lemmas provable with ι present, and the iota edits to master code are surgical (no master lemma gained a hypothesis). The decisive caveat is that `VEnv.WF.patsStrong` (EnvLemmas.lean:334) is `sorry` and is reached through a `CoeOut` instance, so the whole strong system and the `Verify/` layer are now conditional on it, where master's `Ordered.strong` was proved — a fair trade, since master's inductive layer was stubbed with `sorry` definitions, but one the blueprint must state prominently.

- HIGH Lean4Lean/Theory/Typing/EnvLemmas.lean:334 — `VEnv.WF.patsStrong` is `sorry`; via the `CoeOut` instance at :343 every `VEnv.WF` hypothesis silently yields `OrderedStrong`, making the strong system and all of `Verify/` conditional (master's `Ordered.strong` was proved)
- HIGH Lean4Lean/Theory/Typing/Basic.lean:60 — `IsDefEq.pat` asserts the reduct at the redex's type with no premise on the reduct, i.e. subject reduction for ι is assumed; the author's own counterexample (EnvLemmas.lean:327) shows it is false over a merely `Ordered` environment
- MEDIUM Lean4Lean/Theory/Typing/Basic.lean:92 — `PatTyped` is existential in U, Γ, e, m2, B and `RHS.Generic` constrains only the holes the reduct *uses*, so `Ordered.pat` admits rules typed at one lucky instantiation; the name oversells the predicate
- MEDIUM Lean4Lean/Theory/Typing/Basic.lean:62 — the `Pattern.Check`/`Realizes` side-condition machinery (≈10 lemmas + a premise in 10 induction cases) is exercised by nothing: every registered ι rule uses `Check.true` and K-like reduction is explicitly not modelled (Inductive.lean:332)
- MEDIUM Lean4Lean/Theory/Typing/EnvLemmas.lean:130 — `PatsStrong` has explicit env arguments and six positional hypotheses; call sites read `hp _ _ hpre₀ .rfl … rfl rfl hord` (:279,282,291,293,305,308) and the same `.trans` plumbing is repeated in three lemmas
- MEDIUM Lean4Lean/Theory/Typing/EnvLemmas.lean:188 — `addQuot_strong` hand-unrolls five steps into ~25 lines of `l1…l5/d1…d4/p1…p4/O1…O4/I1…I4` chains; brittle, and cannot reuse `foldlM_addConst_strong` only because `addQuot` is a bind chain not a fold
- LOW Lean4Lean/Theory/Typing/QuotLemmas.lean:19 — `addQuot_chain` re-proves via the same `type_tac` calls the four constant typings `addQuot_WF` (:7) already establishes; `addQuot_WF` could be a corollary of it
- LOW Lean4Lean/Theory/Typing/Basic.lean:58 — the `pat` docstring cites `Params.pat_wf` unqualified; the intended declaration is `Lean4Lean.VEnv.Params.pat_wf` (ChurchRosser.lean:20) and a homonym exists in Experimental/ShapeLogRel.lean

## experimental-a.json

Group experimental-a is four research modules on logical relations, all 100% master (Mario Carneiro); git log --author=Alessandro is empty for every one, so the user's iota/trproj work does not appear here at all. The substantive pair is ShapeLogRel.lean (6100 lines) + ShapeLogRelAdequacy.lean: a finitary "shape" model (Shape/WShape/TShape algebra, shape typing, the LE_Interp interpretation, SoundEq/StrongSound soundness, and the shape-indexed logical relation LR0/LRS/LR) whose entire purpose is the three definitional-inversion results at ShapeLogRelAdequacy.lean:450/455/459 — Pi injectivity, sort-vs-Pi discrimination and sort injectivity — consumed by UniqueTyping.lean to get unique typing. This is a genuine methodological improvement on the thesis, which reaches the same clauses (unique.tex:29-39, thm:1dinv) only via a stratified ⊢ₙ judgement plus Church–Rosser; here proof irrelevance is absorbed into the model (proofs get shape ⊥) instead of quarantined. Quality is high in idea but poor in reviewability and honesty of claims: six proofs exceed 100 uncommented lines (LR.adequacy is 325), there are 13 docstrings in 6100 lines and none on the pivotal definitions, and the headline theorems are conditional on a live sorry at ShapeLogRelAdequacy.lean:154 plus the sorry in SExpr.IsDefEq.strong and axiom Params.extra_pat — none of which is flagged anywhere, despite the introducing commit being titled "Finished injectivity! 🎉". LogRel.lean and CoinductiveLogRel.lean are abandoned dead branches imported by nothing.

- HIGH: Lean4Lean/Experimental/ShapeLogRelAdequacy.lean:154 — live `sorry` in the `const` case of LR.adequacy, so forallE_inv/sort_inv/sort_forallE_inv (:450,:455,:459) and everything UniqueTyping.lean derives are unproved; commit 84f2b04 is titled 'Finished injectivity! 🎉'
- HIGH: Lean4Lean/Experimental/ShapeLogRel.lean:5055 — LE_Interp.strongSound (and LR.adequacy at Adequacy:107) start with `replace H := H.strong`, where SExpr.IsDefEq.strong is `sorry` at SExpr.lean:679; SExpr.lean carries ~20 further sorries
- HIGH: Lean4Lean/Experimental/ShapeLogRel.lean:5261 — the delta/iota (`extra`) case is discharged via `axiom Params.extra_pat` (SExpr.lean:614), a real axiom whose Params field is commented out at SExpr.lean:42; invisible from these files
- MEDIUM: Lean4Lean/Experimental/LogRel.lean:353 — `fundamental` unproved (`| _ => sorry` catch-all); 7 sorries + 5 `stop` (= `repeat sorry`, so grep-invisible); file imported by nothing and superseded
- MEDIUM: Lean4Lean/Experimental/LogRel.lean:70 — `Lean4Lean.SExpr.LogRel` declared three times incompatibly (LogRel.lean:70, ShapeLogRel.lean:5271, MoreStepIndexed.lean:371); same for Classifier' and NormalType vs StepIndexed.lean
- MEDIUM: Lean4Lean/Experimental/CoinductiveLogRel.lean:1 — 95% block comment sketching a nonexistent `coinductive` command; 2 live unused declarations, `Classifier` discards all its indices; pure build weight
- MEDIUM: Lean4Lean/Experimental/ShapeLogRel.lean:1665 — 68-line block-commented abandoned `Shape.WF.plift` holding all 6 textual sorries + 1 `stop`, making grep-based sorry counts of the file misleading
- LOW: Lean4Lean/Experimental/ShapeLogRel.lean:53 — `Shape : Nat → Type` as a def plus a separate WF predicate forces every order/lift/join/typing lemma to be stated three times (Shape/WShape/TShape); ~2500 lines of boilerplate, and `Shape.trim` (:820) is dead

## theory-metatheory.json

This group is the metatheory spine: Strong.lean (annotated judgments IsDefEqStrong/HasTypeStrong, strengthening, stratified typing, substitution), UniqueTyping.lean (thesis thm:utype plus the weakening/transitivity toolkit), ChurchRosser.lean (NormalEq = thesis ≡ₚ, ParRed/CParRed = ≫κ/⋙κ, triangle lemma, confluence, completeness of κ reduction) and HeadReduction.lean (WHRed/StRed, standardization after Kashima 2000, InferType). The iota branch contributes 164 lines in Strong.lean and 9 in ChurchRosser: the IsDefEqStrong.pat rule annotated with the reduct's typing, a uniform pat case in each of ten structural lemmas, the new PatStrong/PatsStrongOn/OrderedStrong layer, the refactoring of master's proved Ordered.strong into EnvStrong.of_hasType + OnTypes.addConst/addDefEq/addPat, and Params.pat_env + the pat case of IsDefEq.church_rosser. Quality of the contributed parts is high: the reduct annotation is genuinely load-bearing (forallE_inv', isType', hasType', EqUpToLevels.defeq all need it), hypotheses are minimal and forced, nothing is dead, and every new declaration has a docstring that explains why rather than restating what. The cost is equally clear and must be stated in any claim: master's Ordered.strong was proved, whereas OrderedStrong is reachable from VEnv.WF only through the admitted VEnv.WF.patsStrong, so all of UniqueTyping (textually untouched, silently re-based via two CoeOut instances), the typing inversions, ChurchRosser and HeadReduction became sorry-dependent — on top of two pre-existing master sorries in NormalEq.parRed and one in IsDefEqU.weakN_iff that already leave Church-Rosser unproved.

- HIGH Strong.lean:679 — OrderedStrong (iota) is derivable from VEnv.WF only via the `sorry`ed VEnv.WF.patsStrong (EnvLemmas.lean:334); master's Ordered.strong was proved, so IsDefEq.strong and everything that depends on it is now sorry-dependent
- HIGH ChurchRosser.lean:1193 and :1212 — two master `sorry`s in NormalEq.parRed (thesis thm:gg_compat), exactly in the extra/ι cases; ParRedS.church_rosser, CRDefEq.trans, IsDefEq.church_rosser, reduce_sort/reduce_forallE, InferType.exists all inherit them
- HIGH UniqueTyping.lean:174 — IsDefEqU.weakN_iff forward direction is `sorry` (master) and is used by NormalEq.weakN_inv_DFC, ParRed.weakN_inv, hasType_app_bvar0, OnCtx.weakN_inv and the whole weakN_iff/weak'_iff family
- MEDIUM ChurchRosser.lean:30 — Params.pat_env docstring justifies the field by extra_pat, but the two are independent (defeqs vs pats); the comment reads as if pat_env were derivable
- MEDIUM ChurchRosser.lean:12 — Params has exactly one instance (VEnv.toParams, InductiveParams.lean:393) requiring DefEqsAsPats, which fails once a `def` or the quotient rule is present, so the whole confluence/standardization development currently covers only axiom+inductive environments
- MEDIUM Strong.lean:684 — the CoeOut OrderedStrong→Ordered instance plus CoeOut VEnv.WF→OrderedStrong (EnvLemmas.lean:343) hide the new obligation: UniqueTyping.lean is byte-identical to master yet changed from Ordered-dependent to patsStrong-dependent
- LOW Strong.lean:89 — Pattern.Check.Realizes constrains only the two term components of each check triple, leaving the type free, so a rule instance does not determine its `chk`
- LOW HeadReduction.lean:29 and ChurchRosser.lean:632 — Subpattern.varN_const lands in namespace Lean4Lean.VEnv so dot notation on Lean4Lean.Subpattern misses it; ten lines of commented-out IsDefEqU.applyL draft left in ChurchRosser (both master)

## verify-primitive-arith.json

Soundness of the kernel's GMP-accelerated arithmetic-primitive recognizer: Condition.lean (3117 lines) supplies the `Condition`/`Reflection` layer and the transport kit (`TrExprS.weakR`, `ofClosed`, the `_nil_inv` family) that identifies a closed piece read at different binder depths, and Clauses/DivMod/Gcd/Bitwise give one WF theorem per recognized primitive. This is master code and, by the standards of the repo, exceptionally well documented: nearly every design choice (the Expr-level `WF_ite` vs VExpr-level `IteEval` split, the `natOnly` flag, the beta-redex gadget in `Condition.check`) is argued in its docstring. The thesis does not model these primitives at all, so correspondences are only structural (Weakening thm:weak, Substitution thm:subst, the beta rule). All 67 iota lines plus 2 trproj lines are one mechanical edit: `VEnv.Ordered` -> `VEnv.OrderedStrong`, six statements weakened in Condition.lean (421, 744, 754, 771, 786, 855) and 61 proof-internal call-site adjustments; the trproj lines are a line re-wrap. The reasoning behind the change is sound — real ι rules force the strong system to assume ι subject reduction — but every result in this group is thereby made dependent on the new `sorry`ed `VEnv.WF.patsStrong`, and nothing in these five files says so.

- Lean4Lean/Verify/Environment/Primitive/Condition.lean:421 — the iota `Ordered`->`OrderedStrong` re-threading routes all five files through `VEnv.WF.patsStrong` (Theory/Typing/EnvLemmas.lean:334, `sorry`); the `CoeOut (VEnv.WF env) env.OrderedStrong` instance hides it and none of the ~67 touched lines carries a comment
- Lean4Lean/Verify/Environment/Primitive/Condition.lean:26 — stale module docstring: claims the `Condition.check` calls for `Nat.bitwise` and the `eager` gadget 'currently sit under binders'; they were hoisted (Lean4Lean/Primitive.lean:409-410, :372), which is why Bitwise/Gcd pass `hnil := rfl`
- Lean4Lean/Verify/Environment/Primitive/Condition.lean:2438 — `Condition.fvarsIn_ite` is dead code, no use anywhere in the repo (sibling `fvarsIn_dite` used once at DivMod.lean:163)
- Lean4Lean/Verify/Environment/Primitive/Bitwise.lean:41 — `have hE := ctx.Ewf.orderedStrong` is downgraded with `hE.ordered` at six of its eight uses (86, 150, 152, 182, 315, 362)
- Lean4Lean/Verify/Environment/Primitive/Condition.lean:1448 — inconsistent merge resolution: `VExpr.WF.lam_inv' c.Ewf.orderedStrong` at 1448 but plain `c.Ewf` (via coercion) at 1449-1450 for the same lemma
- Lean4Lean/Verify/Environment/Primitive/Condition.lean:485 — three different environment hypotheses now coexist with no stated rationale: `VEnv.WF` (ofClosed 540), `Ordered` (weakR 485, of_nil_any 664, app1_nil_inv 687), `OrderedStrong` (the six upgraded lemmas)
- Lean4Lean/Verify/Environment/Primitive/Bitwise.lean:31 — `checkNatBitwise.WF` is the only clause-level WF theorem with no docstring, and the longest proof in the group (375 lines)
- Lean4Lean/Verify/Environment/Primitive/Clauses.lean:225 — `checkNatLAnd.WF`/`checkNatLOr.WF`/`checkNatXor.WF` are near-duplicate 25-30 line proofs; `checkNatBoolCases.WF` (170) shows the parametrised alternative

## verify-typing.json

This group is the Lean.Expr → VExpr translation layer: Closed/FVarsIn, VLCtx and its weakening/instantiation/abstraction relations, TrExprS/TrExpr and their full metatheory, plus the primitive-constant invariant and the real-LocalContext bridge. The headline contribution is trproj's TrProjCtor/TrProj (Verify/Typing/Expr.lean:69-135), which replaces master's literal `def TrProj … := sorry` with a real specification modelling a projection as the recursor expansion P_i (the thesis's inv_x construction, typesys.tex §undecidable), and then proves five of the seven TrProj lemmas that were all `sorry` on master (weak'/weakN, defeqDFC, wf, instN, instL, plus a new mono), leaving weak'_inv and uniq open with precise explanations; it also adds three Expr spine lemmas and TrExprS.mkAppList_inv for the projection/ι WHNF proof. iota contributes a small, fully used LocalContext lookup block (LocalContext.lean:181-222, consumed by Verify/Environment/Quot.lean) and a mechanical Ordered→OrderedStrong sweep of 12 signatures. Quality of the contributed material is high — short structured proofs, unusually thorough docstrings, statements chosen so later proofs get simpler (TrProj.wf even drops a hypothesis) — but the projection model is specified and transported, not yet inhabited: inferProj.WF_struct is still `sorry`, TrProj.uniq's documented proof sketch does not explain how the per-field level list `uss` is pinned, and the `pat` field records only a key, not the registered reduct.

- Lean4Lean/Verify/Typing/Lemmas.lean:992 — TrProj.uniq still `sorry`; its sketch omits how the per-field level list `uss` is pinned, so either the sketch is incomplete or TrProjCtor is under-constrained (TrExprS.uniq and TrExpr.proj depend on it)
- Lean4Lean/Verify/Typing/Lemmas.lean:745 — TrProj.weak'_inv still `sorry` (root cause: admitted IsDefEqU.weakN_iff, Theory/Typing/UniqueTyping.lean:174), keeping TrExprS.weakFV'_inv and all three Conditionally*.weakN_inv sorry-dependent
- Lean4Lean/Verify/Typing/Expr.lean:97 — TrProjCtor.pat asserts only that *some* reduct is registered under the key np+1+1+0; the reduct is not pinned, so TrProj alone does not determine reduction behaviour
- Lean4Lean/Verify/Typing/Expr.lean:133 — TrProj quantifies six existentials of which two (np, fieldTys) are pinned by other fields; np := params.length would shrink the interface and every transport proof
- Lean4Lean/Verify/Typing/Expr.lean:84 — the scope claims about inferProj's Prop gate and accepted structures are prose only; inferProj.WF_struct and inferProj.WF (InferType.lean:392,407) are both `sorry`, so TrProjCtor has no general inhabitation theorem
- Lean4Lean/Verify/Typing/Lemmas.lean:1856 — iota's Ordered→OrderedStrong sweep (10 sigs here, 2 in TrTerm.lean) makes every later use of the literal lemmas depend on the admitted VEnv.WF.patsStrong (Theory/Typing/EnvLemmas.lean:334)
- Lean4Lean/Verify/LocalContext.lean:183 — a global `instance : DecidableEq FVarId` is installed inside `namespace Lean.LocalContext` under an unrelated section heading; could be instDecidableEqOfLawfulBEq given LawfulBEq FVarId at Verify/Expr.lean:13
- Lean4Lean/Verify/Typing/TrTerm.lean:98 — docstring of TrExprS.ofConst still describes an `Ordered` hypothesis after iota changed it to `OrderedStrong` (doc/code mismatch)

## verify-primitive-core.json

This group (3964 lines over 4 files) verifies Lean 4's GMP-accelerated Nat/String primitive recognizer: Verify/Primitive.lean turns the checker's open probe equations into the closed Reflects* specs and transports them from the definition's value onto the constant via the δ-equation addDefEq records; Environment/Primitive/Basic.lean supplies the shared telescope/beta/ground-judgement vocabulary (lams_appN, lambdaTelescope.WF, VContext.Ext, Data, PrimitiveResult, mkResult*); Recursion.lean verifies the well-founded-recursion recognizer (ProbeBundle, Done, NatFixUnfold, the fuel induction) used by Nat.gcd/bitwise/div/mod; Environment/Primitive.lean is the top-level checkDef.WF dispatch plus an unused, pre-staged inductive clause. The subject matter has no counterpart in Carneiro's thesis (which does not model primitive acceleration); only the ambient regularity/weakening/substitution/unique-typing lemmas and axioms.tex's δ-reduction section correspond. The contributed delta is thin and mechanical: all 71 non-master lines are one Ordered → OrderedStrong hypothesis-strengthening applied on the iota branch, and the 5 trproj-tagged lines are a pure line re-wrap of that same edit. The master code itself is of high quality with unusually candid documentation, but the iota adaptation silently converts a previously unconditional layer into one depending on the admitted VEnv.WF.patsStrong, and it was applied globally rather than minimally.

- HIGH Lean4Lean/Verify/Primitive.lean:29 — iota's Ordered→OrderedStrong edit makes the entire GMP-primitive verification (checkDef.WF, every PrimitiveResult) transitively depend on the admitted `VEnv.WF.patsStrong := sorry` (Theory/Typing/EnvLemmas.lean:334); on master the inversion lemmas it uses were provable from Ordered alone, so this layer was unconditional
- MEDIUM Lean4Lean/Verify/Primitive.lean:84 — the strengthening is not minimal: natZeroT(29), natSuccT(34), natPredT(39), natLitT(84), natFstLamApp(96), natIsType(114), boolLitT(129), boolIsType(134), appChar_inv'(177) only use HasType.weak0 and IsDefEq.isType, which still take Ordered; only const_inv users (47,76,122,140) need it
- MEDIUM Lean4Lean/Verify/Environment/Primitive.lean:6 — module docstring still asserts 'introduce no additional sorry-backed assumptions'; not revisited after OrderedStrong (sorry-backed) was threaded through this layer
- LOW Lean4Lean/Verify/Environment/Primitive/Basic.lean:431 — the only trproj-attributed lines in the group (431-433, 455-456) are a pure >100-column reflow of iota's edit; `git diff iota HEAD` over all four files is exactly these two hunks. No projection content here
- LOW Lean4Lean/Verify/Environment/Primitive.lean:73 — ~120 of 195 lines are dead: AddsConsts, constants_stable/of_mem/holds_of_ne, boolConsts, natConsts, hasPrimitives_bool/_nat, PrimitiveInductiveResult and checkInductive.WF have no consumer anywhere in Lean4Lean/
- LOW Lean4Lean/Verify/Environment/Primitive.lean:121 — hasPrimitives_bool and hasPrimitives_nat are byte-identical proof scripts with boolConsts/natConsts swapped
- LOW Lean4Lean/Verify/Environment/Primitive/Recursion.lean:587 — unfoldNatWellFounded.WF' is a single 1135-line tactic proof needing maxHeartbeats 1000000, effectively unreviewable; its BlockQ docstring (527-543) is stale ('only the first two checks are recorded' while all four are)
- LOW Lean4Lean/Verify/Environment/Primitive/Basic.lean:318 — namespace inconsistency: some List helpers use _root_.List (222, 229) while append_inv(318), forall_mem_pair(333), snoc(336), rev(342), fvars_uniq(357), mapIdx_replicate_nat(141) land in Lean4Lean.List.*, shadowing the real List namespace

## verify-environment.json

The verify-environment group is the refinement layer between a real Lean.Kernel.Environment and the thesis's abstract VEnv: TrEnv'/TrEnv, the Aligned companion relation, the per-declaration add*.WF driver, quotient initialization, and the Verify/Axioms.lean trust boundary. The iota branch's central contribution is replacing master's deliberately empty `inductive AddInduct : Prop` (documented as "essentially a sorry") with a real 15-field structure plus 17 derived bookkeeping lemmas (rec_find, rec_reg, ctor_find), the Aligned.pat/block cases, the ι interface (pats_iota', iota_rec), and — forced by AddInduct becoming inhabitable, which destroyed master's vacuous checkEqType.WF — the entire new 615-line Quot.lean proving addQuot.WF for real. The trproj branch added TrEnv'.ctor_arity, the 125-line structure_rec, the IotaRule structure with its inverse pats_iota_inv_shape, a β-telescope calculus, and the 138-line TrEnv.proj_defeq, which correctly derives projection reduction from the registered ι rule rather than postulating it. Quality is high: no sorry in any of these files, excellent and honest docstrings, statements matched to what the kernel actually tests, and distinctness/arity facts derived rather than assumed. The decisive caveat is connectivity — addDecl.WF's inductDecl case is still sorry because nothing constructs an AddInduct witness, and proj_defeq's only consumer in the repo is a #print axioms test, so the trproj work is a well-built component still awaiting its socket while the iota work reaches the kernel model at exactly one proved point (inductiveReduceRecCore.WF).

- Lean4Lean/Verify/Environment.lean:208 — addDecl.WF's inductDecl case is still `sorry`; nothing constructs an AddInduct witness from Environment.addInductive, so the whole iota/trproj inductive apparatus is conditional on an undischarged hypothesis (the sorry moved, it did not go away)
- Lean4Lean/Verify/Environment/Lemmas.lean:1021 — TrEnv.proj_defeq is orphaned: only consumer is the #print axioms check at Tests/ProjInhabit.lean:595; reduceProj.WF (WHNF.lean:149) and both inferProj.WF (InferType.lean:398,410) are sorry, and its premise TrProjCtor is only hand-inhabited in tests
- Lean4Lean/Verify/Environment/Lemmas.lean:903 — TrEnv.pats_iota_inv_shape is a definitional restatement of the primed version (proof is literally the primed lemma) and, contrary to its docstring, does not convert SMap.find? to Environment.find?; proj_defeq redoes the conversion by hand at :1060-1062 and :1072-1074
- Lean4Lean/Verify/Environment/Basic.lean:240 — TrRecursor.all (:240) and TrRecursor.k (:245) are never used anywhere; pure extra obligations on a nonexistent producer, and `k` records a K-like-reduction flag the model has no rule for
- Lean4Lean/Verify/Environment/Basic.lean:165 — insertConsts_find?_none is dead code (only occurrence besides its statement is its own recursive call)
- Lean4Lean/Verify/Environment/Basic.lean:272 — AddInduct is declared Type-valued (no `: Prop`), so TrEnv' gains a data-carrying constructor field and loses large elimination; necessary and harmless today but an undocumented change to the project's central invariant vs master's Prop version
- Lean4Lean/Verify/Environment/Lemmas.lean:959 — the β-telescope family (:959-1010) and TrExpr.mkAppList (:1166) are pure VExpr/TrExpr metatheory misfiled in the environment module; TrEnv.iota_defeq (:917) is a 3-line wrapper over IsDefEq.pat named as if it had a TrEnv hypothesis, which it does not
- Lean4Lean/Verify/Environment/Quot.lean:521 — addQuot.WF repeats four near-identical 6-line checkName extraction blocks (:521-547) and a 24-line hand-nested find?_add_of_ne chain for safePrimitives (:584-607); AddQuotAux (:27-70) also hand-replays Lean4Lean/Quot.lean's binder names/order with no cross-check

## contrib-iota.md

Branch `iota` (28 commits, +5628/−331 over master) gives lean4lean its first model of ι-reduction — a `VEnv.pats` rule registry, `IsDefEq.pat`, a thesis-shaped `VInductDecl.WF` and a real `addInduct` — closing 3 upstream sorries but opening one (`VEnv.WF.patsStrong`) that master's whole strong system now depends on.

- Substance: ι is modelled as a schematic pattern-rule registry (`VEnv.pats` @ Theory/VEnv.lean:21, `IsDefEq.pat` @ Theory/Typing/Basic.lean:60, `SimplePattern.iotaRHS` @ Theory/Typing/Pattern.lean:581), promoting Mario's own `SimplePattern.iota`/`Params.Pat` prototypes to live code with one forced new class field (`Params.pat_env` @ ChurchRosser.lean:32). It replaces master's three sorries — `VInductDecl.WF` (now a 20-field thesis §2.6.1–2.6.4 direct-block spec @ Inductive.lean:335), `VEnv.addInduct` (four-stage, @ :316), `addInduct_WF` (@ InductiveLemmas.lean:229) — and turns the vacuous `AddInduct`/`addQuot.WF` into real ones (new sorry-free 615-line Verify/Environment/Quot.lean). Sorry census non-Experimental: master 23 → iota 21; no new axioms; Experimental sorry count restored to master's 103.
- The one deferred obligation is expensive. `VEnv.WF.patsStrong` (EnvLemmas.lean:334, `sorry`) is the thesis's regularity of ι; it needs type-former injectivity, itself open on master. Because master's sorry-free `Ordered.strong` was DELETED and replaced by the new `OrderedStrong` structure, ~25 public signatures and ~115 call sites of master's substitution family and the entire `Verify/Environment/Primitive/*` layer were retyped from `Ordered` to `OrderedStrong` and are now conditional on that sorry (via `instance : CoeOut (VEnv.WF env) env.OrderedStrong` @ :343). PR_iota.md discloses this but its summary line 'nothing that was previously sorry-free at a bare Ordered hypothesis loses that' is checked-false as stated.
- Commit narrative shows two full reworks driven by real defects, not churn for its own sake: M3 (1a1ebe8) fixed an interface that provably did not compose for a client of the theory (opaque `∃ r`, no link from the registered reduct to the kernel rule rhs); M5 (4016efa, f53b727) answered an adversarial review whose mechanised counterexample `cexB.lean` derived `(∀p:Prop,p→p) ≡ (∀p:Prop,p)` inside a `VEnv.WF` environment admitted by the old `WF`. Also: one revert pair (cb920e1/3bbdb22, which also briefly committed `.claude/`+`.mcp.json`), one commit with a gmail committer identity, and two commits duplicated with trproj (7a68882≡6fd8a1d, 3de1dcb≡b4aba6d) — the merge kept trproj's side, so the precomputed blame files mis-tag ~170 lines of ι work (WHNF.lean, Inductive/Reduce.lean, the toParams demo) as P.
- Biggest remaining weakness is unconsumed surface, not unsoundness. `Theory/Typing/InductiveParams.lean` (433 lines: PatsIota, the five proved Params side conditions, toParams, inductParams, crDefEq_of_induct) has ZERO references outside itself, and applies only to environments with no `def` and no `quot`. The advertised 'first internal consumer' `inductiveReduceRecCore.WF` (WHNF.lean:17) is itself consumed by nothing — `reduceRecursor.WF` is still `sorry` two lines below. And nothing constructs an `AddInduct` at all (`Verify/Environment.lean:208` `| inductDecl … => sorry`), so `VInductDecl.WF` — one of whose clauses, `rules_wf`, is explicitly not a kernel check — is never established for a real declaration. Exactly one ι export (`iota_rec`) has an in-repo consumer.
- Documentation hazard for the review: the three untracked notes in the repo root are two reworks out of date. IOTA_CONTRIBUTION.md and PR_DESCRIPTION_formal.md claim a `.safe` gate on `TrEnv'.induct`, 12 `IOTA-TODO` sorries, an unenforced positivity/universe/large-elim spec, `Aligned.addInduct` sorried, and `pat_uniq`/`pat_app_uniq` false — all five now checked-false against the code. PR_iota.md and PR_DESCRIPTION.md are current. The claims table in contrib-iota.md numbers all 33 claims with statuses (18 checked-true, 6 checked-false, 9 unverified) and grep/lean recipes; the notable unverified ones are the `#print axioms` cleanliness claims for `iota_defeq`/`pats_iota'` (no `#guard_msgs` in the repo for them) and 'cexA/cexB now refuted' (the in-repo test proves only `¬declB.LargeElimShape`, not `¬declB.WF ∅`).

## theory-proj-and-tests.json

This group is 100% contributed code: Theory/Proj.lean (449 lines, all trproj) models Expr.proj as a recursor expansion with the thesis's dependent motive (projFn, projMotiveBody, projTy, fieldSelector, instPis, instFields, binderArity?, plus ~30 subst/lift/inst/instL lemmas), and four Tests modules validate it — ProjShape kernel-checks the expansion on nine structure/field pairs (kernel typechecks it, agrees with inferProj, whnf-reduces P_i (mk ps fs) to fs[i]), ProjInhabit inhabits TrProjCtor on two hand-built blocks including the dependent Sigma-shaped case via a real IsDefEq.pat iota step plus a six-step beta chain, and IotaShape/ShapeDecide (iota branch) decide VInductDecl.WF against the kernel on 41 type formers with an unusually dense set of negative controls. Quality is high: no sorry in Theory/Proj.lean, proofs are tight (everything proved once against Subst, weakening/instantiation read off), documentation is best-in-class, and the lemma set matches exactly what TrProj.weak'/instN/instL and TrEnv.proj_defeq consume. The two real weaknesses are that the bridge from a kernel-accepted projection to TrProjCtor is still the sorry'd inferProj.WF_struct (the general derivation exists only as a docstring proof), and that the witnesses' coverage is narrower than it looks (np=0, usS=[], uss constant, at most two fields), so the per-field level list and the substitution lemmas are never exercised on the model side. The hinted "sorry" in ProjInhabit is not a literal sorry but the sorryAx recorded by the #print axioms guard on TrEnv.proj_defeq (lines 586-595).

- Lean4Lean/Verify/TypeChecker/InferType.lean:398 — inferProj.WF_struct is `sorry`; the general TrProjCtor construction exists only as the 15-line informal derivation in Theory/Proj.lean:38-52, mechanized on two instances
- Lean4Lean/Theory/Proj.lean:127 — VExpr.projTy is unused in the library (only ProjShape.lean:178) and the identity it documents, projTy … e = (projMotiveBody … i).inst e, is never proved
- Lean4Lean/Tests/ProjInhabit.lean:49 — VEnv.addInduct checks nothing and VInductDecl.WF/env.WF is never proved for either hand-built env, so the witnesses show satisfiability, not derivability in a well-formed environment
- Lean4Lean/Tests/ProjInhabit.lean:70 — uss is the constant `fun _ => [l1]` in both blocks and np=0, usS=[], so the per-field level list and projFn_subst/instL are exercised only on the kernel side (ProjShape.lean:126)
- Lean4Lean/Tests/ProjShape.lean:136 — the Sigma.snd level-list negative control catches any exception from checkProj, so an unrelated failure makes it pass vacuously
- Lean4Lean/Theory/Proj.lean:111 — projFns mentions its own predecessor twice in its defining equation, making the expansion exponential in the field index and doubling every induction
- Lean4Lean/Tests/ProjInhabit.lean:586 — the #guard_msgs axiom list for TrEnv.proj_defeq hardcodes seven axioms including sorryAx; brittle, and its prose attribution to patsStrong/unique typing/Pi-injectivity is unverifiable by the guard
- Lean4Lean/Theory/Proj.lean:52 and :136 — two doc overclaims: "only forward rules of IsDefEq are used" (symm/trans are used throughout ProjInhabit) and binderArity? "has exactly the nf fields as binders" (it pins only the count)

## thesis-map.json

Thesis mapped in full (61 items, 8 chapters): lean4lean realises thesis §2–§4 only; iota replaced two `sorry` stubs with a real §2.6 spec but introduced the admitted `patsStrong`; trproj's "thesis's dependent motive" cites the W-type chapter, which lean4lean does not formalise.

- Coverage: lean4lean's Theory/ realises thesis §2 (axioms) and parts of §3–§4 (regularity/weakening/substitution, unique typing, Church-Rosser). §5 (W-types), §6 (ZFC model, soundness, relative consistency) are ENTIRELY unformalised, and §7 (normalization) and §8 (compilation) are themselves stubs ending in 'UNFINISHED' — as is §6.5. lean4lean's top-level theorem is kernel correctness, never consistency. Also unformalised: the algorithmic relation ⇔ of §2.3 has no inductive judgment anywhere, so the thesis's results about it (non-transitivity, failure of subject reduction, 'algorithmic implies definitional') have no statements to point at.
- iota baseline matters: on master, Theory/Inductive.lean was SEVEN LINES — `def VInductDecl.WF := sorry` and `def VEnv.addInduct := sorry`. Thesis §2.6 had no counterpart at all. iota replaced both stubs with ~450 lines of specification + ~1200 lines of lemmas, added VEnv.pats/addPat/IsDefEq.pat/Ordered.pat/PatTyped/PatWF/addRecRule (master had the Pattern machinery but NOTHING registered a pattern), and supplied the FIRST instance of the VEnv.Params class (toParams/inductParams), which Church-Rosser had been conditional on since master with no instance.
- iota cost: VEnv.WF.patsStrong (EnvLemmas.lean:334) is `sorry`. It is exactly the thesis's subject-reduction lemma (typesys.tex 'Regularity continued' item:red_equiv) specialised to ι, and its docstring says so. Master's `Ordered.strong` was unconditional; on iota it becomes `VEnv.WF.strong` with a PatsStrong premise, so VEnv.WF → OrderedStrong now routes through the sorry and IsDefEq.uniq (unique typing) plus everything that depends on it — including trproj's TrEnv.proj_defeq — now depend on sorryAx where the general metatheory previously did not. The repo is transparent (a #print axioms guard in Tests/ProjInhabit.lean names the three causes), but it is a real regression to weigh.
- Faithfulness gaps in the ι/inductive spec, all documented in-source but all real: (1) thesis §2.6.3's minor premise ε_c = ∀b::β.∀v::δ. C p[b] (c b) with δ_i = ∀x::ξ_i. C π_i[b,x] (u_i x) is only partially pinned — MinorFor/MinorHeaded fix the head and last-argument head constant, the inductive-hypothesis binders are unconstrained; (2) §2.6.4's recursive calls v are pinned only in NUMBER (RuleShape's recArgs.length), not shape, so a reduct need not be the thesis's; (3) K-like reduction is NOT modelled at all (VRecursor.k recorded and unused) though the kernel implements it; (4) conversely lean4lean registers ι for subsingleton blocks too, where thesis §4.1 restricts ι to non-SS and covers the rest by K⁺ — so lean4lean diverges from §4 on both sides of that split, undiscussed; (5) thesis §4.1's rec-normal-form/η-expansion device has NO counterpart; (6) positivity is the kernel's checkPositivity, not the thesis's ctor grammar, and no lemma relates them.
- trproj's thesis claim, checked: the thesis has NO treatment of Lean's Expr.proj, structure η, or inferProj. Projections appear only (i) as PRIMITIVE Σ-projections in Wtypes.tex §5.1 — `Γ ⊢ p : Σx:α.β ⟹ Γ ⊢ π₂ p : β[π₁ p/x]`, in the system built to REPLACE general inductives, which lean4lean does not formalise — and (ii) as the `inv_x` idiom in typesys.tex §3.1, introduced there to prove conversion UNDECIDABLE. Theory/Proj.lean cites both plus §2.6.3's κ for the per-field level list; each citation is individually accurate but it is a borrowed idiom, not an inherited result. The realisation is sound work (TrEnv.proj_defeq DERIVES π_i(a,b) ≡ a_i rather than assuming it), but structure η is admittedly absent from IsDefEq (tryEtaStructCore.WF open) — giving a three-way disagreement: thesis ADDS Σ-η it admits Lean lacks; Lean's kernel HAS structure η; the model has none. inferProj.WF and the new inferProj.WF_struct are both `sorry`.
- Pre-existing master holes the review should not attribute to either branch: Theory/Typing/Injectivity.lean is three `sorry`s and nothing else (the thesis's Definition 4.3 definitional inversion, which the thesis obtains SEMANTICALLY via §6.4 tagged types, and which patsStrong's docstring names as the missing ingredient); ChurchRosser.lean:1193,1212 are two `sorry`s in the thesis's Lemma 4.8; reduceRecursor.WF and inferProj.WF are `sorry`; UniqueTyping.lean:174 has one. Neither branch touched Injectivity.lean.
- Doc drift: across iota+trproj the documentation delta is ONE LINE in divergences.md (the checkEqType/unsafe-Eq entry). README.md was not touched at all — its Theory breakdown still omits LevelSat.lean, Proj.lean and Typing/{ChurchRosser,EnvLemmas,HeadReduction,InductiveLemmas,InductiveParams,Injectivity,Pattern}.lean, its Verify breakdown omits Environment/Quot.lean and all of Environment/Primitive/, and it still calls UniqueTyping.lean 'conjectures about the typing relation' although unique typing is proved there now. Also (pre-existing): Lean4Lean.Tests is not in lakefile defaultTargets, so the IotaShape/ProjShape/ProjInhabit/ShapeDecide kernel-validation guards do not run on a plain `lake build`.
- Two design choices with outsized knock-on effect, both worth questioning: (a) δ (§2.5) and the quotient ι_q (§2.7.1 — which the thesis explicitly calls an ι rule) are modelled as VDefEq definitional axioms, not as `pats` rules, so VEnv.DefEqsAsPats fails for any environment containing a single `def` or an initialised Quot — the new Params/Church-Rosser instance therefore applies only to axioms-plus-inductives environments; (b) level inequality (§2.2) skips the thesis's syntactic derivation system entirely and takes the semantics as the DEFINITION (VLevel.LE := ∀ ls, eval ≤ eval), so lean4lean is complete by fiat, the thesis's algorithm is nowhere verified, and divergences.md separately records that lean4lean decides more (in)equalities than either the kernel or the stdlib.

## theory-inductive.json

This group is the heart of the iota contribution: it replaces master's `def VInductDecl.WF := sorry` / `def VEnv.addInduct := sorry` (Theory/Inductive.lean was 7 lines on master) and master's `addInduct_WF := sorry` with a full specification of mutual inductive blocks (positivity, result type, large elimination, recursor/constructor/iota-reduct shapes), a four-stage environment extension, and complete proofs that it preserves `Ordered`; Pattern.lean gains the `Generic`/`Realizes`/`TemplateHeaded` apparatus behind the new `IsDefEq.pat` rule plus `SimplePattern.iotaRHS` and the `iotaRHS'_apply`/`matches_varN_const` bridge the verified WHNF uses, and InductiveParams builds `VEnv.PatsIota` and the first concrete `Params` instance for Mario's abstract Church-Rosser development. Quality is high: the de Bruijn arithmetic of all six shape predicates checks out by hand and against `Nat.rec`/`Eq.rec`, the staging of `VInductDecl.WF` makes the orderedness proofs three lines each, there is no `sorry` in any of the four files, the additions to Pattern.lean are strictly additive (`git diff master` shows no deletions), and the specifications are validated by `decide` against real kernel data in Tests/IotaShape.lean. Documentation is the best in the repo, with per-clause thesis references (axioms.tex 2.6.1-2.6.4) and explicit "not modelled" disclosures. The serious caveats are that nothing derives `VInductDecl.WF` from the kernel's own checker (TrEnv'.induct assumes it), that `toParams` only applies to environments with no `def`/`quot` at all so the `Params` demo is a toy, and that `crDefEq_of_induct` silently inherits ChurchRosser's two open sorries. Branch note: `git diff iota trproj` on these four files is empty apart from two line re-wraps and one docstring rewrite, so every P-tagged block here is byte-identical on `iota` (first committed on trproj as b4aba6d, ported to iota as 3de1dcb) - trproj contributed nothing of substance to this group.

- Lean4Lean/Verify/Environment/Basic.lean:583 - `TrEnv'.induct` assumes `decl.WF env`; nothing derives `VInductDecl.WF` from Lean4Lean/Inductive/Add.lean, so the spec is still an axiom of the refinement (validated only by decide-tests)
- Lean4Lean/Theory/Typing/InductiveParams.lean:426 - `crDefEq_of_induct` is documented as "Church-Rosser" but `IsDefEq.church_rosser` still has two sorries (ChurchRosser.lean:1193, :1212); the docstring does not say so and nothing uses the theorem
- Lean4Lean/Theory/Typing/InductiveParams.lean:393 - `toParams` needs `DefEqsAsPats`, which fails for any environment containing a `def` or `quot`; the only instance built (`inductParams`) is a single-block toy environment
- Lean4Lean/Theory/Inductive.lean:174 - `RuleShape` pins only the number of recursive arguments in the reduct, not their shape (thesis 2.6.4 requires v_i = lambda x. rec C e pi_i (u_i x)), so WF admits iota rules Lean would never generate
- Lean4Lean/Theory/Inductive.lean:257 - K-like reduction is unmodelled (`VRecursor.k` recorded but unused) while the kernel performs it (Inductive/Reduce.lean:109); Eq/subsingleton recursors reduce in the kernel but not in the model
- Lean4Lean/Theory/Inductive.lean:93 - `FieldPositive`/`universes` read manifest Pi-binders where the kernel whnfs (Add.lean:190), making the model strictly stricter than the kernel and obstructing any future derivation of WF from the checker
- Lean4Lean/Theory/Inductive.lean:196 - dead code: `RecShape.one_le_numMotives`, `RecShape.majorFormer?_eq` (:202), `LargeElim.shape` (:242), and the 8-declaration `RHS.spine`/`iotaCounts` cluster at Pattern.lean:509-678 (~70 lines) are referenced nowhere
- Lean4Lean/Theory/Typing/InductiveLemmas.lean:451 - placement: `nodup_map_inj_on` is a pure List lemma in namespace `VEnv`, and the generic foldlM/addConst toolkit plus `addQuot_*`/`addDefEqs_*` live in a file named InductiveLemmas

## contrib-trproj.md

trproj turns master's opaque `TrProj := sorry` into a real recursor-expansion model of structure projections, proves `TrEnv.proj_defeq` and 6 of 8 structural lemmas (projection-side sorries 9 → 4), but has zero in-library consumers and inherits the iota branch's unresolved soundness blockers

- Design: `.proj S i e` is modelled as `S.rec (uss i) ps (λx. F_i[f_j := P_j x]) (λf. f_i) e` — the chained dependent motive with per-field level lists. Key decls: `Lean4Lean.TrProjCtor` (Verify/Typing/Expr.lean:90, an 8-field Prop structure), `Lean4Lean.TrProj` (:133), the builders in the new `Lean4Lean/Theory/Proj.lean` (449 lines, 100% trproj), and `Lean4Lean.TrEnv.proj_defeq` (Verify/Environment/Lemmas.lean:1021, a fully proved ~130-line theorem). Justified by upstream's own divergences.md argument (Expr.proj no more powerful than the generated recursor); thesis links are `typesys.tex:11-17` (inv_x) and `Wtypes.tex:26-28` (π₂), but the thesis never models Expr.proj and has no chained motive — the commit title '811a52a define TrProj by the thesis's dependent motive' overclaims.
- Commit narrative: 24 commits in 5 milestones with TWO design reversals. The motive changed free → constant → chained-dependent (7a5e96d fixed non-functionality that would have made TrProj.uniq false; 811a52a fixed the constant motive's inability to type Sigma.snd). proj_defeq's statement changed three times (ctor buried → TrProjCtor → kernel structure facts). M4 (2026-09-08, 11 commits) is a direct response to REVIEW_2026-09-07: TrProjCtor made a structure (F13), IotaRule packaging (F28), inferProj.WF split into an honest WF_struct + a 'not provable as stated' general one (B3), inhabitation tests added (F19), Proj.lean reproved once against VExpr.subst cutting ~405 lines (F12).
- Sorry ledger verified by grep: master 23 real sorries → iota 21 → trproj 16, matching PR_trproj's claim exactly. Projection side: 9 (def + 7 lemmas + inferProj.WF) → 4 (TrProj.weak'_inv :747, TrProj.uniq :995, inferProj.WF_struct :398, inferProj.WF :410). No new axioms, no native_decide, zero IOTA-TODO/PROJ-TODO markers remain, no over-100-column lines added. TrProj.uniq is the load-bearing gap: TrExprS.uniq (Lemmas.lean:998) inducts through it.
- 35 claims catalogued with verify recipes. Notable checked-FALSE: TRPROJ_CONTRIBUTION.md is comprehensively STALE (describes the abandoned constant-motive design, cites 15 IOTA-TODOs that no longer exist, cites TrEnv.pats_iota_inv which was deleted) — it must not be handed to a reviewer; PR_trproj.md omits that the branch refactors the EXECUTABLE kernel (Inductive/Reduce.lean, 34 lines — behaviour-preserving, I verified the diff); the earlier claim that proj_defeq 'is the interface the proj case of TrExprS soundness consumes' is false (grep shows zero consumers; reduceProjCore.WF is still sorry). Commission Cluster 2 (IsDefEqU.weakN_iff forward) was honestly NOT delivered and NOT faked.
- Quality assessment: strong proof hygiene (real 130-line proof, two-sided testing — kernel-side ProjShape.lean validates the builders against the actual Lean kernel with negative controls, model-side ProjInhabit.lean inhabits TrProjCtor sorry-free with #guard_msgs axiom pins), excellent module documentation, and the d7ec0fa refactor generalises a master lemma. Weaknesses to flag for review: TrProj carries HasType side conditions (major_ty/fn_ty) making .proj the only non-syntactic TrExprS clause; `uss : Nat → List VLevel` admits junk; VExpr.projTy is test-only; proj_defeq/structure_rec/ctor_arity/pats_iota_inv_shape have NO consumer in lean4lean; and structure η is absent, which diverges from the thesis's own soundness model (Wtypes.tex:92-93 admits (π₁x,π₂x) ≡ x). Upstreaming is blocked by iota-side issues (B1 VInductDecl.WF admits inconsistent WF environments, B2 patsStrong plausibly false, B4 does not build against current master), not by the projection work itself.

## blueprint-tooling.md

leanblueprint 0.0.20 fully scouted and tested end-to-end (pdf + web + checkdecls) in a throwaway scratch scaffold; report at scratchpad/understand/blueprint-tooling.md

- Architecture is newer than most docs describe: \lean/\leanok/\uses/\proves/\mathlibok/\notready/\alsoIn/graph-colouring are plasTeX Python plugin classes (Packages/blueprint.py + plastexdepgraph), not TikZ macros in blueprint.sty (now a 4-line stub). Task brief's `\alsoin` is wrong case — actual macro is `\alsoIn`; `\leanurl` does not exist at all (grepped source + upstream README, no matches).
- `leanblueprint new` is non-interactive-drivable by piping one answer per line (verified against a copy of lean4lean's real lakefile.toml); it never touches the lakefile unless you separately confirm the checkdecls/docgen prompts. All commands (not just `new`) require being inside a git repo with a lakefile.lean/.toml at root, checked at import time.
- checkdecls (read its Main.lean source) just does `lake exe checkdecls <file>` -> imports every lean_lib root module -> `env.contains name.toName` per line; no smarts for private/instance names. Since we can't touch lean4lean's lakefile, recommended read-only-safe substitute: use the already-available mcp__lean-lsp tools (or `lake env printenv` + a scratch #check script) to verify \lean{} names instead of adding the checkdecls git dependency.
- Three pitfalls empirically confirmed by actually breaking things in the test scaffold, not just read from code: (1) a proof nested inside \begin{theorem} (vs. as a sibling after \end{theorem}) makes the auto proof-matcher silently fail to set proved_by, so the node gets no 'fully proved' fill colour with zero warning; (2) a duplicate \label silently merges two graph nodes into one, whichever was parsed last wins colouring/edges, no warning at all; (3) math in a \chapter/\section title (e.g. `$\alpha$`) builds fine in `leanblueprint web` but is a FATAL 'Improper alphabetic constant'/Emergency stop in `leanblueprint pdf` (hyperref+unicode-math bookmark clash) — needs \texorpdfstring{}{}.
- Full tested recipe documented: exact commands, required `export PATH=.venv/bin:$PATH` fix (plastex is invoked via bare shell name and isn't found otherwise — this cost a failed run), the clean passing content.tex, resulting output files (blueprint/print/print.pdf, blueprint/web/index.html + dep_graph_document.html) and their exact dot-graph colours matching the derived colouring model.
- No 'coverage' or 'declaration_pattern' plastex.cfg option exists anywhere in this leanblueprint/plastexdepgraph/plastexshowmore installation (grepped all three); large-graph knobs that do exist are the dep_graph package options `dep_by=chapter`, `thms=...`, `nonreducedgraph`, `tpl=`, plus plastex.cfg's split-level/localtoc-level and the showmore plugin.
- lean4lean repo confirmed untouched throughout (git status before/after identical); all experimentation happened in scratchpad/bp-test/ with its own throwaway git repo and a copy of lean4lean's lakefile.toml, including a real (harmless) `lake` network fetch of batteries triggered only inside that scratch tree.

## census.md

Census done: 7710 user-level decls, 638 sorryAx-tainted, 110 axioms; the branches opened 5 new holes (patsStrong reaches 343 constants) but deleted 5 master sorries that were on definitions

- Environment census ran against the prebuilt oleans (HEAD 20ec229, Lean v4.33.0-rc2, nothing rebuilt). 20725 constants in Lean4Lean.*/Main reduce to 7710 user-level declarations: Theory 1594, Verify 2470, Experimental 2446, Tests 468, kernel 732. Branch attribution from the blame files: 6174 master, 606 iota, 312 trproj, 241 mixed, 377 unattributable (no declRange).
- Lean4Lean/Experimental/ CANNOT be imported as one unit — LogRel, MoreStepIndexed, StepIndexed, DomainTheory and ShapeLogRel all redeclare names in namespace Lean4Lean.SExpr (e.g. Lean4Lean.SExpr.Classifier), so importing two fails. The census therefore ran one pass per Experimental module (Census.lean = pass A for everything else; Census_<Mod>.lean for each Experimental module). Thierry.lean/Thierry2.lean import nothing at all and declare ~57 axioms in the ROOT namespace (D, proj, HasType, subst, mySorry, ...).
- 638 declarations reach sorryAx; only 16 of them are branch-attributed. Shortest dependency paths (computed by BFS, census/why.txt) show exactly five genuinely new holes: VEnv.WF.patsStrong (iota, Theory/Typing/EnvLemmas.lean:334, `:= sorry`), TrProj.uniq and TrProj.weak'_inv (trproj, Verify/Typing/Lemmas.lean:995 and :747), inferProj.WF and inferProj.WF_struct (trproj, Verify/TypeChecker/InferType.lean:410 and :398). All other branch sorries depend on these or on pre-existing master holes (NormalEq.parRed, IsDefEqU.forallE_inv_stratified).
- Reverse reachability over the 14826 non-Experimental constants (census/reach.txt): patsStrong is now the widest-reaching unproved statement in the project at 343 dependents (via WF.orderedStrong and the CoeOut instance at EnvLemmas.lean:343, which makes it reachable implicitly from any VEnv.WF); TrProj.uniq 138; TrProj.weak'_inv 92; inferProj.WF 61. Master's own worst are forallE_inv_stratified 282 and sort_inv 281. inferProj.WF_struct has 0 dependents — it is declared, documented, sorry-ed and used by NOTHING (inferProj.WF does not call it): a dead stub.
- Net sorry delta vs master is -6 literal lines (131 -> 125) but the qualitative change is larger: the master sorries the branches removed were on DEFINITIONS — master:Theory/Inductive.lean:5,7 had `def VInductDecl.WF := sorry` and `def VEnv.addInduct := sorry`, and master:Verify/Typing/Expr.lean:68 had `def TrProj ... := sorry`, which made every statement about inductives/projections vacuous. iota and trproj replaced all three with real definitions and discharged 5 of master's 7 TrProj sorries in Verify/Typing/Lemmas.lean.
- Trust boundary is master's, not the contribution's: 110 declared axioms, of which 32 in Verify/Axioms.lean plus 3 in Verify/Level.lean, 4 in Verify/Expr.lean, 2 in PtrEq.lean assert that Lean core's Expr/Level/PersistentHashMap/Syntax implementations match the model. Lean.PersistentHashMap.WF.toList'_insert and WF.find?_eq are each pulled in by 231 declarations. Lean.Level.instLawfulBEqLevel (Verify/Axioms.lean:292) is an `@[instance] axiom` — instance resolution introduces it silently into 152 declarations. Seven `_native.bv_decide.ax_*` axioms come from bv_decide LRAT certificates (two reach 90 declarations each). 12 opaques, mostly `unsafe`-implemented kernel helpers.
- Build-configuration gap worth flagging for the review: lakefile.toml sets defaultTargets = ["Lean4Lean", "lean4lean", "Lean4Lean.Theory", "Lean4Lean.Verify"], so a bare `lake build` compiles NEITHER Lean4Lean.Tests NOR Lean4Lean.Experimental. Lean4Lean/Tests.lean imports only Toolchain, Environment, LevelStd — not IotaShape, ProjInhabit, ProjShape, ShapeDecide. The contributed worked instances (Tests/IotaShape 208 decls, Tests/ProjInhabit 148, Tests/ProjShape 22, Tests/ShapeDecide 26, all sorry-free) exist as oleans here only because they were built explicitly. Also: Theory/LevelSat.lean (59 decls) is imported by nothing, Lean4Lean/Theory.lean does not reach Theory.Proj, and three stale oleans with no source survive in .lake/build (Verify/Environment/Boundaries, Theory/Pattern, Std/Variable!).
- Contributed regions extend well beyond the file list I was given — a blueprint restricted to it will under-report: Theory/VExpr.lean (I=200, P=195), Theory/Typing/Strong.lean (I=164, where PatsStrongOn/OrderedStrong are defined — absent on master), Theory/Typing/EnvLemmas.lean (I=237), Verify/TypeChecker/WHNF.lean (P=141), Verify/Typing/Lemmas.lean (P=131), Verify/Typing/Expr.lean (P=70), Theory/Typing/Lemmas.lean (I=74), Std/SMap.lean (I=54), Inductive/Reduce.lean (P=21). Full per-file M/I/P table is in census.md section 7.4.
- Method caveats recorded in census.md section 0: the kind column reports `instance` in preference to the ConstantInfo case (so axiom/opaque lists were recomputed from all-constants.tsv, which keeps the raw kind); private decls are kept under their de-privatised name with a 7th column marking them; `X.rec_1`/`X.rec_2` and `X.below.<ctor>` slip past the internal-name filter; Verify/Environment/Basic.lean and Lemmas.lean have mtimes newer than their oleans but are clean in git, so content matches HEAD.

## upstream-comparison.md

Network was available. Upstream master has NOT moved past 8223d22 (byte-identical to the fork's master; no push since 2026-08-29), and neither upstream master nor any of its 10 other branches implements ι rules, an inductive-block spec, TrProj, or a non-vacuous quotient init — the branches target holes upstream's own docstrings flag as open. But the user already has an open, un-reviewed upstream PR (#43, iota only) whose CI has been gated on action_required since 2026-08-18, trproj has never been proposed at all, and the direct precedent (PR #32, +24k lines, closed unmerged as superseded) makes maintainer supersession, not git conflict, the real risk.

- Upstream master HEAD = 8223d223ed98661882e95d9d6a7126df7097cd76, identical SHA to the fork's master, so zero commits since; repo pushed_at = 2026-08-29T18:50:24Z (16 days quiet). Verified independently by byte-comparing 11 raw.githubusercontent files at that SHA against `git show master:<path>` — all 11 SAME.
- Upstream Theory/Inductive.lean is the whole file in 7 lines: `VInductDecl.WF … := sorry` and `VEnv.addInduct … := sorry`. addInduct_WF in InductiveLemmas.lean is also sorry. No ι registry exists: repo-wide grep for `VEnv.pats`, `\bpats\b`, `IsDefEq.pat`, `addPat` returns 0 hits; upstream VEnv has exactly two fields and IsDefEq has no `pat` constructor.
- Upstream Verify/Environment/Basic.lean:101-113 declares `inductive AddInduct … : Prop` with `-- TODO` and no constructors, docstringed "This definition is essentially a `sorry` … the `TrEnv'.induct` case below can never fire"; `AddInduct.to_addInduct` is proved `nomatch H`. Upstream Verify/Typing/Expr.lean:68 is `def TrProj : … := sorry`, with 7 further TrProj sorry lines in Verify/Typing/Lemmas.lean.
- Partial overlap only: upstream Theory/Typing/Pattern.lean:226-232 already has `SimplePattern.iota`/`toPattern`, but it is used solely by the *assumed* `Params` typeclass in ChurchRosser.lean and in Experimental/ — nothing constructs an instance from a real environment. The fork's Theory/Typing/InductiveParams.lean is 404 upstream (fork-only, 423 lines).
- Quotient init is the one area upstream really has (AddQuot1/AddQuot/TrEnv'.quot), but its consumer is self-described as dead: "This is currently vacuous in the non-initialized case: `TrEnv` cannot contain the inductive `Eq` declaration until `AddInduct` is implemented", discharged by `False.elim`. The iota branch's new 615-line Verify/Environment/Quot.lean re-proves addQuot.WF for real — an unadvertised strengthening.
- All 10 other upstream branches (cpp2026, cpp2025, logrel, differential, arena, arena-v4.29.0, types2025, itp2024, v4.27.0-rc1, bitvec_example) carry the identical 6-line Inductive.lean stub, 0 `pats` in VEnv.lean, and `TrProj := sorry`. Nothing anywhere upstream duplicates this work.
- Hole ledger: iota closes 5 upstream holes (VInductDecl.WF, VEnv.addInduct, addInduct_WF, empty AddInduct, vacuous addQuot.WF) and adds 1 admitted lemma (VEnv.WF.patsStrong, EnvLemmas.lean:334). trproj additionally closes `TrProj := sorry` plus 5 of 7 TrProj lemmas, adding 2 inferProj sorries + 3 in Tests/ProjInhabit.lean. Cross-checked against sorry-grep.md's declRangeExt/sorryAx classification.
- The top-level boundary is NOT closed on either branch: `addDecl.WF | inductDecl _ _ _ _ => sorry` remains in Verify/Environment.lean, so the new AddInduct structure is a well-specified object nothing in the verified pipeline yet produces. The author states this himself in his 2026-08-11 PR comment.
- digama0/lean4lean#43 "Iota Reduction" by barabbs is OPEN since 2026-08-08 (head barabbs:iota@38ea0de4 → master@8223d223, +5628/−331, 45 files, mergeable:true, mergeable_state:unstable). It has ZERO human review: all 7 inline comments and the sole review are from copilot-pull-request-reviewer[bot] posted 5 minutes after opening, against the first push; the only 2 thread comments are the author's own.
- CI has never run on the fixed code: workflow runs on branch `iota` are failure (08-08, 08-08, 08-11) then action_required (08-18, 09-08, 09-10) — GitHub's first-time-contributor gate. `/commits/38ea0de4/check-runs` → total_count 0, status pending. Upstream's last visible verdict on the PR is a red run from 2026-08-11 (the Experimental build break the author fixed on 08-18).
- digama0 has been subscribed to #43 since 2026-08-10T07:55 and never replied, while commenting on #26/#27/#20/#22/#33/#11/#32 in the same window. On 2026-08-31 he wrote on PR #32 that the inductive case is "Still open … what is missing is a constructive `AddInduct` whose added constants are exactly those" delivered by checkInductive.WF as AddsConsts — exactly the iota deliverable, described as missing. `git grep AddsConsts trproj` finds only unmodified upstream code, so that named handshake is genuinely not made.
- Supersession precedent and unsubmitted half: PR #32 (kim-em, +24247/−1264, 33 files) was closed UNMERGED 2026-08-31 as superseded by Mario's own master work, with a closing argument explicitly favouring the smaller diff. barabbs/lean4lean has 0 PRs of its own, and trproj's entire projection contribution (Theory/Proj.lean 449 lines, ProjInhabit 597, ProjShape 185, the TrProj definition and lemma proofs) appears in no PR — upstream has never seen it. Branches also change 3 existing interfaces (VEnv field arity, TrProj arity, AddInduct arity).

## unused-contrib.md

31 of 875 author-written contributed declarations (3.5%; 19 iota, 12 trproj) have no reference anywhere — a lower dead rate than upstream's 11.2%, and all of it leaf lemmas, structure fields or test theorems; the real finding is wiring, not volume: trproj's headline `TrEnv.proj_defeq` is reached only by a test's `#print axioms`, and its `lift'_inst_hi` refactor orphaned a 6-declaration upstream cluster in `Theory/VExpr.lean`.

- Population: 875 author-written contributed declarations (553 iota / 322 trproj) after skipping ctor/instance/rec and 33 Lean-generated names (`below_N`, `brecOn_N`, `ctorElim` in Tests/IotaShape.lean; `decEq` from two `deriving instance DecidableEq` lines in Tests/ShapeDecide.lean).
- 31 have zero real references (iota 19 / trproj 12 = 3.4% / 3.7%); outside Tests/ it is 21/635 (3.3%), inside Tests/ 10/240 (4.2%). Full table in unused-contrib.md §2 and unused-contrib.json.
- Method mattered: a plain word-boundary grep is unsound here because Lean4Lean/Experimental/SExpr.lean is a parallel copy of the VExpr substitution theory duplicating names (lift_r_one, Subst.trunc, lift'_inst_hi). I resolved every dotted identifier against the enclosing namespace stack, `open`s and the import closure, plus every dotted prefix; unresolvable dot-notation falls back to namespace proximity (flagged as a residual caveat).
- Using 'next decls.tsv entry' as the body end (as the brief suggested) yields 8 false deaths — it swallows following `example`/`#guard`/`run_meta` blocks (e.g. Tests/ProjShape.lean:160 `P₀`, used by four examples; Tests/ProjShape.lean:68 `checkProj`, called 11× from a `run_meta do` at :121).
- Four automatic zeros were false positives from proximity attribution and were removed after grep verification: AddInduct.find?_mono / rec_find / ctor_find (used as `h2.find?_mono`, `hadd.rec_find`, `hadd.ctor_find` in Verify/Environment/Lemmas.lean:563,570,733,735,631; collide with AddQuot./TrEnv'.IotaRule. homonyms) and VEnv.HasPrimitives.trBool (Primitive/Basic.lean:85,1116,1123; collides with a local `def trBool`). Four more were macro/notation-generated names whose syntax is used (tacticQuot_simp/mem → `quot_simp`/`quot_mem`; termU/termV → notations `u`/`v`).
- Composition of the dead set: 29 theorems + 2 structure-field projections — no unused definition, inductive or structure. 9 leaf theory lemmas (RecShape.one_le_numMotives, RecShape.majorFormer?_eq, LargeElim.shape, WF'.pats_origin, iotaRHS_iotaCounts, Lift.liftVar_consN_lt/succ, projMotiveBody_zero, instFields_nil), 10 never-projected structure fields (7 of iota's `VInductDecl.WF` + Dep.v, V3.h, Refl.next), 3 lemmas referenced only by their own recursive proof (insertConsts_find?_none, Expr.getAppFn_mkAppList, lamBinders_length), 7 test theorems, 2 unreferenced `sorry`-carrying theorems.
- Two unreferenced declarations still carry `sorry`: VEnv.IsDefEq.crDefEq_of_induct (Theory/Typing/InductiveParams.lean:426, trproj) and TypeChecker.Inner.inferProj.WF_struct (Verify/TypeChecker/InferType.lean:392, trproj) — unfinished and unused, though both are advertised in docstrings elsewhere.
- Four of the dead lemmas are `@[simp]` (projMotiveBody_zero, instFields_nil, lamBinders_length, getAppFn_mkAppList) and so may be reached through simp sets with no textual reference — flagged, not claimed dead-dead.
- Used only from Tests/ (9): the trproj headline theorem `TrEnv.proj_defeq` (Verify/Environment/Lemmas.lean:1021) has no library consumer — its sole outside mention is `#guard_msgs in #print axioms Lean4Lean.TrEnv.proj_defeq` at Tests/ProjInhabit.lean:595; `VExpr.projTy` (Theory/Proj.lean:127) only in two `example`s at Tests/ProjShape.lean:178-179; the four `*_iff` lemmas exist to build Decidable instances in Tests/ShapeDecide.lean (legitimate test-only purpose).
- Nine dead declarations are named only in prose/strings: the five §2.6 `VInductDecl.WF` fields (throwError strings + prose in Tests/IotaShape.lean:234,255,257,259 and :285,488-497), matchPat_sound (IotaShape.lean:102), projMotiveBody_zero (Theory/Proj.lean:25), crDefEq_of_induct (InductiveParams.lean:391), inferProj.WF_struct (Tests/ProjInhabit.lean:15, Verify/Typing/Expr.lean:125, InferType.lean:401).
- Master orphaned by the branches (§5): exactly one head, `VExpr.lift_r_one` — master proved `lift'_inst_hi` at Theory/VExpr.lean:903 in one line from it (:897); trproj moved lift'_inst_hi to :1018 and re-derived it from the new lift'_instN_hi (:1014), leaving a 6-declaration cluster with no live consumer: lift_r_one (:919), Subst.lift_r_comm (:911), Subst.trunc (:908), Subst.Depth.one (:885), Subst.Depth.id (:858), Subst.Depth (:796). EquivManager.WF.empty and InferCache.WF.empty (Verify/TypeChecker/Basic.lean:93,234) are NOT collateral — their apparent master reference is a `| empty =>` case tag in Extension.lean:21; they are pre-existing upstream dead code. No master declaration was orphaned by iota.
- Baseline for judgement: the same resolver over master-side declarations finds 383/3422 core (non-Experimental, non-Tests) declarations with no in-repo consumer — 11.2%, versus 3.3% for contributed non-Tests code. The dead code does not read as speculative over-production; the criticisms that survive are wiring-level (proj_defeq unconnected, two unreferenced sorries left in place, six upstream declarations orphaned without removal).
