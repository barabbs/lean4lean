# Claim verification -- branch `trproj` (`20ec229`)

Method: every claim below was checked against the read-only checkout at
the repository root (branch `trproj`, HEAD `20ec229`,
`master` = `8223d223`), using `git grep/diff/show/log/blame`, and `lake env lean` on scratch files
under `scratchpad/claims/` (`#print axioms`, a `run_cmd` walk of the constant-dependency graph to
compute each theorem's *sorry leaves*, and re-elaboration of the two test files and the review's
own probes/counterexamples). No `lake build`, no writes to the repo.

Verdict key: **TRUE** / **FALSE** / **PARTIAL** (true in substance, wrong or incomplete in a
stated particular) / **UNVERIFIABLE** (needs evidence not present in either checkout).

| id | claim | source | verdict | evidence |
|---|---|---|---|---|
| 1 | `TrProj` was `sorry` on master, and with it every lemma about it (9 sorries on the projection side). | PR_trproj.md Summary; contrib-trproj.json #1 | **PARTIAL** | `git grep -n -w sorry master -- Lean4Lean/Verify/Typing/{Expr,Lemmas}.lean` -> Expr.lean:68 (`def TrProj ... := sorry`) plus Lemmas.lean:642,723,727,893,938,1240,1509 = 8. The 8th lemma, `TrProj.weakN`, was NOT sorry on master: `git diff iota..trproj -- Lean4Lean/Verify/Typing/Lemmas.lean` shows it removed as `theorem TrProj.weakN (W : Ctx.LiftN n k ...) ... := by simpa [VExpr.lift'_consN_skipN] using H.weak' <\| Ctx.liftN_iff_lift'.1 W` -- a real proof off the sorried `weak'`. The count 9 holds only if master:Lean4Lean/Verify/TypeChecker/InferType.lean:391 (`inferProj.WF`) is counted; contrib-trproj.md's prose scopes the 9 to the two Typing files, where it is 8. |
| 2 | `TrProjCtor` is a `Prop`-valued structure with named fields `params_length, ctor, field_lt, minor_arity, major_ty, fn_ty, eq`. | PR_trproj.md; contrib-trproj.json #2 | **PARTIAL** | Lean4Lean/Verify/Typing/Expr.lean:90 `structure TrProjCtor (env : VEnv) (U : Nat) ... : Prop where` with fields at :93 `pat`, :99 `params_length`, :107 `ctor`, :109 `field_lt`, :116 `minor_arity`, :119 `major_ty`, :126 `fn_ty`, :131 `eq` -- 8 fields. The PR list omits the first, `pat` (the registered iota rule), which is the field carrying the ι-registry dependence. |
| 3 | `TrProj.weak'`, `.weakN`, `.mono`, `.instN`, `.instL`, `.wf` and `.defeqDFC` are proved; they were `sorry` on master. | PR_trproj.md; contrib-trproj.json #3 | **PARTIAL** | Proved: the only `sorry` tokens in Lean4Lean/Verify/Typing/Lemmas.lean are :747 (`weak'_inv`) and :995 (`uniq`) (`grep -n -w sorry`). `lake env lean` + `#print axioms`: `TrProj.wf`=[propext], `.mono`=[propext], `.instL`=[propext,Quot.sound], `.weak'`/`.weakN`/`.instN`=[propext,Classical.choice,Quot.sound] -- all sorryAx-free; `.defeqDFC`=[propext,sorryAx,...]. But 'they were sorry on master' is wrong for two of the seven: `weakN` was proved on master (see #1) and `mono` did not exist on master at all (master's `TrExprS.mono` proj case was `exact .proj ih h2`). The PR's own Validation census correctly lists only weak'/instN/instL/wf/defeqDFC as closed. |
| 4 | `TrEnv.proj_defeq` is a theorem with no `sorry` of its own. | PR_trproj.md; REVIEW_2026-09-07.md §2; contrib-trproj.json #4 | **TRUE** | Lean4Lean/Verify/Environment/Lemmas.lean:1021-1178, a continuous tactic proof; `awk 'NR>=1021&&NR<=1200' \| grep sorry` -> no hits; file has no `sorry` at all (`grep -n -w sorry` on the file is empty). `lake env lean Lean4Lean/Verify/Environment/Lemmas.lean` elaborates with zero errors and zero `declaration uses sorry` warnings. |
| 5 | `proj_defeq` takes what `inferProj`/`reduceProjCore` actually check (non-mutual, single-constructor, non-indexed) rather than the recursor's telescope split. | PR_trproj.md; contrib-trproj.json #5 | **TRUE** | Lean4Lean/Verify/Environment/Lemmas.lean:1030-1032: `(hS : kenv.find? S = some (.inductInfo ival)) (hall : ival.all = [S]) (hctors : ival.ctors = [ctorName]) (hnind : ival.numIndices = 0)`. The split is *derived*, not assumed: `TrEnv.structure_rec` (:644) concludes `rval.numParams = ival.numParams ∧ rval.numMotives = 1 ∧ rval.numMinors = 1 ∧ rval.numIndices = 0 ∧ ∃ cval, ...` from exactly those facts, and is used at :1076. `#print axioms Lean4Lean.TrEnv.structure_rec` -> [propext, Classical.choice, Quot.sound, 3 PersistentHashMap axioms] -- sorryAx-free. |
| 6 | Sorry census non-`Experimental`: ι branch 21, trproj 16; no new axioms. | PR_trproj.md Validation; contrib-trproj.json #6 | **TRUE** | `git grep -n -w sorry {master,iota,trproj} -- 'Lean4Lean/*' \| grep -v Experimental`, discarding 5 comment/docstring hits on master (TypeChecker.lean:970, Environment/Basic.lean:101-102, Primitive.lean:8, Level.lean:27) and the analogous ones on the branches: master 23, iota 21, trproj 16. PR_iota.md's 'master 23, this branch 21' uses the same convention and agrees. Note REVIEW_2026-09-07.md §9's 'master 22 -> iota 21 -> trproj 15' mixes conventions: ChurchRosser.lean:1193 and :1212 are two sorries inside ONE declaration (`NormalEq.parRed`, :1181), so declaration counts are master 22 / iota 20 / trproj 15. `git diff master..trproj \| grep -E '^\+\s*(axiom\|opaque)'` -> only a docstring line. |
| 7 | Closed by this branch: the `TrProj` definition and `weak'`/`instN`/`instL`/`wf`/`defeqDFC`. New: `inferProj.WF_struct`. Projection-side sorries 9 -> 4. | PR_trproj.md Validation; contrib-trproj.json #7 | **TRUE** | 21 - 6 + 1 = 16, matching the census in #6. Projection side, master: Expr.lean:68 + Lemmas.lean x7 + InferType.lean:391 = 9; trproj: Lemmas.lean:747,995 + InferType.lean:398,410 = 4. (Reduce.lean:145 `reduceProjCore.WF` and IsDefEq.lean:227,488 are projection-adjacent and unchanged on both sides, so they cancel.) |
| 8 | The motive must be pinned to make `TrProj` functional; a free motive would make `TrProj.uniq` false (well-typedness constrains the motive only on constructor-shaped majors). | TRPROJ_CONTRIBUTION.md; commit 7a5e96d; contrib-trproj.json #8 | **UNVERIFIABLE** | The argument is stated only in the 7a5e96d commit message and TRPROJ_CONTRIBUTION.md; no counterexample is mechanized anywhere in the tree or in review-artifacts/. What IS verifiable is the consequence the argument was used to justify: the motive is now a function of the exposed data -- `VExpr.projFn S usS uss ps Fs i` (Theory/Proj.lean:120) reads `uss j` only for j <= i through `projFns` (:108), so two `TrProjCtor` witnesses at the same parameters give literally equal `e'`. Note the pinning the commit chose (the CONSTANT motive) was itself abandoned 8 days later in 811a52a. |
| 9 | The constant motive is correct for non-dependent fields; dependent Sigma/Subtype fields are out of scope. | TRPROJ_CONTRIBUTION.md §A1; contrib-trproj.json #9 | **FALSE** | Stale. The current motive is the chained dependent one: Theory/Proj.lean:114 `projMotiveBody ... := projMotiveBodyOf Fs i (projFns S usS uss ps Fs i)` with :91 substituting `f_j := P_j x`. Dependent fields are exercised and pass: `lake env lean Lean4Lean/Tests/ProjShape.lean` (clean exit) runs `checkProj ``Sigma 1`, ``PSigma 1`, ``Subtype 1`, ``V3 2` at split universes (ProjShape.lean:122-135), and Tests/ProjInhabit.lean inhabits a Sigma-shaped block (`Dependent.inhabDep1`, :555-560). |
| 10 | 15 `IOTA-TODO` markers unchanged; 3 `PROJ-TODO(soundness)` remain. | TRPROJ_CONTRIBUTION.md; contrib-trproj.json #10 | **FALSE** | Stale. `git grep -n 'IOTA-TODO\\|PROJ-TODO' trproj -- Lean4Lean` -> 0 hits. (At the review head f7dabf1 there were 4: InductiveLemmas.lean 1, InferType.lean 1, Verify/Typing/Lemmas.lean 2.) |
| 11 | `TrEnv.pats_iota_inv` is fully proved and sorryAx-free. | TRPROJ_CONTRIBUTION.md Round 2; contrib-trproj.json #11 | **FALSE** | Stale: the declaration no longer exists (`git grep -n 'pats_iota_inv\b' trproj -- Lean4Lean` -> nothing; it and `pats_iota_inv'` were dropped in 2a901f4/e202975). Its successor `TrEnv.pats_iota_inv_shape` (Verify/Environment/Lemmas.lean:903) IS sorryAx-free: `#print axioms` -> [propext, Classical.choice, Quot.sound, 3 PersistentHashMap axioms]. |
| 12 | The prior `TrEnv.proj_defeq` statement was unprovable because the ι rule's `ctorName` and the spine's `ctorName` were unrelated. | TRPROJ_CONTRIBUTION.md; commit b6a5a38; contrib-trproj.json #12 | **PARTIAL** | The remedy is real and checkable: b6a5a38 introduced `TrProjCtor` with `ctorName` as a parameter, and the current statement threads one `ctorName` through both `hp : TrProjCtor ... ctorName ...` and `hd : ... (VExpr.const ctorName cus).mkApps (params ++ fields)` (Lemmas.lean:1028,1036). 'Unprovable' itself is the author's assessment in the commit message, never mechanized (no negative witness in review-artifacts/). |
| 13 | The expansion's typing is derivable from the forward rules of `IsDefEq` alone -- no structure eta, no K-like reduction, no injectivity. | PR_trproj.md; Theory/Proj.lean:38-52; contrib-trproj.json #13 | **PARTIAL** | Verified on instances: `lake env lean Lean4Lean/Tests/ProjInhabit.lean` exits clean and its `#guard_msgs in #print axioms` pins `Plain.inhab0/inhab1` and `Dependent.inhabDep0/inhabDep1` at [propext, Quot.sound] (ProjInhabit.lean:563-578) -- no sorryAx, and `IsDefEq` has no structure-eta rule to use (Verify/TypeChecker/IsDefEq.lean:227,488 are the open obligations for it). Not verified in general: the general construction is `inferProj.WF_struct`, still `sorry` (Verify/TypeChecker/InferType.lean:398). |
| 14 | `TrProjCtor` is inhabited sorry-free for a plain and a Sigma-shaped dependent structure; `#print axioms` = [propext, Quot.sound] for all four witnesses. | PR_trproj.md Validation; commit acd6b46; contrib-trproj.json #14 | **TRUE** | `lake env lean Lean4Lean/Tests/ProjInhabit.lean` -> no output, exit 0; the four `#guard_msgs in #print axioms` at ProjInhabit.lean:563-578 would fail the file otherwise (negative control run: a deliberately wrong `#guard_msgs` does produce `error: Docstring on #guard_msgs does not match generated message`). Both blocks are declared through `VEnv.addInduct` (ProjInhabit.lean:8-11 docstring, and the `decl : VInductDecl` at :38). |
| 15 | `TrEnv.proj_defeq`'s sorryAx is inherited from unique typing, Pi-injectivity and `VEnv.WF.patsStrong`, not from itself. | PR_trproj.md; Tests/ProjInhabit.lean:582-595; contrib-trproj.json #15 | **TRUE** | Computed the sorry cone with a `run_cmd` walk of the constant-dependency graph (scratchpad/claims/cone2.lean): `Lean4Lean.TrEnv.proj_defeq => [Lean4Lean.VEnv.IsDefEqU.forallE_inv_stratified, Lean4Lean.VEnv.IsDefEqU.sort_inv, Lean4Lean.VEnv.WF.patsStrong]` -- exactly Pi-injectivity/sort-injectivity (Theory/Typing/Injectivity.lean:21,12, both `sorry` on master too) plus the ι branch's `patsStrong` (EnvLemmas.lean:334). `#print axioms` matches the file's own pin at ProjInhabit.lean:587-595. |
| 16 | Tests/ProjShape.lean: the kernel accepts `fun ps x => projFn x`, `projTy` isDefEq to `inferType (.proj S i x)`, `projFn (mk ps fs)` whnfs to `fs[i]`, and the reflexive control fails. | PR_trproj.md Validation; contrib-trproj.json #16 | **TRUE** | `lake env lean Lean4Lean/Tests/ProjShape.lean` -> no output, exit 0 (0.9s). The checks are real kernel calls: `(← getEnv).addDeclCore 0 512 (.defnDecl dv) none` (:104), `isDefEq tProj tExp` (:113), `whnf (.app ... mk)` then `red == fs[i]!` (:118-119). Negative controls at :137-145 (`Sigma.snd` with `Sigma.fst`'s level list must be rejected; `Refl.rec`'s minor must have `binderArity? 1 = some 2`) `throwError` if they pass. Cases run: Prod 0/1, Sigma 0/1, PSigma 1, Subtype 0/1, Fin 1, And 1, V3 1/2 (:121-132). |
| 17 | `minor_arity` excludes a reflexive structure, which the `np+1+1+0` key does not. | PR_trproj.md; Verify/Typing/Expr.lean:110-118; contrib-trproj.json #17 | **TRUE** | Two independent checks. ProjShape.lean:142-145 asserts `Refl.rec`'s `binderArity? 1 = some 2`, not 1, and the file elaborates clean. `lake env lean review-artifacts/probes/proj_gap.lean` prints `kernel ACCEPTS projRefl` and `Refl.rec: numMotives=1 numMinors=1 numIndices=0 binderArity?(np+1)=some 2 (nfields=1)` -- the key is satisfied, the arity pin is not. |
| 18 | `inferProj.WF` is not provable as stated: `inferProj` also accepts projections of reflexive, indexed and nested single-constructor types, e.g. core's `Lean.Language.SnapshotTree.element`. | PR_trproj.md Deferred obligations; REVIEW_2026-09-07.md B3; contrib-trproj.json #18 | **TRUE** | Re-ran the probes. `probes/proj_gap.lean`: `kernel ACCEPTS projRefl`, `kernel ACCEPTS projNest`, and `Nest.rec: numMotives=2 (TrProjCtor key requires 1)`. `probes/l4l_proj.lean`: `lean4lean ACCEPTS projRefl`, `lean4lean ACCEPTS projNest`. `probes/l4l_proj3.lean` (genuinely indexed `Idx2 : Nat → Type`, `mk : (a b : Nat) → Idx2 (a+b)`): `Idx2: numParams=0 numIndices=1` / `real kernel ACCEPTS .proj Idx2 0` / `lean4lean ACCEPTS .proj Idx2 0 : Nat`. `probes/core_proj.lean`: `SnapshotTree.element value: fun self => self.1` / `contains .proj node: true`, and proj_gap reports `rec numMotives=5 numMinors=6` for SnapshotTree. (The simpler `Idx : Nat → Type`, `mk : (a : Nat) → Idx a` IS rejected -- index promoted to a parameter -- so the 'indexed' half needs `Idx2`, not `Idx`.) |
| 19 | `IsDefEqU.weakN_iff` forward was not closed; it is a genuine research blocker (module import cycle plus same-measure logical cycle). | TRPROJ_CONTRIBUTION.md Cluster 2; PR_trproj.md; contrib-trproj.json #19 | **PARTIAL** | Not closed: TRUE -- `git diff master..trproj -- Lean4Lean/Theory/Typing/UniqueTyping.lean` is empty and :174 still reads `refine ⟨fun h => have := henv; have := hΓ; sorry, fun h => h.weakN henv W⟩`. The two structural facts the analysis rests on also check out: ChurchRosser.lean:1-3 imports UniqueTyping (not the reverse; UniqueTyping.lean:1-2 imports only Injectivity and Pattern), and ChurchRosser's confluence development consumes the forward direction -- `IsDefEqU.weakN_iff ... \|>.1` at ChurchRosser.lean:288,313,348,366 and `IsDefEq.weakN_iff ... \|>.1` at :323,331,350. The conclusion 'genuine research blocker / no well-founded measure' remains an argument, not a proof. |
| 20 | No new `sorry` and no renamed gap were introduced in Cluster 2. | TRPROJ_CONTRIBUTION.md; contrib-trproj.json #20 | **TRUE** | `git diff master..trproj -- Lean4Lean/Theory/Typing/UniqueTyping.lean` and `-- Lean4Lean/Theory/Typing/Injectivity.lean` are both empty. |
| 21 | Theory/Proj.lean proves each builder's structural lemmas once against `VExpr.subst`; master's `lift'_inst_hi` is the `m=0` case of the new `lift'_instN_hi`. | PR_trproj.md; commit d7ec0fa; contrib-trproj.json #21 | **TRUE** | Theory/VExpr.lean:1014 `theorem lift'_instN_hi (e1 e2 : VExpr) (ρ : Lift) (m : Nat) : ... := by simp only [lift'_eq_subst, ← Subst.liftN_lift_l_id, subst_instN]` and :1018-1019 `theorem lift'_inst_hi ... := lift'_instN_hi e1 e2 ρ 0`; master's VExpr.lean:903-905 carries the identical statement with its own proof, so this is a genuine generalisation of a master lemma. Bridge lemmas present: `subst_instN` (:1009), `liftN_subst_liftN` (:1030 region), `lift'_eq_subst`, `instN_eq`. Evidence correction: contrib-trproj.json says d7ec0fa 'shrank Proj.lean by ~405 lines'; `git show --numstat d7ec0fa` gives `101 304 Lean4Lean/Theory/Proj.lean`, i.e. net -203 (652 -> 449 lines, as REVIEW §9 says). 405 is the combined churn. |
| 22 | `TrProj.uniq` compares two structure names `s₁ s₂` because the `proj` case of `IsDefEqE` compares two projections up to the index alone. | PR_trproj.md; commit e9fefbe; contrib-trproj.json #22 | **TRUE** | The cited fact holds: Verify/TypeChecker/Basic.lean:59 `\| proj : IsDefEqE e₁ e₂ → IsDefEqE (.proj _ i e₁) (.proj _ i e₂)` -- the structure name is a wildcard, as is Verify/EquivManager.lean:24 `RelevantEq.proj`. Upstream records the same in divergences.md:16 ('lean4lean uses only the projection index, while the C++ kernel also compares the structure name'). REVIEW F25's objection also holds and is not refuted: both of today's consumers instantiate one `s` -- Verify/Typing/Lemmas.lean:1031 (`TrExprS.uniq` proj case, same source `Expr` on both sides) and :1162-1167 (`TrExpr.proj`, one `s`). So the generality is prospective, justified by the relation's shape rather than by a present need. |
| 23 | `lake build Lean4Lean.Theory/Verify/Tests` and `lake build Lean4Lean.Experimental` are green. | PR_trproj.md Validation; REVIEW_2026-09-07.md Established facts; contrib-trproj.json #23 | **PARTIAL** | Full `lake build` not run (task is read-only). Strong partial evidence: all 117 `Lean4Lean/**/*.lean` sources have a built `.olean` under .lake/build (0 missing), including Tests and Experimental; and `lake env lean` re-elaborates the load-bearing files with zero errors -- Theory/Proj.lean, Verify/Typing/Expr.lean, Verify/Environment/Lemmas.lean, Tests/{ProjShape,ProjInhabit,ShapeDecide,IotaShape}.lean produce no output at all, while Verify/TypeChecker/WHNF.lean and InferType.lean produce only the expected `declaration uses sorry` warnings at :147 and :392,:407. |
| 24 | PR_trproj.md's 'Changes to existing definitions and statements' is a complete list of what the branch touches (contrib-trproj.json reads this as FALSE, citing the 34-line executable-kernel change in Inductive/Reduce.lean). | PR_trproj.md (by omission); contrib-trproj.json #24 | **PARTIAL** | The conclusion survives but the attribution does not. PR_trproj declares its scope as `iota..trproj`, and in that range `Lean4Lean/Inductive/Reduce.lean` and `Lean4Lean/Verify/TypeChecker/WHNF.lean` are UNCHANGED (`git diff --stat iota..trproj` lists 17 files, neither of them) -- iota re-landed 6fd8a1d's content independently as 7a68882, which the merge 20ec229 message states. The real finding: NEITHER PR text discloses the executable-kernel refactor or its verification. `grep -n 'inductiveReduceRec' PR_iota.md PR_trproj.md` -> one passing mention of `inductiveReduceRec` at PR_iota.md:105 in a test description; `inductiveReduceRecCore` and `inductiveReduceRecCore.WF` appear in neither. That hides both a 34-line change to the executable kernel (`git diff master..trproj -- Lean4Lean/Inductive/Reduce.lean`) and the branches' only ι consumer. |
| 25 | `TrEnv.proj_defeq` is the interface the `proj` case of `TrExprS` soundness consumes. | earlier docstring (removed); REVIEW_2026-09-07.md F38; contrib-trproj.json #25 | **FALSE** | `grep -rn proj_defeq Lean4Lean --include='*.lean'` -> 6 hits: its own statement and docstring (Verify/Environment/Lemmas.lean:1014,1021), two docstring mentions in Verify/Typing/Expr.lean:96,115, and the axiom-profile test (Tests/ProjInhabit.lean:582,587,595). Zero in-library consumers. `reduceProjCore.WF` (Verify/TypeChecker/Reduce.lean:143, `sorry` at :145) is unchanged from master. Nor is there a library consumer today: a client of the theory pins lean4lean at exactly `20ec229` and a search of its sources for `of_trEnv`/`TrEnv.proj_defeq` returns nothing -- `ProjDefeqSpec` occurs only in one of its scratch files. |
| 26 | `TrEnv.proj_defeq` is DELIVERED, no obligations remain, do not re-open. | the commission brief's scorecard D5/D6; contrib-trproj.json #26 | **PARTIAL** | True as to proof status (#4, #15). Misleading as to reach: the same scorecard row asserts 'is in active use today' and that '`ProjDefeqSpec.of_trEnv` is a real theorem (landed)', which I could not confirm -- see the grep in #25. Inside lean4lean it has no consumer at all, and the projection half of REVIEW F09 ('prove at least one internal consumer') is unanswered; only the ι half was answered, by `inductiveReduceRecCore.WF`. |
| 27 | `TrProj.uniq` has reach over 44 of 63 dependent declarations and all four capstones. | the commission brief D6; contrib-trproj.json #27 | **UNVERIFIABLE** | Requires the dependency sweep's dirty-declaration ledger, which is not in either checkout. Partial corroboration only: a client of the theory does depend on the gap -- its sources name 'the unique-typing cluster (`TrExprS.uniq` → `TrProj.uniq` ...)' as the source of its sorryAx -- and the lean4lean side of the dependence is confirmed (#45). |
| 28 | No new axioms, no `native_decide`. | REVIEW_2026-09-07.md Established facts; PR_trproj.md; contrib-trproj.json #28 | **TRUE** | `git diff master..trproj \| grep -E '^\+\s*(axiom\|opaque)'` -> only the docstring line 'axiom strongly typed. -/'. `grep -rn native_decide Lean4Lean` -> none. Caveat worth recording: cones do contain `..._native.bv_decide.ax_*` axioms (visible in `#print axioms Lean4Lean.TypeChecker.Inner.inductiveReduceRecCore.WF`), but these come from `bv_decide` at 7 sites that are all pre-existing on master (`git grep -n bv_decide master -- Lean4Lean` -> Verify/Expr.lean:208,288,609,639 and Verify/Level.lean:56,70,85, the identical set). |
| 29 | Every line this branch added past 100 columns is rewrapped; the source is otherwise unchanged. | commit d114c2d; contrib-trproj.json #29 | **TRUE** | Exhaustive check over the per-line branch attribution in scratchpad/blame/*.txt: zero lines tagged I or P exceed 100 columns. `awk 'length>100'` over every file in `git diff --name-only master..trproj` finds 17 long lines; each is tagged M (master): Pattern.lean:27, Strong.lean:253, Environment/Lemmas.lean:285,375, WHNF.lean:195, ChurchRosser.lean:199,210,1085, Extension.lean:339, Primitive/Basic.lean:112, Primitive/Condition.lean:2177, NormalEq.lean:61,62,104,110,112,117. (contrib-trproj.json lists only 5 of the 11 non-Experimental ones, but the claim itself holds.) |
| 30 | The `inductiveReduceRec` split is behaviour-preserving: the extracted body is the original one with `getMajorIdx` recomputed where the caller had bound it. | commit 6fd8a1d; contrib-trproj.json #30 | **TRUE** | `git diff master..trproj -- Lean4Lean/Inductive/Reduce.lean`: the 11 removed lines reappear verbatim inside `inductiveReduceRecCore` (Reduce.lean:75-89), with `info` renamed to the parameter `rval` and the caller's `majorIdx` (bound at Reduce.lean:106 as `let majorIdx := info.getMajorIdx`) written out as `rval.getMajorIdx`; `return none` becomes `none` in the Option monad. The caller is now `return inductiveReduceRecCore info ls recArgs major` (:115). |
| 31 | `TrProj` is monotone under `VEnv.LE`, as `TrExprS.mono` requires. | TRPROJ_CONTRIBUTION.md §A1; trproj-commission.md R4; contrib-trproj.json #31 | **TRUE** | Verify/Typing/Lemmas.lean:767 `theorem TrProj.mono (H : TrProj env U Γ s i e e') : TrProj env' U Γ s i e e'`, proved by transporting `pat`/`ctor`/`minor_arity` through `henv.pats`/`henv.constants` and the two typing fields through `.mono henv`; consumed at :791 (`\| proj _ h2 ih => exact .proj ih (h2.mono henv)`) inside `TrExprS.mono` (:781). `#print axioms`: `TrProj.mono`=[propext], `TrExprS.mono`=[propext, Classical.choice, Quot.sound] -- both sorryAx-free. |
| 32 | The design formalizes the argument upstream itself put in divergences.md -- that `Expr.proj` is no more powerful than the recursor the kernel generates. | REVIEW_2026-09-07.md §2; Theory/Proj.lean:7-10; contrib-trproj.json #32 | **TRUE** | divergences.md:15 (= master:14, unchanged text): '... Lean4lean's rule keeps `Expr.proj` no more powerful than the recursor the kernel generates for the same type.' Theory/Proj.lean:7-9 cites it directly. |
| 33 | The shape is the thesis's `inv_x` with the dependent typing of the thesis's `π₂ p : β[π₁ p/x]`; commit title: 'define TrProj by the thesis's dependent motive'. | PR_trproj.md; Theory/Proj.lean:10-24; commit 811a52a; contrib-trproj.json #33 | **PARTIAL** | Precedents verified in ../lean-type-theory: typesys.tex:11-14 defines `inv_x := λ a. λ y. rec_acc (λ z. y<z → acc y) (λ z. λ h. λ_. h y) x a` -- a projection built from the recursor with a SPECIFIC, non-constant motive; Wtypes.tex:26-28 gives `Γ ⊢ π₂ p : β[π₁ p/x]`; axioms.tex §2.6.3's fresh-universe-per-`rec_P` matches the per-field `uss`. A precedent the contrib note misses and which strengthens the claim: unique.tex:109 -- 'These `inv_i` projection operators can be defined using the recursor, like we demonstrated for `acc`'. Overclaim: `grep -c '\\proj' *.tex` -> 0 in all 11 files, so the thesis never models Lean's `Expr.proj`, and it has no chained motive; the chaining is an original extension. Theory/Proj.lean's current wording hedges correctly ('the dependent typing SHAPE of the thesis's *primitive* projections'); the 811a52a commit title does not. |
| 34 | The branches are 'not upstreamable as it stands' with four blockers B1-B4 (contrib-trproj.json: B1, B2 and B4 remain unresolved). | REVIEW_2026-09-07.md §1; contrib-trproj.json #34 | **FALSE** | B4 is resolved: `git merge-base --is-ancestor master trproj` succeeds and `git merge-base master trproj` = 8223d223 = `master` HEAD, i.e. upstream master is merged into the branch. B1/B2's mechanized witnesses no longer apply: `lake env lean review-artifacts/counterexamples/cexB.lean` and `cexA.lean` both fail with `'rules_own_params' is not a field of structure 'VInductDecl.WF'` and `Fields missing: universes, ctors_params, ctors_result, ctors_positive, recs_over_block, rec_counts` -- the spec was redesigned (4016efa/f53b727, REVIEW §9). What remains is the residual obligation, not the counterexample: `VEnv.WF.patsStrong` is still `sorry` (Theory/Typing/EnvLemmas.lean:334, `#print axioms` -> sorryAx) and gates every strong-system consumer. B3 is mitigated, not closed (#18, and `inferProj.WF_struct` is a new sorry). |
| 35 | Structure eta is not represented and nothing here blocks adding a per-structure eta rule; `tryEtaStructCore.WF` stays open. (Divergence from the thesis, which admits the eta rule.) | PR_trproj.md Scope boundaries; Theory/Proj.lean:54-58; contrib-trproj.json #35 | **TRUE** | Verify/TypeChecker/IsDefEq.lean:227 (`tryEtaStructCore.WF`) and :488 (`isDefEqUnitLike.WF`) are `sorry` on trproj at the same line numbers as on master (`git grep -n -w sorry master` vs `trproj`). The thesis divergence is real: Wtypes.tex:92-93 takes `(π₁ x, π₂ x) ≡ x` as an admitted 'modest strengthening', explicitly because 'we've omitted the recursors in favor of projections for Σ and ulift' -- the branch takes the opposite route (recursors, no eta), which is faithful to Lean's kernel. |
| 36 | PR_trproj: 'the general `inferProj.WF` keeps its statement'. | PR_trproj.md, Changes / Deferred obligations | **FALSE** | The statement was changed -- and the change is a bug fix the PR does not claim. master:Verify/TypeChecker/InferType.lean:388-391 concluded `∃ ty', c.TrTyping (.proj st i e) ty e' ty'`, asserting that `e'` (the translation of the STRUCT) is the translation of the projection, and took `hasty : c.HasType e' ty'` with `ty'` auto-bound. trproj:407-410 reads `∃ e'' ty', c.TrTyping (.proj st i e) ty e'' ty'` with `hasty : c.HasType e' ety'`. contrib-trproj.md records this correctly as a fixed latent statement bug; PR_trproj contradicts itself on it. |
| 37 | TRPROJ_CONTRIBUTION: axiom profiles -- `TrProj.wf` = [propext]; `weak'`/`instN`/`mono`/`instL` and `TrExprS.mono` sorryAx-free; `defeqDFC` inherits the pre-existing unique-typing sorryAx (no new trust). | TRPROJ_CONTRIBUTION.md, Exposed interface | **PARTIAL** | Profiles confirmed exactly by `lake env lean` + `#print axioms` (see #3, #31). But 'no new trust' is wrong: the computed sorry cone of `TrProj.defeqDFC` is [IsDefEqU.forallE_inv_stratified, IsDefEqU.sort_inv, VEnv.WF.patsStrong]. The first two are master's open injectivity lemmas; `VEnv.WF.patsStrong` (Theory/Typing/EnvLemmas.lean:334) is NEW on the ι branch -- master has no such declaration. So `defeqDFC` (and `proj_defeq`) depend on an obligation the contribution itself introduced. |
| 38 | TRPROJ_CONTRIBUTION's quoted `TrProj` definition and its 'Trust surface -- 3 PROJ-TODO(soundness)' at Lemmas.lean:748, Lemmas.lean:1019, Environment/Lemmas.lean:536. | TRPROJ_CONTRIBUTION.md §A1 and Trust surface | **FALSE** | Stale on every particular. The quoted `def TrProj ... := ∃ recName ctorName us params fieldTys motive np r, ...` is not the current definition (Verify/Typing/Expr.lean:133 is a 6-fold existential over `TrProjCtor`, with a chained motive and a per-field `uss`). The three cited sites: Lemmas.lean:748 -> the sorry is at :747 (`weak'_inv`); Lemmas.lean:1019 -> `uniq`'s sorry is at :995; Environment/Lemmas.lean:536 -> `proj_defeq` is at :1021 and is PROVED. The file must not be handed to a reviewer as-is. |
| 39 | Commit 6fd8a1d: `inductiveReduceRecCore.WF` is the first real consumer of the ι interface; `reduceRecursor.WF` stays open. | commit 6fd8a1d; REVIEW_2026-09-07.md §9 F09 | **TRUE** | Verify/TypeChecker/WHNF.lean:17 `theorem inductiveReduceRecCore.WF ... : c.FVarsBelow ... ∧ c.TrExpr e₁ e'`, proved (no `sorry` in the proof body; the file's only `sorry` is at :147). It calls the ι interface at WHNF.lean:119 `c.trenv.iota_rec hrec hrule hsafe hctor (hm1.app hm2) hty`. `reduceRecursor.WF` at WHNF.lean:147 is `sorry`. Caveat: its axiom cone is not clean -- the computed sorry leaves are [TrProj.uniq, IsDefEqU.forallE_inv_stratified, IsDefEqU.sort_inv, VEnv.WF.patsStrong], so the ι consumer inherits the projection gap through `TrExprS.uniq`. |
| 40 | `TrProj.uniq` is the load-bearing gap: `TrExprS.uniq` inducts through it, so the whole uniqueness layer carries sorryAx. | PR_trproj.md 'Axiom cone, plainly'; contrib-trproj.json key_decls | **TRUE** | Verify/Typing/Lemmas.lean:1031 `\| proj _ l2 ih1 => let .proj r1 r2 := H2; exact l2.uniq henv hΔ.defeqCtx r2 (ih1 hΔ r1)` inside `TrExprS.uniq` (:998). Computed cone: `Lean4Lean.TrExprS.uniq => [Lean4Lean.TrProj.uniq, IsDefEqU.forallE_inv_stratified, IsDefEqU.sort_inv, VEnv.WF.patsStrong]` -- `TrProj.uniq` is a leaf the branch owns. |
| 41 | `TrEnv.structure_rec`, `TrEnv.ctor_arity` and `TrEnv.pats_iota_inv_shape` have no consumer in lean4lean other than `proj_defeq` itself. | REVIEW_2026-09-07.md F09; contrib-trproj.md (e) | **TRUE** | `grep -rn '<name>' Lean4Lean --include='*.lean'` excluding Verify/Environment/Lemmas.lean returns nothing for all three; within that file the only call sites are inside `proj_defeq` (:1051 `H.ctor_arity`, :1059 `H.pats_iota_inv_shape`, :1076 `H.structure_rec`). (`iota_rec`, by contrast, does have an external consumer: WHNF.lean:119.) |
| 42 | `TrEnv.proj_defeq` is a theorem about a `TrEnv` hypothesis that nothing in the repo can yet construct for an inductive-containing environment. | contrib-trproj.md (e); not claimed in the PR | **TRUE** | Verify/Environment.lean:208 `\| inductDecl _ _ _ _ => sorry` is the `addDecl.WF` case that would build the witness, and `grep -rn AddInduct Lean4Lean` finds no construction site -- only theorems ABOUT `AddInduct` and its `consts`. So `structure_rec`'s new obligations (`TrIndType.all/numParams/numIndices`, `TrRecursor.name_major`, Verify/Environment/Basic.lean:216,237) are free today and untested against real kernel data. |
| 43 | `TrIndType` was extended with `all`/`numParams`/`numIndices` and `TrRecursor` with `name_major`. | contrib-trproj.json key_decls; commit e202975 | **TRUE** | `git diff iota..trproj -- Lean4Lean/Verify/Environment/Basic.lean`: `TrIndType` (:216) gains parameters `(nparams : Nat) (block : List Name)` and fields `all : ival.all = block`, `numParams : ival.numParams = nparams`, `numIndices : ival.numIndices = t.type.piArity - nparams`; `TrRecursor` (:237) gains `name_major : ∀ n, r.type.majorFormer? r.getMajorIdx = some n → rval.name = mkRecName n`; `AddInduct.types` is re-threaded at :283-284. |
| 44 | The zero-use wrappers `TrProjCtor.toTrProj` and `TrProj.exists_ctorName` were deleted (REVIEW F29). | commit e9fefbe; REVIEW_2026-09-07.md §9 | **TRUE** | `grep -rn 'toTrProj\\|exists_ctorName' Lean4Lean --include='*.lean'` -> no hits. (TRPROJ_CONTRIBUTION.md still advertises both as part of the exposed interface -- another staleness in that file.) |
| 45 | UNDERCLAIMED: because master defined `TrProj` by `sorry`, every master declaration whose type mentions `TrExprS`/`TrExpr` carried sorryAx by construction; the branch removes that. | not claimed anywhere in PR_trproj.md, TRPROJ_CONTRIBUTION.md, REVIEW_2026-09-07.md or contrib-trproj.json | **TRUE** | Mechanism confirmed by a standalone control (scratchpad/claims/probe4.lean): `def SorryPred : Nat → Prop := sorry` + `inductive Foo \| mk : SorryPred n → Foo n` gives `'Foo' depends on axioms: [sorryAx]` and `'foo_mono' depends on axioms: [sorryAx]`. master:Verify/Typing/Expr.lean:68 is `def TrProj ... := sorry` and master:Expr.lean:104 `\| proj : ... → TrProj Δ.toCtx s i e' e'' → TrExprS Δ (.proj s i e) e''`, so master's `TrExprS` itself is sorryAx-tainted. Today: `#print axioms Lean4Lean.TrExprS` -> [propext, Classical.choice, Quot.sound]; `TrProj` and `TrProjCtor` -> [propext]. Measured over the built environment (scratchpad/claims/count.lean): 663 `Lean4Lean.*` declarations whose TYPE mentions `TrExprS`/`TrExpr`/`TrProj`/`TrProjCtor`, of which 614 are now sorryAx-free. Concretely `TrExprS.wf` and `TrExprS.mono` are sorryAx-free. |
| 46 | UNDERCLAIMED: the kernel-to-model bridge lemmas the branch adds are themselves sorryAx-free. | not stated in PR_trproj.md | **TRUE** | `#print axioms`: `TrEnv.structure_rec`, `TrEnv.ctor_arity`, `TrEnv.pats_iota_inv_shape` all = [propext, Classical.choice, Quot.sound, Lean.PersistentHashMap.findAux_isSome, ...WF.find?_eq, ...WF.toList'_insert] -- master's environment-lemma axioms only, no sorryAx. `proj_defeq`'s sorryAx enters solely through `VEnv.IsDefEqU.betaN` ([propext, sorryAx, ...]), i.e. through unique typing, not through the bridge. |
| 47 | `TrProj.weak'`, `.weakN` and `.instN` gained hypotheses relative to master's statements, and `TrProj.wf` dropped one. | commit e9fefbe (wf only); not disclosed in PR_trproj.md for weak'/weakN/instN | **TRUE** | `git diff iota..trproj -- Lean4Lean/Verify/Typing/Lemmas.lean`: `weak'` and `weakN` gain `(henv : Ordered env)`; `instN` gains `(henv : Ordered env)` and `(h₀ : env.HasType U Γ₀ e₀ A₀)`; `wf` goes from `(H1 : TrProj ...) (H2 : VExpr.WF env U Γ e)` to `(H : TrProj env U Γ s i e e')` alone. All four were `sorry` on master, so nothing is actually weakened for its callers: the `TrExprS.*` callers already carried `henv`/`t₀` in their `variable!` blocks, and no `TrExprS`/`TrExpr` statement changed except for threading `env`/`U` through `TrProj` (`TrExpr.proj`, Lemmas.lean:1162). |
| 48 | 'No master lemma gained a hypothesis' -- taken over the whole master..trproj footprint. | task framing; PR_iota.md discloses the retype | **FALSE** | On the trproj half (`iota..trproj`) it holds for every dependent master lemma (see #47). Over `master..trproj` it does not: ~45 master declarations are retyped from `Ordered` to `OrderedStrong`, e.g. `Ordered.strong`, `CtxStrong.strong`, `IsDefEq.strong`, `IsDefEqStrong.substEq'`, `.subst`, `IsDefEq.substDF`, and the whole `VEnv.HasPrimitives.*` / `TrExprS.*` primitives layer (`git diff master..trproj \| grep -E '^-\s*theorem'`). This is ι-side and IS disclosed in PR_iota.md:28-34 ('now take `OrderedStrong` where they took `Ordered`'), together with the consequence that master's substitution and primitives cones acquire `patsStrong`. |
| 49 | The M5 merge (20ec229) changes 1 line; 75ffde9 was cherry-picked onto iota. | commit 20ec229 message; REVIEW_2026-09-07.md §9 F20 | **TRUE** | `git diff --stat 20ec229^1 20ec229` -> `Lean4Lean/Verify/Environment/Lemmas.lean \| 2 +-  1 file changed, 1 insertion(+), 1 deletion(-)`. `git log --format='%h %s' iota \| grep nrec` -> `655dd3f feat(theory): pin the ι reduct's recursive-argument count`, the cherry-pick of 75ffde9. |
| 50 | REVIEW_2026-09-07.md Established facts: 'one IOTA-TODO sorry (`VEnv.WF.patsStrong`), three PROJ-TODO'. | REVIEW_2026-09-07.md, Established facts | **PARTIAL** | True of the reviewed heads (iota@d69ac5d, trproj@f7dabf1): `git grep -c 'IOTA-TODO\\|PROJ-TODO' f7dabf1 -- Lean4Lean` -> InductiveLemmas.lean 1, InferType.lean 1, Verify/Typing/Lemmas.lean 2. Stale at HEAD: the markers are gone (#10) and the open obligations are five -- `VEnv.WF.patsStrong` (EnvLemmas.lean:334), `TrProj.weak'_inv` (:747), `TrProj.uniq` (:995), `inferProj.WF_struct` (:398) and `inferProj.WF` (:410) -- alongside the master-inherited ones. |

## Summary of verdicts

| verdict | ids | count |
|---|---|---|
| TRUE | 4, 5, 6, 7, 14, 15, 16, 17, 18, 20, 21, 22, 25(as refutation), 28, 29, 30, 31, 32, 39, 40, 41, 42, 43, 44, 45, 46, 47, 49 | 28 |
| PARTIAL | 1, 2, 3, 12, 13, 19, 23, 24, 26, 33, 37, 50 | 12 |
| FALSE | 9, 10, 11, 25, 34, 36, 38, 48 | 8 |
| UNVERIFIABLE | 8, 27 | 2 |

The headline technical claims -- `TrProj` is a real definition, `TrEnv.proj_defeq` is proved,
the structural lemmas are proved and sorryAx-free, the sorry census, the axiom cone, the two
test files, the kernel probes -- all check out exactly as stated. Every FALSE verdict falls into
one of three buckets: (i) `TRPROJ_CONTRIBUTION.md` is a stale snapshot of an abandoned design
(9, 10, 11, 38); (ii) `contrib-trproj.json`'s own findings 25/34 and its evidence for 21 and 24
are partly out of date or mis-attributed; (iii) two self-contradictions inside `PR_trproj.md`
(36) and a scope question the task raised (48).

## What the notes overclaim

1. **`TRPROJ_CONTRIBUTION.md` is not a description of this branch.** It documents the
   *constant-motive* design abandoned in `811a52a`, quotes a `TrProj` definition that no longer
   exists, advertises `TrEnv.pats_iota_inv` and the two `TrProjCtor` wrappers that were deleted,
   counts 15 `IOTA-TODO` + 3 `PROJ-TODO` markers where there are now zero, and cites three
   `PROJ-TODO` line numbers of which one (`Environment/Lemmas.lean:536`, `proj_defeq`) is now a
   *proved theorem*. Claims 9, 10, 11, 38. It should not be handed to a reviewer.
2. **"and with it every lemma about it" / "they were `sorry` on master"** (claims 1, 3).
   `TrProj.weakN` was proved on master (a one-liner off the sorried `weak'`) and `TrProj.mono`
   did not exist there. Seven of the eight lemmas were sorry, not eight.
3. **`PR_trproj.md` says the general `inferProj.WF` "keeps its statement"** (claim 36). It does
   not: the statement was corrected from `∃ ty', c.TrTyping (.proj st i e) ty e' ty'` (which
   asserted the *struct's* translation is the projection's) to `∃ e'' ty', ... e'' ty'`. The
   branch fixed a latent bug in a master statement and then denied doing so.
4. **"no new trust"** for `TrProj.defeqDFC` (claim 37). Its sorry cone contains
   `VEnv.WF.patsStrong`, a *new* obligation introduced by the ι half of this work, not only
   master's pre-existing injectivity gaps.
5. **Thesis attribution** (claim 33). `inv_x` and `π₂` are genuine precedents -- and
   `unique.tex:109` is a stronger one than the notes cite -- but `\proj` occurs zero times in
   the thesis, which never models `Expr.proj` and has no chained motive. The commit title
   "define TrProj by the thesis's dependent motive" overclaims; the module docstring does not.
6. **"DELIVERED ... is in active use today"** (claims 25, 26). `TrEnv.proj_defeq` has
   zero consumers inside lean4lean, and none in a client library on its current
   branch either, although that repository pins lean4lean at exactly this commit.
7. **Undisclosed change** (claim 24). Neither PR text mentions `inductiveReduceRecCore` or
   `inductiveReduceRecCore.WF`, i.e. neither discloses the 34-line refactor of the *executable*
   kernel nor the branches' only ι consumer -- the very thing REVIEW F09 asked for.
8. **`contrib-trproj.json`'s own claim 34** asserts blockers B1/B2/B4 are unresolved. `master`
   is an ancestor of `trproj` (B4 fixed), and the mechanized counterexamples `cexA`/`cexB` no
   longer elaborate against the redesigned `VInductDecl.WF` (B1/B2 addressed in form, whatever
   one thinks of the new spec). What genuinely remains is `VEnv.WF.patsStrong`.

## What the notes underclaim

1. **The largest single result is not stated anywhere** (claim 45). On master `TrProj` was a
   `sorry`-*definition*, so `TrExprS` itself -- and therefore every declaration whose statement
   merely mentions `TrExprS`/`TrExpr` -- carried `sorryAx` by construction. Today `TrExprS` is
   `[propext, Classical.choice, Quot.sound]`, `TrProj`/`TrProjCtor` are `[propext]`, and 614 of
   the 663 `Lean4Lean.*` declarations whose type mentions them are sorryAx-free. The PR sells
   this as "five sorries closed"; it is a qualitative change to the trust status of the whole
   `Verify` layer.
2. **The kernel-to-model bridge is clean** (claim 46). `TrEnv.structure_rec`, `TrEnv.ctor_arity`
   and `TrEnv.pats_iota_inv_shape` are sorryAx-free; all of `proj_defeq`'s inherited `sorryAx`
   enters through `IsDefEqU.betaN`, i.e. through master's unique-typing layer plus `patsStrong`.
   The PR's "axiom cone, plainly" paragraph is right but does not say *where the cut is*.
3. **The telescope split is derived, not assumed** (claim 5). `TrEnv.structure_rec` turns
   `ival.all = [S]`, one constructor and `numIndices = 0` into `(numParams, 1, 1, 0)` with a
   real induction over the `TrEnv'` derivation, sorry-free. This directly answers REVIEW F07 and
   is stated in one clause of the PR.
4. **The test discipline is stronger than advertised.** `Tests/ProjShape.lean` runs the real
   kernel (`Environment.addDeclCore`, `Meta.whnf`, `isDefEq`) on ten structure/field pairs with
   two negative controls, and `Tests/ProjInhabit.lean` builds model-side witnesses through
   `VEnv.addInduct` with four `#guard_msgs` axiom pins. Both re-elaborate clean. Little else in
   the repo is tested on both sides of the kernel/model divide.
5. **`master`'s `lift'_inst_hi` was generalised**, not merely reproved (claim 21): the identical
   master statement is now the `m = 0` corollary of `lift'_instN_hi`. That is a contribution to
   upstream's own `VExpr` library independent of projections.
6. **`TrProj.uniq` is precisely located.** The computed sorry leaves of `TrExprS.uniq` are
   `{TrProj.uniq, forallE_inv_stratified, sort_inv, patsStrong}` -- `TrProj.uniq` is the one
   leaf this contribution owns, and closing it is the single highest-value remaining item. The
   PR says this, but only in prose; it is worth stating as the measured fact it is.
