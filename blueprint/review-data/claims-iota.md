# Claim verification — branch `iota`

Method. All checks were run against the repo at `/home/barabba/Documents/Research/Projects/Peregrine/lean4lean`,
branch `trproj`, HEAD `20ec229` (the state the blueprint reads; `iota` = `38ea0de` is a strict
ancestor). Branch-specific facts were checked with `git show <rev>:<path>` / `git grep <rev>`.
Semantic checks were run with `lake env lean` on scratch files under
`…/scratchpad/claims/` (the repo was **not** rebuilt and **not** modified; `lake env lean`
writes nothing). The `.lake/build` tree covers all 117 modules, and re-elaborating
`Lean4Lean/Tests/IotaShape.lean`, `Experimental/Stratified.lean` and
`Experimental/StratifiedUntyped.lean` from source succeeded with zero errors, so the
loaded oleans agree with the working tree.

Two results in this pass **upgrade** the reader's `contrib-iota.json`, which marked them
unverified: claim 9 (measured) and claim 29 (both counterexamples mechanically refuted,
sorry-free, in `claims/cexB_refute.lean` and `claims/cexA_refute.lean`). Claim 19 and 33
are also upgraded from "exists / not checkable here" to "run and passing" / "verified in the
sibling repo".

Verdict key: TRUE / FALSE / PARTIAL / UNVERIFIABLE. For the stale notes
(`IOTA_CONTRIBUTION.md`, `PR_DESCRIPTION_formal.md`) the verdict is the truth of the claim
*as written today*, so FALSE there means "the reader's `checked-false` is confirmed".

---

## A. The 33 claims of `contrib-iota.json`

| id | claim | source | verdict | evidence |
|---|---|---|---|---|
| 1 | master's `VInductDecl.WF` and `VEnv.addInduct` sorries are closed | PR_iota.md | **TRUE** | `git show master:Lean4Lean/Theory/Inductive.lean` is a 7-line file whose only two declarations are `def VInductDecl.WF … := sorry` (`:5`) and `def VEnv.addInduct … := sorry` (`:7`). Now `Theory/Inductive.lean:316` `def VEnv.addInduct` (four-stage chain) and `:335` `structure VInductDecl.WF` (20 fields, counted by `awk`), file 444 lines, no `sorry`. |
| 2 | `addInduct_WF` is proved | PR_iota.md, PR_DESCRIPTION.md | **TRUE** | master `Theory/Typing/InductiveLemmas.lean:10` is `sorry`. Now `theorem addInduct_WF` at `InductiveLemmas.lean:229`; `git grep -n -w sorry` over that file returns nothing. `#print axioms Lean4Lean.VEnv.addInduct_WF` → `[propext, Classical.choice, Quot.sound]` — no `sorryAx`. |
| 3 | the branch leaves exactly one `sorry` of its own, `VEnv.WF.patsStrong` | PR_iota.md, PR_DESCRIPTION.md | **TRUE** | `git grep -n -w sorry iota -- 'Lean4Lean/**.lean' \| grep -v Experimental` vs the same on `master`: the only hit not present on master is `iota:Lean4Lean/Theory/Typing/EnvLemmas.lean:334: theorem VEnv.WF.patsStrong … := sorry`. Everything else is a line-shifted master sorry (`ChurchRosser 1190/1209 → 1193/1212`, `Verify/Environment.lean:235 → 208`, `WHNF.lean:8 → 149`). `#print axioms Lean4Lean.VEnv.WF.patsStrong` → `[propext, sorryAx, Quot.sound]`. |
| 4 | sorry census non-`Experimental`: master 23, this branch 21 | PR_iota.md | **TRUE** | master: 28 grep hits, of which 5 are prose/commented-out (`TypeChecker.lean:970`, `Verify/Environment/Basic.lean:101,102`, `Verify/Environment/Primitive.lean:8`, `Verify/Level.lean:27`) → 23 terms. iota: 24 hits, 3 prose (`TypeChecker.lean:970`, `Primitive.lean:8`, `Level.lean:27`) → 21. (`trproj` is 19.) |
| 5 | no new axioms | PR_iota.md, PR_DESCRIPTION_formal.md | **TRUE** (claim); the reader's *recipe* is wrong | `diff <(git grep -n '^axiom ' master …) <(… iota …)` differs in exactly one line, `iota:Lean4Lean/Theory/Typing/Strong.lean:769: axiom strongly typed. -/` — a docstring continuation, not a declaration (`sed -n '768,771p'` shows `/-- … leaves every constant and definitional / axiom strongly typed. -/`). Note the recipe quoted in `contrib-iota.json` ("only `Experimental/`") is false on *both* branches: `Lean4Lean/PtrEq.lean` (2) and `Lean4Lean/Verify/Axioms.lean` (10) carry axioms on master too. |
| 6 | no `native_decide` | REVIEW_2026-09-07.md | **TRUE** | `git grep -n native_decide iota -- 'Lean4Lean/**.lean'` → no hits. The ι theory files also contain zero `by decide` (`Theory/Inductive.lean`, `Theory/Typing/InductiveParams.lean`); decidability lives in `Tests/ShapeDecide.lean`. |
| 7 | `lake build` green on `Lean4Lean.Theory/.Verify/.Tests/.Experimental` | PR_iota.md, REVIEW §9 | **PARTIAL** (strong indirect evidence; `lake build` forbidden in this pass) | Every one of the 117 `.lean` modules has an olean under `.lake/build/lib/lean/` (120 oleans). Re-elaborating from source with `lake env lean` gave zero errors for `Tests/IotaShape.lean` (1.7 s), `Experimental/Stratified.lean`, `Experimental/StratifiedUntyped.lean`. Caveat: this is the `trproj` tree, not `iota`; and `Lean4Lean.Tests` is **not** in `defaultTargets` (`lakefile.toml:2` lists only `Lean4Lean`, `lean4lean`, `Lean4Lean.Theory`, `Lean4Lean.Verify`), so the test assertions fire only under an explicit target. |
| 8 | `Experimental` sorry counts equal master's; the four `pat` cases closed via `Typing.pat_env` / `IsDefEq1.pat` | REVIEW §9 (F15), be30d35 | **TRUE** | Per-file `git grep -c -w sorry`: NormalEq 0=0, ParallelReduction 2=2, Stratified 3=3, StratifiedUntyped 3=3, Stronger 4=4; totals 103 = 103. The closures are real proofs: `Experimental/NormalEq.lean:97` `pat_env : env.pats p r → Pat p r`; `ParallelReduction.lean:857` and `:909` discharge `| pat` via `hr.toOK … ; TY.pat_wf (TY.pat_env hp) …`; `Stratified.lean:71` / `StratifiedUntyped.lean:51` add `| pat` constructors to `IsDefEq1`/`IsDefEqU1`. Both stratified files re-elaborate error-free. |
| 9 | `TrEnv.iota_defeq` is axiom-clean (`#print axioms` = `[propext]`) | PR_DESCRIPTION.md, IOTA_CONTRIBUTION.md | **TRUE** (upgraded from "unverified") | `lake env lean claims/ax1.lean`: `'Lean4Lean.TrEnv.iota_defeq' depends on axioms: [propext]`. |
| 10 | `pats_iota'` / `iota_rec` carry `sorryAx` only via the pre-existing `TrProj` placeholder | PR_DESCRIPTION.md, PR_FOLLOWUP.md | **PARTIAL** (consistent; the `iota`-specific measurement is not reproducible without building `iota`) | On the built `trproj`, where `TrProj` is *defined*, both are **sorryAx-free**: `TrEnv.pats_iota'` and `TrEnv.iota_rec` → `[propext, Classical.choice, Quot.sound, Lean.PersistentHashMap.findAux_isSome, .WF.find?_eq, .WF.toList'_insert]` (all four hash-map axioms are master's own, `Verify/Axioms.lean:72,78,83`). That is exactly what the claim predicts: remove `TrProj := sorry` and no `sorryAx` remains. On `iota` itself `TrProj := sorry` (`git show iota:Lean4Lean/Verify/Typing/Expr.lean:68`) and `TrExprS.proj` uses it, so the inheritance is structural. Not directly measured on `iota`. Note the claim as written omits the PHM axioms. |
| 11 | `TrEnv'.induct` applies at every safety level (no `.safe` gate) | PR_iota.md, PR_DESCRIPTION.md | **TRUE** | `Verify/Environment/Basic.lean:582-586`: `| induct : decl.WF env → AddInduct safety C env decl C' env' → TrEnv' C Q env → TrEnv' C' Q env'` — no safety premise. Contrast `git show dc01931:…Basic.lean:220-226`, which had `safety = .safe →` plus a comment saying so. |
| 12 | inductive processing is guarded to `.safe`; unsafe inductives future work | IOTA_CONTRIBUTION.md, PR_DESCRIPTION_formal.md | **FALSE** (stale — true at `dc01931`, false since `d69ac5d`) | Contradicted by the evidence for 11. |
| 13 | twelve deferred proofs tagged `-- IOTA-TODO(soundness)` | IOTA_CONTRIBUTION.md, PR_DESCRIPTION_formal.md | **FALSE now; TRUE at the time it was written** | `grep -rn 'IOTA-TODO' Lean4Lean` and `git grep -n 'IOTA-TODO' iota` → zero hits. It *was* accurate at `eddf009`: the 12 ι sorries there are ChurchRosser:1390, Lemmas:818,863, Strong:275,511,646,788, InductiveLemmas:110, InductiveParams:432,443,447, Verify/Environment/Lemmas:73 (`git grep -n -w sorry eddf009`). |
| 14 | `VInductDecl.WF` records only the checkable conditions; positivity, the `imax` bound and large elimination are not yet enforced | PR_DESCRIPTION_formal.md | **FALSE** (stale) | Fields exist: `ctors_positive` `Theory/Inductive.lean:374`, `universes` `:351` (carrying `VLevel.imax u ℓ ≤ ℓ` and `(∃ r ∈ decl.recs, r.uvars = decl.uvars + 1) → decl.LargeElim envT ℓ`), `recs_elim` `:361`, `LargeElim` `:226`. |
| 15 | `pat_uniq`, `pat_app_uniq` and `extra_pat` are *false* against the current `VInductDecl.WF` | IOTA_CONTRIBUTION.md | **FALSE** for the first two; **true in spirit** for `extra_pat` | `WF.pat_uniq` `InductiveParams.lean:260` → `#print axioms` `[propext, Classical.choice, Quot.sound]`; `WF.pat_app_uniq` `:334` → `[propext, Quot.sound]`. `extra_pat` is not proved but is an explicit hypothesis `DefEqsAsPats` (`:378`), whose docstring states it fails once a `def` or `quot` is declared. |
| 16 | `Aligned.addInduct` is a deferred `sorry` | IOTA_CONTRIBUTION.md, PR_DESCRIPTION_formal.md | **FALSE** (stale) | `Verify/Environment/Lemmas.lean:102 theorem Aligned.addInduct`; `#print axioms` → `[propext, Classical.choice, Quot.sound]`. `Aligned` gained the `pat` and `block` clauses it needed (`Lemmas.lean:21`, `:28`). |
| 17 | the downstream consumer interface inherits no new soundness gap | IOTA_CONTRIBUTION.md | **FALSE as a statement about the model; TRUE narrowly about the four handback lemmas** | Narrow half: `iota_defeq` `[propext]`, `pats_iota'`/`iota_rec`/`addInduct_pat`/`addInduct_le` all sorryAx-free on `trproj`. Wide half: `instance : CoeOut (VEnv.WF env) env.OrderedStrong` (`EnvLemmas.lean:343`) is now the only route from `VEnv.WF` into the strong system, and it goes through the sorried `patsStrong` — `#print axioms Lean4Lean.VEnv.WF.orderedStrong` → `[propext, sorryAx, …]`. The file itself says so: `EnvLemmas.lean:342` `-- Every use of the strong system is therefore conditional on VEnv.WF.patsStrong.` |
| 18 | `iotaCheck = .true` because the kernel performs no parameter check at ι-reduction time | PR_DESCRIPTION.md | **TRUE** | `Theory/Inductive.lean:257-266` `VEnv.addRecRule` registers `(SimplePattern.iotaRHS …, .true)`. `Inductive/Reduce.lean:75-88` `inductiveReduceRecCore` does exactly: `getRecRuleFor`, `rule.nfields > majorArgs.size`, `ls.length != levelParams.length`, then three `mkAppRange`s — no parameter comparison. |
| 19 | `SimplePattern.iotaRHS` reproduces `inductiveReduceRec`'s argument slicing exactly, validated against real kernel data | PR_iota.md, PR_DESCRIPTION.md | **TRUE** (upgraded: the assertions were run) | `Tests/IotaShape.lean:141 checkIota` builds the redex over free variables, calls the executable `inductiveReduceRec kenv e Meta.whnf …`, translates both sides with `Meta.ofExpr`, and asserts `R.apply m1 m2 = redv` where `R = SimplePattern.iotaRHS …`. `lake env lean Lean4Lean/Tests/IotaShape.lean` → exit 0, no diagnostics. Control: a deliberately wrong pair (`claims/control2.lean`) does produce an error, so the assertions really execute. Driver at `:454`; 12 explicit `checkIota` calls, `checkIotaAuto` inside `checkAll` over 48 formers and `checkNested` over 9. |
| 20 | `VInductDecl.WF` specifies a direct block; nested inductives are outside it; `Tree` is the documented negative control | PR_iota.md, PR_DESCRIPTION.md | **TRUE** | `recs_over_block` `Theory/Inductive.lean:377` (`∃ t ∈ decl.types, r.type.majorFormer? … = some t.name`), `rules_ctor` `:392`. `Tests/IotaShape.lean:605` runs `checkNested` on `Tree, TreeP, T2, T3, MA, MB, T4, TreeQ, Fn`; the driver also asserts `¬ nodeTy.CtorPositive [``Tree] 0` and that `Tree.rec_1`'s major is over `List`, so `RecShape` rejects it. All pass. |
| 21 | `AddInduct` carries only kernel data + stage witnesses; `env_eq`/`wf`/`find?_mono`/`rec_find`/`rec_reg`/`ctor_find`/`value_find` are derived theorems | PR_iota.md, PR_DESCRIPTION.md | **TRUE** | `Verify/Environment/Basic.lean:272` fields: `ivals rvals envT envC envR stT stC stR stP types recs order order_perm fresh map_eq`. Theorems at `:296 env_eq`, `:308 le`, `:395 wf`, `:400 find?_mono`, `:422 value_find`, `:434 rec_find`, `:464 rec_reg`, `:494 ctor_find`. Compare `git show eddf009:…Basic.lean`, where `env_eq consts wf rec_find value_find` were *fields*. |
| 22 | `checkEqType` rejects an unsafe `Eq`; `Environment.EqSafe` and its preconditions are gone; divergence recorded | PR_iota.md, REVIEW §9 (F06) | **TRUE** | `git diff master..iota -- Lean4Lean/Quot.lean` is exactly one added line, `Quot.lean:24 if info.isUnsafe then fail "'Eq' type is unsafe"`. `grep -rn EqSafe Lean4Lean` → none. `divergences.md` gained one bullet (third), and is the only tracked file changed outside `Lean4Lean/`. `addQuot.WF` (`Verify/Environment/Quot.lean:500`) takes only `ves.WF env`. |
| 23 | `Verify/Environment/Quot.lean` replaces a proof that was vacuous through `no_inductInfo` with a real one | PR_iota.md, PR_DESCRIPTION.md | **TRUE** | master `Verify/Environment.lean:132-139`: docstring "This is currently vacuous in the non-initialized case", proof `exact (checkEqType.WF wf).bind fun _ h => False.elim h`, where master's `checkEqType.WF` concludes `fun _ => False`. Now: new 615-line `Verify/Environment/Quot.lean` with no `sorry`; `#print axioms Lean4Lean.addQuot.WF` → no `sorryAx` (only `propext/Classical.choice/Quot.sound` plus master's `Lean.Expr.*`/`PersistentHashMap` axioms). `no_inductInfo` and `checkEqType.WF` no longer exist (`#print axioms Lean4Lean.checkEqType.WF` → unknown constant); `checkEqType_ok` (`Quot.lean:253`) replaces it with a real postcondition. |
| 24 | the first internal consumer landed: `inductiveReduceRecCore.WF` via `iota_rec` | REVIEW §9 (F09) | **TRUE**, including the reader's caveat | `Verify/TypeChecker/WHNF.lean:17-146` (130 lines), calling `c.trenv.iota_rec …` at `:119`. But `grep -rn inductiveReduceRecCore Lean4Lean` gives only its definition site and `Inductive/Reduce.lean:75,115` — **no consumer**; and `reduceRecursor.WF` (`WHNF.lean:147-149`) is still `:= sorry` while being consumed at `WHNF.lean:235`. The chain redex → ι rule → checker does not close. |
| 25 | `toParams` is exercised by `inductParams` and `IsDefEq.crDefEq_of_induct` | REVIEW §9 (F23), 3de1dcb | **TRUE**, including the reader's caveat | `InductiveParams.lean:419 inductParams`, `:426 crDefEq_of_induct`. Every occurrence of `toParams`, `inductParams`, `crDefEq_of_induct`, `PatsIota`, `DefEqsAsPats` is inside `Theory/Typing/InductiveParams.lean` (433 lines on trproj, 423 on iota) — no consumer in the library. Additional finding: `#print axioms Lean4Lean.VEnv.IsDefEq.crDefEq_of_induct` → `[propext, sorryAx, Classical.choice, Quot.sound]`, inherited from master's `church_rosser` sorries (`ChurchRosser.lean:1193,1212`). Also nothing in the repo ever *proves* `VInductDecl.WF` for a concrete declaration, so `inductParams` has no instance. |
| 26 | `PatsStrong` restated over well-formed prefixes (F05 fixed) | PR_iota.md, REVIEW §9 | **TRUE** | `Theory/Typing/Env.lean:57 VEnv.WFPrefix`; `EnvLemmas.lean:130 def VEnv.PatsStrong := ∀ env₁ env₀, env.WFPrefix env₁ → env₁ ≤ env₀ → env₀ ≤ env → env₀.defeqs = env₁.defeqs → env₀.pats = env₁.pats → Ordered env₀ → PatsStrongOn env₀` — it does still also quantify over the constant-only extensions, as the reader notes. `WF.strong` (`:263`) now recurses on the `WF'` derivation (`key : ∀ {ds e}, WF' ds e → env.WFPrefix e → …`), not `Ordered.induction`. |
| 27 | master's substitution theorems and the primitives layer now take `OrderedStrong`; every one of those call sites held `VEnv.WF` | PR_iota.md, PR_DESCRIPTION.md | **TRUE** | 67 `OrderedStrong` occurrences across `Theory/Typing/{Strong,EnvLemmas}.lean`, `Verify/Primitive.lean`, `Verify/Environment/Primitive/{Basic,Condition}.lean`, `Verify/Typing/{Lemmas,TrTerm}.lean`, plus 2 Experimental. 64 declaration heads that read `Ordered` on master now read `OrderedStrong` (`git diff master..iota \| grep -E '^-.*(theorem\|def\|variable!).*\bOrdered\b'` = 64). Retyped statements include `IsDefEqStrong.substEq'`, `.subst`, `IsDefEq.substDF`, `.subst`, and the `HasPrimitives.*` family. At the call sites the diff is a mechanical `henv.ordered → henv.orderedStrong` / `ctx.Ewf.ordered → ctx.Ewf.orderedStrong`, and `ctx.Ewf : VEnvs.WF`, confirming the "held `VEnv.WF`" part. |
| 28 | nothing that was previously `sorry`-free at a bare `Ordered` hypothesis loses that | PR_iota.md | **FALSE** | master `Theory/Typing/Strong.lean:675 theorem Ordered.strong (henv : Ordered env) : OnTypes env (EnvStrong env)` is a sorry-free theorem, and it is the *only* unconditional producer of `OnTypes env (EnvStrong env)` on master (`git grep 'OnTypes.*EnvStrong' master -- Theory` finds only it and `CtxStrong.strong'`). It no longer exists (`grep -rn 'Ordered\.strong' Lean4Lean` → none), and `IsDefEq.strong`/`CtxStrong.strong` now take `OrderedStrong`. Mechanised: `claims/ax7.lean`, `theorem envStrong_of_wf (h : env.WF) : env.OnTypes (VEnv.EnvStrong env) := h.strong h.patsStrong` → `depends on axioms: [propext, sorryAx, Classical.choice, Quot.sound]`. A consumer holding only `Ordered env` can no longer get `EnvStrong` at all; one holding `VEnv.WF env` gets it only through `sorryAx`. (The PR discloses the consequence in the sentence before, so the disclosure is honest; the summary sentence is not.) |
| 29 | the review's `cexB` (inconsistent WF environment) and `cexA` (off-block rule) are now refuted sorry-free | REVIEW §9 | **TRUE** (upgraded — I mechanised both; the in-repo artifacts do *not* show it) | The shipped artifacts are stale: `lake env lean review-artifacts/counterexamples/cexB.lean` fails with "Fields missing: `universes`, `ctors_params`, `ctors_result`, `ctors_positive`, `recs_over_block`, `rec_counts`" and "`rules_own_params` is not a field", i.e. it no longer typechecks rather than being refuted. `Tests/IotaShape.lean:446,450` only prove `declB_wants_large` and `¬ declB.LargeElimShape`. I wrote the missing step: `claims/cexB_refute.lean` proves `¬ declB.WF VEnv.empty` (the `universes` field forces `ℓ = .zero`, `declB_wants_large` forces `LargeElim envT .zero`, and a one-former/two-constructor block satisfies none of its three disjuncts) → `depends on axioms: [propext, Quot.sound]`. `claims/cexA_refute.lean` proves `∀ env, ¬ declA.WF env` from `recs_over_block` alone (`declA.types = []`) → `[propext, Quot.sound]`. Caveat: refuting the two counterexamples is not evidence that `patsStrong` is *provable*. |
| 30 | no mention of the downstream consumer anywhere in code or docstrings | REVIEW_2026-09-07.md | **TRUE** | `grep -rni 'lambdabox\|erasure\|peregrine\|lean-to-' Lean4Lean --include='*.lean'` → zero hits. The `consumer`/`downstream` hits are master's own generic prose (`Std/Basic.lean:14`, `Primitive/Recursion.lean`, …). |
| 31 | style conformance: lines within the 100-column convention | REVIEW §9 (d114c2d) | **PARTIAL** (true for `trproj`, off for `iota`) | On the working tree (`trproj`) the ι files have 2 lines > 100 cols (`Theory/Typing/Pattern.lean`, `Verify/TypeChecker/WHNF.lean`). On `iota` itself the same files have **7** (`Theory/Inductive.lean` 1, `InductiveLemmas.lean` 1, `Pattern.lean` 1, `IotaShape.lean` 2, `ShapeDecide.lean` 1, `WHNF.lean` 1). master's whole tree has 49. Substantively compliant either way. |
| 32 | the `rec_find` strengthening strengthens an assumption and creates no proof obligation today | iota-consume-review-brief.md, 1a1ebe8 | **TRUE** | Nothing constructs an `AddInduct`: `Verify/Environment.lean:208 \| inductDecl _ _ _ _ => sorry` is the only would-be site, and `#print axioms Lean4Lean.addDecl.WF` still reports `sorryAx`. Nothing in the repo proves `VInductDecl.WF` for a concrete declaration either (`grep -rn 'VInductDecl.WF' Lean4Lean` finds only docstrings outside `Theory/Inductive.lean` and `InductiveLemmas.lean`). |
| 33 | the downstream consumer pins this fork at `20ec229` and consumes the ι interface end-to-end | downstream-asks-round4.md, PR_FOLLOWUP.md | **TRUE** (upgraded — checked in the sibling repo) | `/home/barabba/Documents/Research/Projects/Peregrine/lean-to-lambdabox/lake-manifest.json:8` `"rev": "20ec229f1a8c6358f3b3852c4e27d2be523d1b87"`, identically on branches `dev/blueprint` and `dev/verify`. On `dev/verify`, `LeanToLambdaBox/Erases.lean:555-580` uses `SimplePattern.iotaRHS'_apply`, `SimplePattern.iotaRHS`, `SimplePattern.iota` and `VEnv.PatTyped` directly; `scratch/final_audit.lean` reports `PatsIotaSpec.of_trEnv` discharged from `TrEnv.pats_iota'` sorryAx-free. |

---

## B. Further substantive claims found in the notes and commit messages

| id | claim | source | verdict | evidence |
|---|---|---|---|---|
| 34 | "the ι files are byte-identical between `iota` and `trproj` except `Verify/Typing/Lemmas.lean`" | contrib-iota.md (header) | **FALSE** | `git diff iota trproj --stat` = 17 files, +2273/−170. ι files that differ: `Theory/Inductive.lean`, `Theory/Typing/InductiveLemmas.lean`, `InductiveParams.lean` (+28/−…), `Verify/Environment/Basic.lean` (34), `Verify/Environment/Lemmas.lean` (**659**), `Tests/IotaShape.lean`, `Tests/ShapeDecide.lean`, `Theory/VExpr.lean` (206). Most of the Theory-side difference is 100-column reflowing plus a few added `Decidable` instances, but `Verify/Environment/Lemmas.lean` genuinely changed (the projection work reshaped `pats_iota_inv_shape` and added `TrEnv.proj_defeq`, which consumes it at `:1059`). Blueprint line references that say "iota" are really trproj's. |
| 35 | `git diff --stat master..iota` = 45 files, +5628/−331, 28 commits | contrib-iota.md | **TRUE** | verbatim from `git diff --stat master..iota` and `git rev-list --count master..iota`. |
| 36 | `master:Theory/Inductive.lean` is an "8-line file"; 4 lines on master → 455 now | contrib-iota.md | **FALSE** (both numbers) | `git show master:Lean4Lean/Theory/Inductive.lean \| wc -l` = **7**; the file now is 444 lines (trproj) / 443 (iota), not 455. |
| 37 | `Theory/Typing/Pattern.lean` master 232 → 725 lines | contrib-iota.md | **TRUE** | `git show master:… \| wc -l` = 232; `wc -l` = 725. |
| 38 | `InductiveParams.lean` is 433 lines | contrib-iota.md | **PARTIAL** | 433 on `trproj`, 423 on `iota`. |
| 39 | `VInductDecl.WF` is a 20-field structure | PR_iota.md, contrib | **TRUE** | field names counted: `types_wf ctors_wf recs_wf types_uvars ctors_uvars universes recs_elim rec_params ctors_params ctors_result ctors_positive recs_over_block rec_counts rec_shape rules_nodup rules_ctor types_have_rec rules_total rule_shape rules_wf` = 20. |
| 40 | the design promotes Mario's own prototypes: `SimplePattern.iota` and the `Params` class were already on master; the branch adds exactly one class field `pat_env` | PR_iota.md, PR_DESCRIPTION.md | **TRUE** | `git show master:Lean4Lean/Theory/Typing/Pattern.lean:227` `\| iota (recursor : Name) (major : Nat) (constr : Name) (args : Nat)`; `git show master:…/ChurchRosser.lean:16-28` already has `Pat`, `pat_simple`, `pat_uniq`, `pat_wf`, `pat_app_l`, `pat_app_l_uniq`, `pat_app_uniq`, `extra_pat`. `Experimental/NormalEq.lean:92` on master likewise has `pat_wf`. The only added field is `pat_env : env.pats p r → Pat p r` (`ChurchRosser.lean:32`). The new `pat` case of `church_rosser` (`:1390-1394`) routes through master's `ParRed.extra`, mirroring the neighbouring `extra` case line for line. |
| 41 | `Aligned` gains a `pat` clause ("a rule changes neither map nor constants") and a `block` clause | PR_iota.md | **TRUE** | `Verify/Environment/Lemmas.lean:21` `\| pat : Aligned C venv → Aligned C (venv.addPat p r)`; `:28` `\| block {cis l} : …` with freshness, `Nodup`, `Forall₂` and a `foldlM` of `addConst`. |
| 42 | validation sweep: "16 adversarial declarations and a sweep of 49 library inductives" | PR_iota.md | **PARTIAL** (stale numbers) | Accurate for `655dd3f` (its `checkAll` list is `TreeP…SigmaLike` = 16 adversarial, then a long library tail). In the shipped file the single `checkAll` list has **48** entries (8 hand-built `Refl Le Dep EvI OdI PropLarge Wrap SigmaLike` + 6 local `Nat List P2 Vec Ev Od` + 34 `Init`/`Std`), plus `checkNested` over **9** and `checkShapes`/`checkTypesHaveRec` over **10** each, 12 explicit `checkIota` calls and 37 `throwError` assertions in the driver. |
| 43 | `insertDefs_wf` moved from `Extension.lean` to `Lemmas.lean`; pure relocation | PR_DESCRIPTION.md | **TRUE** | removed at `Extension.lean` (diff shows the identical header removed), present at `Verify/Environment/Lemmas.lean:211` with identical text, still used from `Extension.lean:122`. |
| 44 | `TrEnv.pats_iota'` needs `hsafe` because master's `ignore` can skip an unsafe recursor without registering its rule | PR_DESCRIPTION.md, dc01931 | **TRUE** | `Verify/Environment/Lemmas.lean:755` `(hsafe : safety ≤ (Lean.ConstantInfo.recInfo rval).safety)`; `Verify/Environment/Basic.lean:537-540` `\| ignore : C.find? ci.name = none → ¬safety ≤ ci.safety → TrEnv' C Q env → TrEnv' (C.insert ci.name ci) Q env`. |
| 45 | the `Inductive/Reduce.lean` split is behaviour-preserving ("the extracted body is the original one, with `getMajorIdx` recomputed where the caller had bound it") | 7a68882 | **TRUE** | `git diff master..iota -- Lean4Lean/Inductive/Reduce.lean`: the removed 12 lines and the new `inductiveReduceRecCore` body are token-identical apart from `info`→`rval`, `return none`→`none` and `majorIdx`→`rval.getMajorIdx`. The caller still binds `let majorIdx := info.getMajorIdx` (`Reduce.lean:106`), so the recomputation is equal by definition. |
| 46 | "the seven fields `AddInduct` previously assumed (`rec_find`, `rec_reg`, `ctor_find`, `value_find`, `find?_mono`, `wf`, `consts`) are now theorems" | d69ac5d | **PARTIAL** | At `eddf009` the structure's fields were `cis tr novalue map_eq env_eq consts wf rec_find value_find`. Only **four** of the seven named (`rec_find`, `value_find`, `wf`, `consts`) were fields; `rec_reg`, `ctor_find`, `find?_mono` never were. `env_eq`, a fifth assumed field, became a theorem but is not named. The substance ("bookkeeping derived, not assumed") is correct. |
| 47 | "Non-Experimental `sorry` terms go from 33 to 24" | d69ac5d | **PARTIAL** | `d69ac5d` side is exact: 30 grep hits − 6 prose lines = **24**. The `eddf009` side is off: 38 hits − 3 prose = **35**, not 33. |
| 48 | "Handback lemmas: `iota_defeq` [propext], `addInduct_pat` [propext, Quot.sound]" | dc01931 | **TRUE** | measured today: `iota_defeq` `[propext]`; `addInduct_pat` `[propext, Quot.sound]` — exactly those lists. |
| 49 | `349da4b` is comment-only, −279/+123 | contrib-iota.md, 349da4b | **TRUE** | `git show --stat 349da4b` → "11 files changed, 123 insertions(+), 279 deletions(-)". |
| 50 | `3bbdb22` is the only commit with a different committer identity | contrib-iota.md | **TRUE** | `git log --format='%ce' master..iota \| sort \| uniq -c` → 27 `sosso@cs.au.dk`, 1 `alesosso@gmail.com`; that one is `3bbdb22` (author is still `sosso@cs.au.dk`). |
| 51 | `VInductDecl.WF` "follows the thesis §2.6.1–2.6.4" | PR_iota.md, docstrings | **TRUE with two documented divergences** | The section numbering checks out: `axioms.tex` is §2 ("The axioms"), §2.6 "Inductive types", §2.6.1 "Inductive specifications", §2.6.2 "Large elimination", §2.6.3 "The recursor", §2.6.4 "The computation rule (ι reduction)". Correspondences verified line by line: thesis `$\imax(\ell',\ell)\le\ell$` on every constructor field ↔ `universes` field's `VLevel.imax u ℓ ≤ ℓ` (`Inductive.lean:351-359`); thesis recursive-argument form `$\forall z::\gamma.\;t\;e$` ↔ `FieldPositive`/`ValidIndApp` (`:93`,`:75`); thesis `$\rec_P:\forall C:\kappa.\;\forall e::\vep.\;\forall a::\alpha.\;\forall z:P\;a.\;C\;a\;z$` ↔ `RecShape` (`:143`, `piArity = np+nm+nmin+nind+1`, body `bvar (nind+nmin+1+(nm-1-j))` applied to `bvarsDesc 0 (nind+1)`); thesis `$\rec_P\;C\;e\;p[b]\;(c\;b)\equiv e_c\;b\;v$` ↔ `RuleShape` (`:174`). Divergence 1: the thesis *generates* recursor types and rules, the branch *carries* them as `VRecursor`/`VRecRule` data and pins 20 syntactic clauses (stated in `PR_iota.md` and the `VInductDecl.WF` docstring). Divergence 2: `RuleShape` pins only `\|v\| = nrec`, not the thesis's `$v_i=\lambda x::\xi_i.\;\rec_P\;C\;e\;\pi_i[b,x]\;(u_i\;x)$` — the docstring says so explicitly ("The terms `v` are not pinned, syntactically or through typing"). |
| 52 | `IsDefEq.pat` "takes the thesis's regularity of ι as a rule", repaid by `PatsStrong`, citing `typesys.tex` "Regularity continued" and `unique.tex` "Regularity of reductions" | docstrings (`Basic.lean:56`, `EnvLemmas.lean:323`) | **TRUE** | `Basic.lean:60-64`: premise `Γ ⊢ e : A`, conclusion `Γ ⊢ e ≡ r.1.apply m1 m2 : A` — no typing premise on the reduct. Both cited lemmas say exactly that: `typesys.tex:114` "If $\Gamma\vdash e:\alpha$ and $e\rightsquigarrow e'$, then $\Gamma\vdash e\equiv e':\alpha$"; `unique.tex:123` "If $\Gamma\vdash e:\alpha$ and $\Gamma\vdash e\rightsquigarrow_\kappa e'$, then $\Gamma\vdash e\equiv e':\alpha$" — "Note that the first part implies subject reduction". |
| 53 | `VInductDecl.LargeElim` mirrors the kernel's `isLargeEliminator` (thesis §2.6.2) | docstring `Inductive.lean:221` | **TRUE, and more permissive than the kernel; stricter than the thesis** | Kernel `Inductive/Add.lean:258-277`: `stats.isNotZero` → true; single type, no ctors → true; single ctor → every binder past the params whose domain's sort is not `isAlwaysZero` must appear in `type.getAppArgs`. Model `Inductive.lean:226-235`: `ℓ.IsNeverZero ∨ (one former, no ctor) ∨ (one former, one ctor, every field either `env.HasType … F (.sort .zero)` or `FieldInIndices`)`. The propositionality test is a typing judgment where the kernel's is the syntactic `isAlwaysZero`, so the model accepts strictly more (the right direction for a refinement, as the reader says). Against the *thesis* it is stricter: the thesis's `\LEctor` rule `$\frac{\Gamma;t:F\vdash\beta\LEctor}{\Gamma;t:F\vdash(\forall z::\gamma.\;t\;e)\to\beta\LEctor}$` lets a recursive field through unconditionally, while the model requires every field, recursive included, to be a `Prop` or an index. |
| 54 | "the only tracked change outside `Lean4Lean/` is `divergences.md`" | contrib-iota.md (d) | **TRUE** | `git diff --name-only master..iota \| grep -v '^Lean4Lean/'` → `divergences.md`. |
| 55 | master's `checkEqType.WF` was itself vacuous (`fun _ => False`) and is deleted | (not claimed in the notes; found here) | **TRUE — additional finding** | `git show master:Lean4Lean/Verify/Environment.lean:113-114` `theorem checkEqType.WF … : (checkEqType env).WF fun _ => False`, proved via `TrEnv'.no_inductInfo`. That theorem is *false* once inductives can be translated, so its deletion is forced, not optional; `checkEqType_ok` replaces it. Worth calling out in the PR: the branch does not merely replace a vacuous *proof*, it removes a now-false *statement* of master's. |
| 56 | nothing constructs an `AddInduct` and nothing establishes `VInductDecl.WF` for a real declaration; the whole `Verify` ι interface is usable only by a caller that *assumes* `TrEnv` | contrib-iota.md (d) | **TRUE** | `Verify/Environment.lean:208` is the only site; `grep -rn 'VInductDecl.WF' Lean4Lean` finds no construction, only docstrings and `Tests/IotaShape.lean`, which decides the *syntactic* clauses on kernel data and never assembles the structure (the typing clauses `types_wf`/`ctors_wf`/`recs_wf`/`universes`/`rules_wf` are not decidable). Consequently `inductParams`/`crDefEq_of_induct` have no instance. |
| 57 | `VEnv.WF.patsStrong` needs type-former injectivity, itself `sorry` on master | PR_iota.md, docstring | **TRUE** | `Theory/Typing/Injectivity.lean:12,21,34` are `sorry` on master and unchanged on `iota` (`IsDefEqU.sort_inv`, `forallE_inv_stratified`, `forallE_inv`, `sort_forallE_inv`). The `patsStrong` docstring (`EnvLemmas.lean:323-333`) gives the concrete counterexample against `Ordered` (`List Nat ≡ List Bool`) and is accurate. |
| 58 | the exported ι interface has exactly one lemma with an in-repo consumer (`iota_rec`), and that consumer has none | contrib-iota.md (e) | **TRUE on `iota`; no longer true on `trproj`** | `iota_rec`: defined `Lemmas.lean:929`, used once (`WHNF.lean:119`). `iota_defeq`: used only inside `Lemmas.lean` (by `iota_rec`). `pats_iota'`: only inside `Lemmas.lean`. On `trproj`, `pats_iota_inv_shape` gained a second consumer, `TrEnv.proj_defeq` (`Lemmas.lean:1021`, firing the ι rule at `:1059`) — projection work, not ι. |
| 59 | "`Tests/IotaShape.lean` validates … over the recursors of 65 inductives" | d69ac5d | **UNVERIFIABLE as stated** | the driver at that revision is spread over several loops of different sizes; the shipped file's single largest sweep is 48. Treat the figure as a snapshot, not a current fact. |
| 60 | `toParams` is `@[reducible]`, `pat_wf` is `IsDefEq.pat` via the `OK`/`Realizes` bridge, `pat_env` is the identity | PR_iota.md, PR_DESCRIPTION.md | **TRUE** | `InductiveParams.lean:392-408`: `@[reducible] def toParams … pat_wf := fun … hok => let ⟨_, hr, hall⟩ := hok.exists_realizer …; ⟨A, IsDefEq.pat hpat hmatch hty hr hall⟩`, `pat_env := id`, `extra_pat := fun h1 h2 h3 => hδ h1 h2 h3`. `#print axioms Lean4Lean.VEnv.toParams` → `[propext, Classical.choice, Quot.sound]`. |

---

## What the notes overclaim

1. **"Nothing that was previously `sorry`-free at a bare `Ordered` hypothesis loses that"** (`PR_iota.md`, claim 28). Master's `Ordered.strong` was precisely such a theorem and is deleted; `OnTypes env (EnvStrong env)` is now reachable only through `PatsStrong`, and the only supplier is the sorried `WF.patsStrong`. Mechanised in `claims/ax7.lean`. The preceding paragraph of the same PR discloses the consequence honestly, so this is a bad summary sentence on top of a good disclosure — but it is the sentence a maintainer will quote.
2. **"The downstream consumer interface inherits no new soundness gap"** (`IOTA_CONTRIBUTION.md`, claim 17). True of the four handback lemmas measured in isolation; false of the model they speak about, since every strong-system result at a `VEnv.WF` hypothesis now routes through `patsStrong` via the `CoeOut` instance. The repo's own comment at `EnvLemmas.lean:342` is more honest than the note.
3. **Three whole notes are two reworks out of date.** `IOTA_CONTRIBUTION.md` and `PR_DESCRIPTION_formal.md` assert the `.safe` gate (12), the 12 `IOTA-TODO` sorries (13), the underspecified `WF` (14), the false `pat_uniq`/`pat_app_uniq` (15) and the sorried `Aligned.addInduct` (16) — all five false today, all five true at some earlier commit. Pasting either into a PR would be actively misleading.
4. **Validation numbers are stale** (claim 42): `PR_iota.md` still says "16 adversarial declarations and a sweep of 49 library inductives"; the shipped driver runs one 48-entry `checkAll` list (14 adversarial), 9 nested, 10 + 10 shape checks.
5. **Small counting errors in commit messages**: `d69ac5d` names seven previously-assumed `AddInduct` fields when only four were fields (46), and says "33 to 24" where the earlier count is 35 (47).
6. **The reader's own note overclaims** that the ι files are byte-identical between `iota` and `trproj` (34); 17 files differ, `Verify/Environment/Lemmas.lean` by 659 lines. Its `master:Theory/Inductive.lean` line counts (36) are also wrong.
7. `contrib-iota.json`'s verification *recipe* for "no new axioms" ("`git grep '^axiom '` → only `Experimental/`") is wrong on both branches — master already carries 12 non-`Experimental` axioms in `PtrEq.lean` and `Verify/Axioms.lean`. The claim itself survives; the recipe does not.

## What the notes underclaim

1. **The two review counterexamples really are dead.** Nothing in the repo shows it — the `review-artifacts/counterexamples/*.lean` files no longer even typecheck, and `Tests/IotaShape.lean` stops one step short. In this pass both fell in a few lines: `¬ declB.WF VEnv.empty` (`[propext, Quot.sound]`) and `∀ env, ¬ declA.WF env` (`[propext, Quot.sound]`). Those two theorems belong in `Tests/IotaShape.lean`; as it stands the branch's strongest defence of its central definition is undocumented.
2. **The kernel-agreement test suite actually runs green.** `PR_iota.md` asserts it; nobody re-ran it. `lake env lean Lean4Lean/Tests/IotaShape.lean` completes in 1.7 s with zero diagnostics, and a deliberately-wrong control does fail, so the 37 assertions — including the ι-reduct equality against the executable `inductiveReduceRec` on 12 explicit rec/ctor pairs plus `checkIotaAuto` over 48 blocks — are live.
3. **`TrEnv.pats_iota'` and `TrEnv.iota_rec` are fully `sorryAx`-free once `TrProj` is real.** The notes only claim "`sorryAx` via `TrProj`". Measured on `trproj`, both come back clean (modulo master's own `PersistentHashMap` axioms), which is the strongest possible confirmation of the claim they hedge.
4. **`addQuot.WF` is not just "real", it is `sorryAx`-free and unconditional**, and it *replaces a false statement of master's*, not merely a vacuous proof: master's `checkEqType.WF : … fun _ => False` cannot survive inductives entering `TrEnv`. The PR sells this as collateral; it is the one place where the branch demonstrably fixes a soundness-relevant vacuity in upstream.
5. **`Experimental`'s four `pat` cases are closed with real proofs**, not merely "restored to master's count": `Typing.pat_env` plus `Realizes.toOK` discharge both `ParallelReduction` inductions, and `IsDefEq1`/`IsDefEqU1` gained genuine `pat` constructors. The note reports only the count.
6. **The thesis alignment is more exact than the notes assert**, down to the `imax(ℓ',ℓ) ≤ ℓ` bound and the recursor telescope, and the two places where it diverges are each stated in the docstring at the point of divergence. The two honest caveats (`v` unpinned; `LargeElim` stricter than the thesis on recursive fields, looser than the kernel on propositionality) are documented in-file but absent from the PR text, where a reviewer would look for them.
