# New code issues noted while completing primitives-arith

- Lean4Lean/Verify/Environment/Primitive/Clauses.lean:170 — checkNatBoolCases.WF is a parametrised schema, not a clause: the file contains fifteen theorems for fourteen recognizer branches. The chapter overview written by the previous author calls them 'fifteen short theorems for the simple clauses', which reads as fifteen clauses; the new section text states the 15/14 split explicitly rather than silently correcting the overview.
- Lean4Lean/Verify/Environment/Primitive/Clauses.lean:58 and :170 — checkNatPred.WF, checkNatBoolCases.WF, checkNatBEq.WF and checkNatBLE.WF are the only four theorems in Clauses.lean with no iota-attributed line, yet the census marks all four usesSorry=true. Their dependence on VEnv.WF.patsStrong arrives entirely through callees that were strengthened in another file (withNatProbe, of_pred_equations, of_constructor_cases) plus the CoeOut instance, so the blast radius of the iota obligation is strictly larger than the blame data suggests.
- Lean4Lean/Verify/Environment/Primitive/DivMod.lean:22 — checkNatFuelRec.WF's interface takes roughly thirty parameters over sixty lines and passes the recognizer's own Expr builders (goName, lhs, H, els, top) in as arguments, so the statement cannot be read without Lean4Lean/Primitive.lean beside it. Not in the json issues array; recorded as a review note on the node rather than a chapter-level issue since it is master's design and each parameter is commented.
