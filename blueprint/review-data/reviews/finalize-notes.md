## claims-iota.md was complete and internally consistent — all 60 rows verdicted, both closing sections present; re-verified independently and emitted claims-iota.json (43 TRUE / 9 FALSE / 7 PARTIAL / 1 UNVERIFIABLE)

- Completeness: claims-iota.md (117 lines) was NOT truncated. Section A carries claims 1-33 (the contrib-iota.json set), section B claims 34-60 (further claims from notes/commit messages), ids 1-60 contiguous with no duplicates, every row has all five columns (id, claim, source, verdict, evidence), every verdict is one of TRUE/FALSE/PARTIAL/UNVERIFIABLE, no thin/empty evidence cell. Both closing sections exist: 'What the notes overclaim' (7 numbered items) and 'What the notes underclaim' (6 numbered items).
- Only repair needed was cosmetic: rows for claims 8 and 11 contained unescaped '|' inside inline code ('| pat', '| induct : ...'), breaking those two rows' markdown arity. The pipes are now escaped; no content was altered. After the fix every table row parses to exactly 5 cells.
- Independent re-verification (nothing weakened or inflated, no verdict changed). Structural checks re-run from scratch and reproduced exactly: master:Theory/Inductive.lean is 7 lines with both decls `sorry`, now 444 lines (trproj) / 443 (iota) with `VEnv.addInduct` at :316 and a 20-field `VInductDecl.WF` at :335 (field names counted = 20); non-Experimental sorry hits master 28 / iota 24 / trproj 22, minus prose = 23 / 21 / 19, with `VEnv.WF.patsStrong` the single branch-own sorry; `git diff --stat master..iota` = 45 files, +5628/-331 over 28 commits; `git diff --stat iota trproj` = 17 files, +2273/-170 (so the 'byte-identical' header claim 34 is indeed FALSE); axiom-line diff master..iota differs only by a docstring continuation line; `TrEnv'.induct` at Verify/Environment/Basic.lean:582 has no safety premise while dc01931 had `safety = .safe →` (claims 11 TRUE / 12 FALSE confirmed); zero `IOTA-TODO` and zero `native_decide` hits; only tracked non-Lean4Lean change is divergences.md; 1 non-user committer (3bbdb22); 100-column violations 2 on trproj vs 7 on iota; every one of the 117 Lean4Lean modules has an olean (120 under Lean4Lean/).
- Semantic re-verification via `lake env lean` (no `lake build`; repo unmodified): `TrEnv.iota_defeq` -> [propext]; `TrEnv.pats_iota'` and `TrEnv.iota_rec` -> [propext, Classical.choice, Quot.sound] + master's three PersistentHashMap axioms, i.e. sorryAx-free; `addInduct_pat`/`addInduct_le` -> [propext, Quot.sound]; `addInduct_WF` and `Aligned.addInduct` -> no sorryAx; `WF.pat_uniq`/`WF.pat_app_uniq` proved (claim 15 FALSE confirmed); `addQuot.WF` sorryAx-free while `Lean4Lean.checkEqType.WF` is now an unknown constant (claims 23/55); `VEnv.WF.patsStrong`, `WF.orderedStrong`, `crDefEq_of_induct` and the ax7 probe `envStrong_of_wf` all carry sorryAx (claims 17 and 28 FALSE confirmed); both counterexample refutations `declA_not_WF` and `declB_not_WF` -> [propext, Quot.sound]; `lake env lean Lean4Lean/Tests/IotaShape.lean` exits 0 in 1.7 s with 12 explicit `checkIota` calls, a 48-entry `checkAll` list (8+6 hand-built + 34 Init/Std), `checkNested` over 9, `checkShapes`/`checkTypesHaveRec` over 10 each and 37 throwError assertions in the run_meta driver at :454 (claims 19/42 confirmed, including that PR_iota.md's '16 adversarial + 49 library' is stale).
- Sorry-count claims in commit messages re-derived at the named revisions: eddf009 = 38 grep hits - 3 prose = 35 terms (so d69ac5d's '33 to 24' is wrong on the earlier figure) and d69ac5d = 30 - 6 prose = 24 (exact); at eddf009 `AddInduct`'s fields were cis/tr/novalue/map_eq/env_eq/consts/wf/rec_find/value_find, so only four of the seven fields d69ac5d names were ever fields — both PARTIAL verdicts (46, 47) confirmed.
- Output written: /tmp/claude-1000/-home-barabba-Documents-Research-Projects-lean4lean/9174ff57-8c00-414a-9c8e-77dd43ee160c/scratchpad/understand/claims-iota.json — top-level keys meta / claims / overclaims / underclaims. Each claim object keeps the claims-trproj.json shape (id, claim, source, verdict, evidence) plus `verdict_note` (the full qualified verdict text, e.g. claim 17 'FALSE as a statement about the model; TRUE narrowly about the four handback lemmas' — `verdict` holds the primary token) and `section`. `overclaims`/`underclaims` carry n, summary, related_claim_ids and the full text. `meta` records the revisions, method, verdict key, verdict counts and the structure_check (including the pipe-escaping repair and the list of re-run checks).

## claims-trproj.md was complete (50 verdict-bearing rows + both closing sections); two internal slips fixed, key findings re-verified, and claims-trproj.json rewritten as the structured form

- The markdown was NOT truncated: /tmp/claude-1000/-home-barabba-Documents-Research-Projects-lean4lean/9174ff57-8c00-414a-9c8e-77dd43ee160c/scratchpad/understand/claims-trproj.md holds all 50 table rows (id, claim, source, verdict, evidence), every row carrying one of TRUE/FALSE/PARTIAL/UNVERIFIABLE, plus 'Summary of verdicts', 'What the notes overclaim' (8 items) and 'What the notes underclaim' (6 items).
- Two internal-consistency slips found and corrected in place, with no verdict changed: (i) the 'Summary of verdicts' TRUE row omitted id 35 and listed id 25 twice (once as '25(as refutation)', once in the FALSE row), covering only 49 distinct claims -- fixed to 4,5,6,7,14,15,16,17,18,20,21,22,28,29,30,31,32,35,39,40,41,42,43,44,45,46,47,49 (count 28 was already right); (ii) claim 25's evidence enumerated 'Verify/Environment/Lemmas.lean:1014,1021' for the 6 proj_defeq grep hits -- line 1014 is docstring prose without the name, so the enumeration summed to 7; corrected to the statement at :1021 only. Hit count and conclusion unchanged.
- Verdict distribution (unchanged): TRUE 28, PARTIAL 12, FALSE 8, UNVERIFIABLE 2, over ids 1-50.
- Re-verified the load-bearing evidence independently. Sorry census: raw non-Experimental grep master 28 / iota 24 / trproj 22, minus comment-and-docstring hits (5/3/6) = 23/21/16, exactly as claimed. Sorry cones (scratchpad/claims/cone2.lean re-run): TrEnv.proj_defeq and TrProj.defeqDFC -> [IsDefEqU.forallE_inv_stratified, IsDefEqU.sort_inv, VEnv.WF.patsStrong]; TrExprS.uniq and inductiveReduceRecCore.WF -> those plus TrProj.uniq; TrProj.weak'/instN -> []. Axiom profiles: TrProj/TrProjCtor = [propext], TrExprS = [propext, Classical.choice, Quot.sound], the three TrEnv bridge lemmas sorryAx-free. Census: 663 Lean4Lean.* declarations whose type mentions TrExprS/TrExpr/TrProj/TrProjCtor, 614 sorryAx-free. Tests/ProjShape.lean and Tests/ProjInhabit.lean re-elaborate with no output. No lake build was run.
- Also re-confirmed: zero IOTA-TODO/PROJ-TODO markers at HEAD (4 at the reviewed head f7dabf1); master 8223d223 IS an ancestor of trproj (B4 resolved); toTrProj/exists_ctorName/pats_iota_inv are gone; proj_defeq has zero in-library consumers while iota_rec has one (Verify/TypeChecker/WHNF.lean:119); master InferType.lean:391 concluded 'exists ty', c.TrTyping (.proj st i e) ty e' ty'' whereas trproj:398,410 read 'exists e'' ty', ... e'' ty'', so PR_trproj.md:44's 'keeps its statement' is false; Inductive/Reduce.lean changes 34 lines over master..trproj yet inductiveReduceRecCore appears in neither PR text.
- claims-trproj.json rewritten as the structured form of the markdown: keys artifact, subject, repo, revisions, method, verdict_key, summary (counts + id lists + headline), claims (50 rows verbatim), overclaims (8, with claim_ids), underclaims (6, with claim_ids), open_obligations_at_head (patsStrong, TrProj.weak'_inv, TrProj.uniq, inferProj.WF_struct, inferProj.WF with files/lines/owners), source, consistency_check (fixes applied, spot re-verification log, what was deliberately not re-verified: claims 8 and 27 stay UNVERIFIABLE, claim 23 stays PARTIAL for want of a build).

## hygiene-review.md was already complete; I verified it against the repo and wrote the structured hygiene-review.json (57 findings, 11 corrections)

- hygiene-review.md needed no completion: all seven lens sections (a)-(g) are present, every bullet carries a severity tag and a file:line anchor (the only exception is the (e) git-history bullet, anchored by commit hashes), the baseline metrics table has a value in every cell, and the Verdict section closes with the solid/questionable split, the six-item maintainer pushback list, and the 'Is it slop?' answer. No hygiene-review.json existed; I created it.
- Independently reproduced exactly: diff scope 49 files +7762/-363 at HEAD 20ec229; non-merge churn +11439/-3399 over 46 commits plus all four per-file churn figures; the entire baseline metrics table and all four tactic-density rows (omega P 2.0% vs M 0.4%); sorry delta 5 added / 12 removed after excluding prose mentions; zero >100-column and zero trailing-whitespace added lines with exactly two double-blank pairs; Theory/Pattern.lean created (cb920e1) and deleted (3bbdb22); exactly one duplicated patch-id (655dd3f/75ffde9) and two duplicated subjects; the Reduce.lean refactor at +21/-13 with inductiveReduceRecCore elaborating free of its section variables; the one-line Quot.lean edit documented at divergences.md:7 by 30897c0; twelve checkIota rules; checkProj calling the real kernel's addDeclCore.
- Two corrections that change what a claim asserts. (1) c9: the md's 'No master file does this' about `#guard_msgs in #print axioms` is FALSE - master already uses that idiom at Lean4Lean/Tests/LevelStd.lean:80 and :90; the branch extends a rare existing practice rather than introducing it. The five pins in ProjInhabit.lean and their quoted axiom profiles are exact. (2) b1: the figure '158 mechanically-rewritten master call sites' is not reproducible - measured 173 added OrderedStrong/orderedStrong occurrences over 169 added lines (148 outside Strong.lean). The finding's substance is confirmed independently: master:Strong.lean:689 `IsDefEq.strong (henv : Ordered env)` became trproj:Strong.lean:805 `(henv : OrderedStrong env)`, and EnvLemmas.lean:342 admits in-source that 'Every use of the strong system is therefore conditional on VEnv.WF.patsStrong' (the sorry at :334).
- Nine further corrections are count- or citation-level and do not change any finding's direction: proj_defeq measures 138 lines with 17 omega / 5 `by simp; omega` / 2 `rw [show ... by omega]` against the md's 131 / ~12 / eight / six; inductiveReduceRecCore.WF measures 129 lines with 5 rather than nine `simp; omega` side conditions; `PatsIota.functional` is at InductiveParams.lean:144 (the md cites :180, which is inside PatsIota.induct's docstring) with the VEnv.WF route at :232; ProjInhabit has 81 h-prefixed namespace theorems, not ~50 (understated); master's longest proof measures 1136 lines, not 887 (comparison strengthened); DefEqsAsPats's definition is 7 lines not 5; master's Theory/ longest comment block outside LevelSat is 10 not 11; Environment.EqSafe was branch-internal (added d69ac5d, removed 30897c0), never on master; 389 not 369 contributed declarations carry docstrings, and checkAll covers 48 inductives (40 real kernel types). The (c) prose says 'Six mismatches' but lists seven - all seven were checked and all seven hold.
- The three HIGH findings all verify: inferProj.WF (InferType.lean:407) is a sorry whose own docstring says 'not provable as stated' and is consumed at :479; VEnv.WF.patsStrong (EnvLemmas.lean:334) is a sorry reached through the CoeOut at :343; the AddInduct layer (Basic.lean:272, a 619-line file) has no producer because addDecl.WF's inductDecl case is still master's sorry at Verify/Environment.lean:208. Dead-declaration claims were cross-checked against the independent census in unused-contrib.json and by direct grep - VRecursor.getFirstIndexIdx, CtorHeaded.forallE, inductiveReduceRecCore.WF and the orphaned lift_r_one chain all confirmed unused, and the spine/iotaCounts cluster confirmed closed (each lemma used only by the next, terminal one by nothing).
- Structured output: 57 findings (a 11, b 12, c 9, d 6, e 5, f 4, g 10; 3 HIGH, 20 MEDIUM, 31 LOW, 2 POSITIVE, 1 VERIFIED-BEHAVIOUR-PRESERVING), each with lens, severity, file, line, finding text, evidence, and a verification record; plus the reproduced baseline metrics, the verdict, a completeness check, 11 corrections, and a list of claims verified exactly. No finding was weakened or inflated - corrections are recorded alongside the original wording.

