\chapter{References and divergences}
\label{chap:references}

This chapter names every published source lean4lean's kernel layer is checked against, places
lean4lean on the same grid as the Rocq development it parallels, and lists, notion by notion,
every point where MetaRocq/PCUIC, Carneiro's thesis and lean4lean's kernel diverge.

\section{The sources}
\label{sec:ref-sources}

\par\smallskip\noindent
\begin{tabular}{p{0.18\linewidth}p{0.1\linewidth}p{0.62\linewidth}}
\toprule
Layer & Canonical & Fallback \\
\midrule
Rocq kernel theory & \src{JACM} & \src{CCC} (earlier framing), \src{HAB} (current naming) \\
Rocq kernel verification & \src{JACM} & \src{CCC} (sound-only precursor), \src{HAB} \\
Rocq pre-PCUIC history & \src{MR} & -- (no successor; superseded outright) \\
Rocq file/line lookup & \src{MRD} & -- (no paper gives line numbers) \\
Lean kernel theory & \src{CT} & -- (lean4lean's own specification) \\
\bottomrule
\end{tabular}\par\smallskip

\begin{description}
\item[\src{CT}] \lead{Identity} \emph{The Type Theory of Lean}, Mario Carneiro, MSc draft,
Carnegie Mellon University; a \LaTeX{} source tree (\texttt{axioms.tex}, \texttt{typesys.tex},
\texttt{unique.tex}, \texttt{Wtypes.tex}, \texttt{soundness.tex}, \texttt{normalization.tex},
\texttt{compilation.tex}), not a rendered, finished document. Two chapters end unfinished
(literal token \texttt{UNFINISHED}): \texttt{normalization.tex} after introducing only the
restricted $K$ (not $K^+$) reduction, with no termination theorem; \texttt{soundness.tex}'s
reflection extension. \texttt{compilation.tex} is a 25-line fragment with no theorem.

\par\smallskip\noindent
\begin{tabular}{p{0.3\linewidth}p{0.32\linewidth}p{0.3\linewidth}}
\toprule
Establishes & Results cited here & Relation to others \\
\midrule
\texttt{axioms.tex}: typing, $\equiv$, $\Leftrightarrow$, reduction, universes with
\texttt{imax}, let/delta, inductive \texttt{spec}/\texttt{ctor}, $\iota$, K-like reduction,
quotients, extensionality, choice. &
K-A rows of \S\ref{sec:ref-divergences} cite it throughout. &
No Rocq counterpart states an algorithmic judgment this way (K-B4). \\
\texttt{typesys.tex}: undecidability of $\equiv$, non-transitivity of $\Leftrightarrow$,
Regularity/Weakening/Substitution. &
Its three lemmas ground Weakening/Substitution/Regularity. &
The negative results have no lean4lean formal home (K-T4). \\
\texttt{unique.tex}: \texttt{thm:unique}, Church-Rosser for $\kappa$
(\texttt{thm:church\_rosser}), \texttt{thm:ckappa}, \texttt{thm:1dinv}. &
Targets of K-C3 and K-C1. &
lean4lean takes a different route (K-C3); \texttt{thm:1dinv} has no proved analogue (K-C4). \\
\texttt{Wtypes.tex}: a $\Sigma$-projection typing shape for structures, with an $\eta$ rule the
text flags as not a real Lean definitional equality. &
Reused, without the $\eta$ rule, for \texttt{Expr.proj} (K-A8). &
Not formalised as an eight-primitive system (K-T7). \\
\bottomrule
\end{tabular}\par\smallskip

\item[\src{JACM}, \src{CCC}] \lead{Identity} \src{CCC}: Sozeau, Boulier, Forster, Tabareau,
Winterhalter, "Coq Coq Correct!", POPL 2020, PACMPL 4, Article~8. \src{JACM}: Sozeau, Forster,
Lennon-Bertrand, Nielsen, Tabareau, Winterhalter, J.~ACM 72(1), Article~8, Jan 2025. \src{JACM}
p.~8:5 names \src{CCC} as the conference version it extends: \src{CCC} is \hl{earlier}, not later.

\par\smallskip\noindent
\begin{tabular}{p{0.3\linewidth}p{0.32\linewidth}p{0.3\linewidth}}
\toprule
Establishes & Results cited here & Relation to others \\
\midrule
\src{CCC} Sec 2: PCUIC syntax, universes, valuations, cumulativity, typing, confluence by the
triangle method; subject reduction, validity, principal typing, strong normalisation and the
guard condition left as \texttt{Conjecture}/\texttt{Axiom}. &
First appearance of PCUIC itself. &
Superseded by \src{JACM} Sec 3-4 on every shared topic. \\
\src{CCC} Sec 3: fuel-free weak-head reduction, a graph-based universe-consistency decision
procedure, a conversion checker, a bidirectional \texttt{infer}, all proved sound. &
First verified checker (sound only). &
Superseded by \src{JACM} Sec 6 (sound and complete). \\
\src{JACM} Sec 3-4: the PCUIC specification plus an algorithmic specification proved equivalent to
it, and cumulative inductive types with a subject-reduction proof. &
Default reference for every K-A/K-B row. &
The theorem reference of record for Rocq kernel theory. \\
\src{JACM} Sec 6, pp.~8:56-8:59: the checker proved \hl{sound and complete}, every rejection
refuting the corresponding derivation; found and fixed a completeness bug in Coq 8.14. &
Default reference for K-D1, K-D2. &
No lean4lean counterpart claims completeness (K-D1). \\
\src{JACM} Sec 5: confluence, subject reduction, weak-CBV standardisation (5.6, the one use of
strong normalisation here), canonicity (5.7), consistency (5.8). &
Default reference for K-C1--K-C7. &
lean4lean states none of canonicity or consistency (K-C6). \\
\bottomrule
\end{tabular}\par\smallskip

\item[\src{MR}] \lead{Identity} Sozeau, Anand, Boulier, Cohen, Forster, Kunze, Malecha,
Tabareau, Winterhalter, "The MetaCoq Project", J.~Automated Reasoning 64:947-999, 2020; extended
journal version of an ITP 2018 paper. Sect.~2.7, p.~973 states explicitly that its
\texttt{infer}/\texttt{check}/\texttt{check\_conv} algorithms have \hl{no soundness or
completeness proof}: the pre-verification stage \src{CCC} and \src{JACM} later prove correct.

\par\smallskip\noindent
\begin{tabular}{p{0.3\linewidth}p{0.32\linewidth}p{0.3\linewidth}}
\toprule
Establishes & Results cited here & Relation to others \\
\midrule
Sect.~2: a reification \texttt{term} of Coq's kernel syntax, its typing judgment, reduction,
well-formed environments, universe algebra -- the pre-PCUIC ancestor of \src{CCC}'s specification.
The string "PCUIC" never occurs in this paper. &
K-E2 cites Sect.~3, pp.~973-977, for the unverified Template quoting layer, and Sect.~2.7 for
the "no proof yet" baseline. &
Establishes no kernel-verification result; superseded by \src{CCC}/\src{JACM} throughout. \\
\bottomrule
\end{tabular}\par\smallskip

\item[\src{HAB}] \lead{Identity} Matthieu Sozeau, \emph{Mechanized Metatheory and Certified
Compilation for The Rocq Prover}, HDR, Universite de Nantes, 2026, HAL tel-05544162; a
retrospective synthesis, not a fresh research paper. Lists \src{JACM} first among four articles it
"is based on"; Ch.~1 is an informal tour with no theorem; Ch.~8's open-problems list,
pp.~127-131, names eta, \texttt{SProp} extensionality and guard-checker verification as
unproved.

\par\smallskip\noindent
\begin{tabular}{p{0.3\linewidth}p{0.32\linewidth}p{0.3\linewidth}}
\toprule
Establishes & Results cited here & Relation to others \\
\midrule
Ch.~2, pp.~26-42: PCUIC syntax, universes, cumulativity, typing, well-formed global environments
including positivity and cumulative inductive types. &
Fallback throughout \S\ref{sec:ref-divergences}, e.g.\ K-A1's universe algebra, K-A5's eta rule. &
Current-naming (MetaRocq v1.4, Rocq 9.0) rewrite of \src{JACM}/\src{CCC}'s theory chapters. \\
Ch.~3-5, pp.~44-84: the algorithmic specification, its equivalence to the declarative one, core
metatheory, the sound-and-complete checker. &
Fallback for K-D1's checker guarantee. &
Rewrite of \src{JACM}/\src{CCC}'s verification chapters. \\
\bottomrule
\end{tabular}\par\smallskip

\item[\src{MRD}] \lead{Identity} \texttt{metarocq.github.io}, the project's live documentation:
a landing page, per-package \texttt{README.md} files, a generated coqdoc index; no version
number, drifts ahead of every paper. \lead{Caveat} no theorem numbers and no page numbers, only
a one-line-per-file prose index; always cited by file name.

\par\smallskip\noindent
\begin{tabular}{p{0.3\linewidth}p{0.32\linewidth}p{0.3\linewidth}}
\toprule
Establishes & Results cited here & Relation to others \\
\midrule
The file/module map: \texttt{PCUICAst}, \texttt{PCUICTyping}, \texttt{PCUICCumulativitySpec},
\texttt{PCUICConfluence}, \texttt{PCUICSR}, \texttt{PCUICValidity}, \texttt{PCUICPrincipality},
\texttt{PCUICSN} (\texttt{pcuic/theories/}); \texttt{PCUICSafeReduce},
\texttt{PCUICSafeConversion}, \texttt{PCUICSafeRetyping}, \texttt{PCUICSafeChecker}
(\texttt{safechecker/theories/}). &
Tiebreaker for a current name or line number, e.g.\ \texttt{check\_wf\_env} at
\texttt{PCUICSafeChecker.v:2398} in K-A12/K-E3. &
No theorem correspondence; a module index only. \\
\bottomrule
\end{tabular}\par\smallskip
\end{description}

\section{Where lean4lean sits}
\label{sec:ref-where}

Restricted to the kernel layer, the project grid has two columns and two rows.

\begin{center}
\includegraphics[width=\linewidth]{fig/layers}
\end{center}

\par\smallskip\noindent
\begin{tabular}{lp{0.43\linewidth}p{0.43\linewidth}}
\toprule
 & Kernel theory & Kernel verification \\
\midrule
Rocq & PCUIC's declarative specification: universes and valuations, cumulativity, typing,
well-formed global environments (\src{JACM} Sec 3-4; \srcs{HAB}{Ch.~2}). &
MetaRocq's SafeChecker: fuel-free weak-head reduction, a universe-consistency decision
procedure, a checker proved sound \emph{and} complete (\src{JACM} Sec 6; \srcs{HAB}{Ch.~3-5}). \\
Lean & Carneiro's thesis \src{CT}: typing, $\equiv$, $\Leftrightarrow$, reduction, universes with
\texttt{imax}, the inductive/recursor calculus, unique typing, Church-Rosser for $\kappa$. &
lean4lean's executable kernel, its correctness stated relative to
\texttt{VExpr}/\texttt{VEnv}/\texttt{IsDefEq}/\texttt{HasType} (\ref{def:isdefeq}) through a
translation \texttt{TrExprS} (K-D2), sound but not complete. \\
\bottomrule
\end{tabular}\par\smallskip

\begin{itemize}
\item \src{MR}: Rocq kernel theory only (the pre-PCUIC \texttt{term}), explicitly not a verification
result.
\item \src{CCC}: first Rocq kernel verification (sound only), plus the introduction of PCUIC itself.
\item \src{JACM}: Rocq kernel theory and verification, sound and complete, conjecture-free.
\item \src{HAB}: a superset of all three, in current MetaRocq naming.
\item \src{MRD}: the file-and-module map, the tiebreaker between a paper and the current code.
\item \src{CT}: the Lean kernel theory in full; no Lean kernel-verification result of its own, only
negative bounds on the algorithmic judgment.
\end{itemize}
\paragraph{Verification method.} \textbf{MetaRocq} writes its checker in Rocq against the same
syntax its specification uses, proves it sound and complete, then extracts it: the checked
object and the specified object are the same terms, with no refinement layer (K-D2).
\textbf{lean4lean} ports Lean's C++ kernel onto the real \texttt{Lean.Expr}/
\texttt{Lean.Kernel.Environment} and proves soundness relative to the model through a
translation \texttt{TrExprS}/\texttt{TrEnv} (K-D2) -- a refinement layer with its own trust
boundary (K-D4, K-D5). It runs on explicit fuel counters rather than a normalisation axiom
(K-C5, K-D7), and claims soundness only: \src{CT}'s \texttt{typesys.tex} proves $\equiv$
undecidable and $\Leftrightarrow$ non-transitive, so no algorithm can decide either (K-D1).

\section{Three-way divergences}
\label{sec:ref-divergences}

Fifty-two items compare MetaRocq/PCUIC, Carneiro's thesis and lean4lean's kernel layer, notion by
notion; each is read off a source file, a blueprint label, or the project's own status chapter,
never inferred from a paper alone. Five kinds, coloured throughout this section:

\par\smallskip\noindent
\begin{tabular}{p{0.22\linewidth}p{0.14\linewidth}rp{0.4\linewidth}}
\toprule
Kind & Badge & Count & \\
\midrule
Lean's theory forces it; no design choice available & \kFL & 7 &
\textcolor{kindFL}{\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}} \\
The port-and-refine method forces it, not Lean's theory & \kFK & 2 &
\textcolor{kindFK}{\rule{2mm}{2mm}\rule{2mm}{2mm}} \\
A deliberate modelling choice, an alternative was open & \kDS & 22 &
\textcolor{kindDS}{\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}} \\
An open proof obligation or missing derivation & \kGP & 19 &
\textcolor{kindGP}{\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}\rule{2mm}{2mm}} \\
Out of scope by explicit statement, no route planned & \kOP & 2 &
\textcolor{kindOP}{\rule{2mm}{2mm}\rule{2mm}{2mm}} \\
\bottomrule
\end{tabular}\par\smallskip

The count is over all 52 items of this chapter (the 39 below plus the 13 of
\S\ref{sec:ref-thesis-code}).

\subsection{Index}

\par\smallskip\noindent
\begin{tabular}{lp{0.22\linewidth}lp{0.5\linewidth}}
\toprule
id & notion & kind & title \\
\midrule
\did{K-A1} & universes & \kFL & universes and their algebra \\
\did{K-A2} & level order & \kDS & the level order is semantic, not a rule system \\
\did{K-A3} & cumulativity & \kFL & no cumulativity \\
\did{K-A4} & proof irrelevance & \kFL & definitional proof irrelevance \\
\did{K-A5} & eta, functions & \kFL & eta for functions as a primitive rule \\
\did{K-A6} & eta, structures & \kGP & structure eta and unit-like types are not modelled \\
\did{K-A7} & let/zeta & \kDS & let is substituted away; no zeta rule \\
\did{K-A8} & projections & \kDS & projections are recursor applications, not a term former \\
\did{K-A9} & projection coverage & \kGP & the projection expansion covers only some structures \\
\did{K-A10} & iota, K-like reduction & \kGP & iota as a schematic registry; K-like reduction absent \\
\did{K-A11} & quotients & \kDS & quotients are a delta axiom, not an iota rule \\
\did{K-A12} & inductive well-formedness & \kGP & inductive-block well-formedness is asserted, not derived \\
\did{K-A13} & primitives & \kDS & no literal node; primitives verified as reflection \\
\did{K-A14} & native reduction & \kOP & native reduction refused \\
\did{K-B1} & judgment shape & \kDS & one relation instead of two \\
\did{K-B2} & typed conversion & \kFL & conversion is typed \\
\did{K-B3} & binder names & \kDS & no binder names in the model \\
\did{K-B4} & algorithmic equality & \kGP & algorithmic equality is not formalised \\
\did{K-B5} & bidirectional layer & \kGP & the bidirectional layer exists but is unwired \\
\did{K-B6} & reduction relation & \kDS & reduction is abstract and parameterised \\
\did{K-B7} & environments & \kDS & two environment invariants, bridged silently \\
\did{K-C1} & confluence & \kGP & confluence holds only for an unrealistic class of environments \\
\did{K-C2} & subject reduction & \kGP & subject reduction for iota is a rule, not a theorem \\
\did{K-C3} & principality & \kGP & unique typing by a different route, and tainted \\
\did{K-C4} & type-former injectivity & \kGP & type-former injectivity is entirely open \\
\did{K-C5} & strong normalisation & \kDS & no normalization statement; the checker runs on fuel \\
\did{K-C6} & canonicity, consistency & \kGP & no canonicity, no consistency \\
\did{K-C7} & evaluation & \kGP & standardization is about the ideal reduction only \\
\did{K-D1} & checker guarantee & \kFL & soundness only, and completeness is not merely unproved \\
\did{K-D2} & verification method & \kDS & verification by translation to a model, not by construction \\
\did{K-D3} & open obligations & \kGP & six open obligations in the checker's verification \\
\did{K-D4} & pointer equality & \kFK & pointer equality as an axiom \\
\did{K-D5} & host data structures & \kFK & bridge axioms over the host's own data structures \\
\did{K-D6} & guard condition & \kFL & no guard condition to axiomatise \\
\did{K-D7} & termination & \kDS & fuel instead of well-founded recursion on normalisation \\
\did{K-D8} & level decision procedure & \kDS & a complete level decision procedure, and a hardness result \\
\did{K-E1} & boundary shape & \kGP & the shape of the trust boundary \\
\did{K-E2} & reification & \kDS & no reification layer \\
\did{K-E3} & vacuous layers & \kGP & three vacuous layers \\
\bottomrule
\end{tabular}\par\smallskip

\subsection{K-A: the object language}

\par\smallskip\noindent
{\setlength{\tabcolsep}{3pt}%
\begin{tabular}{lp{0.05\linewidth}p{0.19\linewidth}p{0.19\linewidth}p{0.23\linewidth}p{0.2\linewidth}}
\toprule
id & kind & PCUIC & Thesis & lean4lean & Why / consequence \\
\midrule
\did{K-A1} & \kFL &
\texttt{sort} = \texttt{sProp}$\,|\,$\texttt{sSProp}$\,|\,$\texttt{sType\ univ}, via a valuation
and constraint store. &
Levels $\ell::=u\mid0\mid S\ell\mid\max\mid\mathrm{imax}$, $\mathbb{N}$ semantics, no
\texttt{SProp}. &
\texttt{VLevel} (\ref{ind:vlevel}): zero/succ/max/imax/param; one sort family
\texttt{VExpr.sort}. &
Lean has no constraint graph, no \texttt{Set}/\texttt{SProp}; judgments index by arity, not
constraints. \\
\did{K-A2} & \kDS &
\texttt{leq\_universe} over valuations plus a graph procedure for consistency. &
$\ell\le\ell'+n$ by nine syntactic rules, three \texttt{imax} identities, a case split. &
\texttt{VLevel.LE}$\,a\,b:=\forall\,ls,\ a.\mathrm{eval}\,ls\le b.\mathrm{eval}\,ls$
(\ref{def:vlevel-le-equiv}). &
Semantic order needs no rule-completeness proof; equality is genuinely hard to decide (K-T2). \\
\did{K-A3} & \kFL &
Directed \texttt{cumulSpec0}, with \texttt{cumul\_Sort}/\texttt{Ind}/\texttt{Construct} for
cumulative inductives. &
No directed relation at all; equality is the only comparison stated. &
\texttt{IsDefEq} is an equivalence; \texttt{defeqDF} transports along equality, not a bound. &
Lean has neither cumulativity nor cumulative inductives; unique typing gives equality directly. \\
\did{K-A4} & \kFL &
No rule; \texttt{sSProp} exists but conversion never identifies its inhabitants. &
From $p{:}P$, $h,h'{:}p$, derive $h\equiv h'$, for any \texttt{Prop}. &
\texttt{IsDefEq.proofIrrel}: the same rule, reading the shared type of both sides. &
Lean's kernel identifies proofs of a \texttt{Prop}; conversion must be typed (K-B2). \\
\did{K-A5} & \kFL &
No eta rule; explicitly out of scope. &
$e:\forall y{:}\alpha.\beta\Rightarrow\lambda x{:}\alpha.(e\,x)\equiv e$. &
\texttt{IsDefEq.eta}, the same rule, read against $e$'s \texttt{forallE} type. &
Lean has definitional eta; it enters \texttt{NormalEq} as two directed rules, needing its own
symmetry. \\
\did{K-A6} & \kGP &
Neither structure eta nor a unit-like rule; both reference sides agree, no obligation. &
No counterpart. &
Checker implements both (\ref{thm:vtc-tryetastruct}, \ref{thm:vtc-isdefequnitlike}); model has
neither rule; both \texttt{sorry}. &
Not derivable from the current rules; the checker's entry points are partial as stated. \\
\did{K-A7} & \kDS &
\texttt{tLetIn} as a term former, with a zeta reduction and an unfolding rule. &
Zeta kept as a rule, with an explicit conservativity argument for expanding it. &
\texttt{VExpr} (\ref{ind:vexpr}) has six constructors, no let; translation extends the context. &
Thesis's own argument licenses moving expansion to syntax; zeta must preserve translation, not
the judgment. \\
\did{K-A8} & \kDS &
\texttt{tProj} with its own typing rule, iota reduction, congruence rule. &
A Sigma-projection typing shape, with an eta rule flagged as not real defeq. &
\texttt{TrProj} (\ref{def:trproj}, \ref{struct:trprojctor}) builds a recursor application,
chaining fields' motives. &
Reuses the iota registry (K-A10) instead of a new node; coverage is partial (K-A9). \\
\did{K-A9} & \kGP &
Every declared projection typechecks, no restriction on structure shape. &
No counterpart. &
\texttt{inferProj.WF\_struct} (\ref{thm:vtc-inferproj-struct}) only for non-mutual
single-constructor shapes, \texttt{sorry}. &
K-A8's expansion needs the assumed shape; a projecting term outside it has no derivation. \\
\did{K-A10} & \kGP &
One \texttt{cumul\_iota} rule, plus a guarded fix rule; no K. &
Constructor rule plus K-like reduction for subsingleton eliminators, tied to axiom K. &
\texttt{VEnv} carries a \texttt{pats} registry (\ref{rule:isdefeq-pat}); \texttt{addRecRule}
registers the constructor rule. &
One generic rule serves every recursor; K-like needs a proof-irrelevance reconstruction the
registry lacks. \\
\did{K-A11} & \kDS &
No quotient types. &
\texttt{Quot}/\texttt{mk}/\texttt{sound}/\texttt{lift}, only \texttt{sound} a real axiom, rest
semi-builtin. &
\texttt{quotDefEq} is one \texttt{VDefEq} entry, fired by \texttt{IsDefEq.extra}, not a
\texttt{pats} entry. &
A single closed equation needs no pattern machinery; confluence (K-B6) needs every axiom as a
\texttt{pats} rule. \\
\did{K-A12} & \kGP &
Positivity, universe and elimination conditions, established by \texttt{check\_wf\_env}. &
Generated from the block's specification, not pinned as fixed fields. &
\texttt{VInductDecl.WF} (\ref{structure:ind-wf}) is a 20-field syntactic specification, not
derived from the checker. &
Deriving it from \texttt{addInductive} is the missing case of environment admission (K-E3). \\
\did{K-A13} & \kDS &
\texttt{tPrim} as a term former, with its own typing rule and invariants. &
No counterpart. &
No literal constructor; a literal unfolds to constructors, acceleration closes into
\texttt{Reflects} (\ref{vpc:thm:reflects-toconst}). &
Keeps the model at six constructors; eighteen per-primitive obligations have no reference
counterpart. \\
\did{K-A14} & \kOP &
No VM or native conversion modelled. &
No counterpart. &
\texttt{reduceNative} refuses \texttt{reduceBool}; the obligation
(\ref{thm:vtc-reducenative}) is vacuous, never fires. &
Verified native compilation is project-sized; lean4lean rejects some kernel-accepted terms,
soundness unaffected. \\
\bottomrule
\end{tabular}}\par\smallskip

\subsection{K-B: judgments}

\par\smallskip\noindent
{\setlength{\tabcolsep}{3pt}%
\begin{tabular}{lp{0.05\linewidth}p{0.19\linewidth}p{0.19\linewidth}p{0.23\linewidth}p{0.2\linewidth}}
\toprule
id & kind & PCUIC & Thesis & lean4lean & Why / consequence \\
\midrule
\did{K-B1} & \kDS &
\texttt{typing} and \texttt{cumulSpec0} as separate inductives, joined by one conversion rule. &
Typing and definitional equality likewise kept separate. &
One four-place \texttt{IsDefEq}\,$E\,U\,\Gamma\,e_1\,e_2\,A$ (\ref{def:isdefeq}), typing as its
diagonal. &
Proof irrelevance and eta already couple the two; every structural lemma is proved once. \\
\did{K-B2} & \kFL &
\texttt{cumulSpec0} relates two terms with no type index. &
$\equiv$ likewise carries no type, though eta/irrelevance read one implicitly. &
\texttt{IsDefEq} carries the type as its last argument (\ref{def:isdefeq}); \texttt{IsDefEqU} is
derived. &
\texttt{proofIrrel}/\texttt{eta} read the type; composing equalities needs unique typing, unlike
PCUIC's transitivity. \\
\did{K-B3} & \kDS &
\texttt{aname} on every binder; \texttt{eq\_binder\_annot} in every congruence rule. &
Named binders throughout, matching Lean's real syntax. &
\texttt{VExpr}'s \texttt{lam}/\texttt{forallE} carry no name, no \texttt{BinderInfo}
(\ref{family:expr-beq}). &
Names carry no logical content; the kernel's own name equality needs a separate lawfulness
argument. \\
\did{K-B4} & \kGP &
A full algorithmic specification, proved equivalent to the declarative one. &
$\Leftrightarrow$: no transitivity, a head-reduction escape hatch, fails subject reduction. &
No relation plays $\Leftrightarrow$'s role; stand-in is the checker's own correctness chain. &
With $\Leftrightarrow$ unformalised, nothing marks acceptance as algorithmic rather than
definitional. \\
\did{K-B5} & \kGP &
\texttt{Bidirectional/} proves inference equivalent to declarative typing; \texttt{infer} stated
against it. &
No separate algorithmic layer of this kind. &
\texttt{VEnv.InferType} (\ref{thm:infertype-exists}) sound/complete/deterministic, checker
verified against \texttt{IsDefEq} directly. &
\texttt{InferType} needs K-B6's abstract class, which no realistic environment satisfies;
unusable in practice. \\
\did{K-B6} & \kDS &
\texttt{red1} first-class; \texttt{cumulSpec0} carries reductions directly, unconditional over
\texttt{wf Sigma}. &
$\kappa$ restricts reduction to $\beta/\delta/\zeta/\iota$ on rec-normal-form terms. &
\texttt{ParRed}/\texttt{CParRed}/\texttt{WHRed}/\texttt{StRed} live under one abstract
\texttt{Params} class. &
Abstracting proves confluence once; it needs every axiom realised by a \texttt{pats} rule
(K-C1). \\
\did{K-B7} & \kDS &
\texttt{wf Sigma} is one predicate, \texttt{on\_global\_env}, carrying everything. &
A single well-formedness notion, matching PCUIC's. &
\texttt{VEnv.WF'} (build-in-order) and \texttt{Ordered}, bridged by a coercion
(\ref{instance:coeout-orderedstrong}). &
\texttt{Ordered} avoids carrying declaration history, but the coercion hides a \texttt{sorry}
with no visible cue. \\
\bottomrule
\end{tabular}}\par\smallskip

\subsection{K-C: metatheory}

\par\smallskip\noindent
{\setlength{\tabcolsep}{3pt}%
\begin{tabular}{lp{0.05\linewidth}p{0.19\linewidth}p{0.19\linewidth}p{0.23\linewidth}p{0.2\linewidth}}
\toprule
id & kind & PCUIC & Thesis & lean4lean & Why / consequence \\
\midrule
\did{K-C1} & \kGP &
Triangle method with an optimal-reduction function, unconditional for any wf environment. &
The $\kappa$ analogue, up to $\equiv_p$, via Tait--Martin-L\"of. &
\texttt{ParRedS.church\_rosser} holds up to \texttt{NormalEq}, under \texttt{Params}, two
literal sorries. &
\texttt{Params} is K-B6's price; unproved for any environment with a definition or quotient
rule. \\
\did{K-C2} & \kGP &
\texttt{subject\_reduction} and its one-step form \texttt{sr\_red1}, both proved. &
The corresponding fact, dismissed as "an easy induction". &
\texttt{IsDefEq.pat} asserts the reduct's type untyped; the honest form needs
\texttt{VEnv.WF.patsStrong} (\ref{thm:wf-patsstrong}), \texttt{sorry}. &
Lets \texttt{IsDefEq} exist before the strong system; every dependent declaration becomes
conditional. \\
\did{K-C3} & \kGP &
\texttt{principal\_type}, proved explicitly without relying on normalisation. &
\texttt{thm:unique} via a $\vdash_n$-indexed joint induction with Church-Rosser. &
\texttt{IsDefEq.uniq} (\ref{thm:unique-typing}) strengthens, projects to syntax-directed,
stratifies by derivation level. &
Avoids the thesis's mutual recursion; K-B2 routes compositions through it, so the taint is
pervasive. \\
\did{K-C4} & \kGP &
Sort and product injectivity under conversion, derived from confluence, proved. &
\texttt{thm:1dinv}, the step closing the unique-typing induction. &
Three declarations (\ref{thm:injectivity-open}) all \texttt{sorry}; \texttt{forallE\_inv}
inherits both gaps. &
Two routes in flight: the blocked Church-Rosser line and an experimental logical-relations
line. \\
\did{K-C5} & \kDS &
Normalisation as two typeclass assumptions, justifying fuel-free weak-head reduction. &
An unfinished sketch of a terminating variant, stopping before any theorem. &
Several independent fuel counters (\ref{thm:vtc-withfuel}); a fuel-exhausted run is
unconstrained. &
Lean needs explicit termination witnesses; the largest Rocq axiom is absent, but completeness
is unstatable. \\
\did{K-C6} & \kGP &
Canonicity and consistency, both proved under the normalisation assumption. &
Soundness of a proof-split variant in ZFC plus inaccessibles, on paper. &
None; states correctness relative to the model, not consistency of the type theory. &
Out of scope by design: the trust chain terminates at \texttt{VEnv.IsDefEq}, not stated
consistent. \\
\did{K-C7} & \kGP &
One-shot weak call-by-value evaluation; \texttt{wcbv\_standardization} the sole
strong-normalisation use here. &
No standardization theorem at all. &
\texttt{ParRedS.standard} (\ref{thm:parreds-standard}), after Kashima (2000), about the
abstract ideal reduction. &
Serves head-reduction lemmas, not running programs; no weak-CBV relation exists for Lean
terms. \\
\bottomrule
\end{tabular}}\par\smallskip

\subsection{K-D: checker and method}

\par\smallskip\noindent
{\setlength{\tabcolsep}{3pt}%
\begin{tabular}{lp{0.05\linewidth}p{0.19\linewidth}p{0.19\linewidth}p{0.23\linewidth}p{0.2\linewidth}}
\toprule
id & kind & PCUIC & Thesis & lean4lean & Why / consequence \\
\midrule
\did{K-D1} & \kFL &
Sound and complete against the declarative spec, every rejection a refutation. &
Proves $\equiv$ undecidable and $\Leftrightarrow$ non-transitive; no algorithm decides either. &
Three public entry points (\ref{thm:vtc-toplevel}) give soundness only; a \texttt{true} result
relates the terms. &
Completeness needs $\Leftrightarrow$ (K-B4), unformalised; a rejection carries no information. \\
\did{K-D2} & \kDS &
Checker written in Rocq against the spec's own syntax; checked and specified object coincide. &
No checker-construction analogue. &
Port of the C++ kernel on real \texttt{Lean.Expr}; correctness via \texttt{TrExprS}/
\texttt{TrEnv} plus a \texttt{.WF} lemma per function. &
Fidelity: verifies the algorithm that actually runs, but adds a refinement layer's own trust
boundary. \\
\did{K-D3} & \kGP &
No open per-function obligations. &
No checker-construction analogue. &
Six sorries: \ref{thm:vtc-tryetastruct}, \ref{thm:vtc-isdefequnitlike} (K-A6);
\texttt{reduceProjCore.WF}; \ref{thm:vtc-reducerecursor} (K-A10); \ref{thm:vtc-inferproj-struct},
\ref{thm:vtc-inferproj} (K-A9). &
Four of six trace to K-A6/K-A8-9: no structure eta, no projection node; every top-level
statement is partial. \\
\did{K-D4} & \kFK &
The checker is a pure Rocq function; no physical-identity test anywhere. &
No checker-construction analogue. &
\texttt{ptrEqExpr\_eq} axiomatises pointer-equal expressions equal; \texttt{EquivManager} cache
verified separately (\ref{thm:eqvmgr-isdefeq-wf}). &
The ported algorithm branches on pointer identity for speed; benign except one non-shortcut
call site. \\
\did{K-D5} & \kFK &
Environments, maps, arrays all defined in Rocq; nothing assumed about them. &
No checker-construction analogue. &
32 axioms relate the model to Lean's \texttt{PersistentHashMap}, \texttt{PersistentArray},
\texttt{Expr}, \texttt{Level}, \texttt{Syntax}. &
The kernel runs on Lean's own containers; the trust boundary mixes math gaps with library
lemmas. \\
\did{K-D6} & \kFL &
\texttt{guard\_checking}/\texttt{guard\_checking\_correct}, its largest single trust axiom. &
Recursion compiled into recursor applications before the kernel sees a declaration. &
Nothing: \texttt{VExpr} (\ref{ind:vexpr}) has no fix constructor, \texttt{IsDefEq} no fix rule. &
Lean's termination checking sits outside the kernel; with K-C5 gone too, neither side has a
direct counterpart. \\
\did{K-D7} & \kDS &
\texttt{reduce\_stack} fuel-free, well-founded on an order accessible via the normalisation
axiom. &
No checker-construction analogue. &
Several independent fuel counters (\ref{thm:vtc-withfuel}); the real kernel shares one
recursion-depth bound. &
Lean needs explicit termination witnesses; the two can disagree on deep input, exhaustion looks
like rejection. \\
\did{K-D8} & \kDS &
Weighted-graph acyclicity, deciding constraint consistency, not level-expression equivalence. &
The rule system of K-A2, with no algorithm and no complexity result. &
A canonical form, sound and complete (\ref{thm:lvl-normalize-complete}), plus coNP-hardness
(\ref{thm:levelsat-equiv-iff-unsat}). &
K-A2's semantic order must be decided completely; lean4lean decides strictly more pairs, never
fewer. \\
\bottomrule
\end{tabular}}\par\smallskip

\subsection{K-E: the trust boundary}

\par\smallskip\noindent
{\setlength{\tabcolsep}{3pt}%
\begin{tabular}{lp{0.05\linewidth}p{0.19\linewidth}p{0.19\linewidth}p{0.23\linewidth}p{0.2\linewidth}}
\toprule
id & kind & PCUIC & Thesis & lean4lean & Why / consequence \\
\midrule
\did{K-E1} & \kGP &
Guard condition and normalisation, two named axioms, plus unverified quoting and extraction. &
No boundary statement of this kind; describes the ideal judgments only. &
16 live sorries outside \texttt{Experimental/}, 110 axioms, 12 opaque constants, quoted per
statement. &
The refinement method spreads the boundary across theory gaps and library assumptions; about
8\% of declarations are tainted. \\
\did{K-E2} & \kDS &
The Template layer quotes Rocq's own term representation into checked syntax, in OCaml,
unverified. &
No reification step; ideal judgments stated directly on Lean-shaped terms. &
\texttt{Replay.lean} reads declarations from a real imported environment and replays them. &
Lean's syntax is already a Lean datatype; closes one Rocq-boundary item, at the cost of K-D5. \\
\did{K-E3} & \kGP &
No layer proved yet inhabited by nothing; theorems stated over a predicate a real checker
establishes. &
No counterpart. &
\texttt{inductDecl} admission is \texttt{sorry} (\ref{structure:ind-wf}); confluence needs every
axiom as a pattern (\ref{thm:indparams-defeqs-as-pats-vacuous}); \texttt{TrEnv.proj\_defeq}
(\ref{thm:tr-env-proj-defeq}) has no caller. &
The specification side is finished, the refinement side is not; a vacuity pass is the honest
reading. \\
\bottomrule
\end{tabular}}\par\smallskip
\section{Thesis versus code}
\label{sec:ref-thesis-code}

Thirteen further items compare Carneiro's thesis directly against the code, where the code
either departs from the thesis by design or goes beyond it; each is cross-referenced to the
K-A--K-E item that treats it in full.

\par\smallskip\noindent
\begin{tabular}{lp{0.12\linewidth}p{0.19\linewidth}p{0.19\linewidth}lp{0.21\linewidth}}
\toprule
id & notion & thesis & code & kind & why \\
\midrule
\did{K-T1} & levels & the nine-rule system of \texttt{axioms.tex} & \texttt{VLevel.LE}, a
semantic order & \kDS & no rule-completeness proof needed (K-A2) \\
\did{K-T2} & level complexity & no complexity result stated & \texttt{LevelSat.lean} proves the
problem coNP-hard & \kDS & bounds what any complete algorithm can cost (K-A2, K-D8) \\
\did{K-T3} & let/zeta & zeta kept, with a conservativity argument & no zeta rule; let substituted
away & \kDS & keeps the model at six constructors (K-A7) \\
\did{K-T4} & algorithmic equality & $\Leftrightarrow$: undecidable, non-transitive, fails
subject reduction & $\Leftrightarrow$ has no formal counterpart at all & \kGP & the thesis's
negative results have no formal home (K-B4, K-D1) \\
\did{K-T5} & unique typing & $\vdash_n$-indexed stratification & \texttt{HasTypeStratified}, a
strong syntax-directed judgment & \kDS & replaces alternation-counting with level-counting
(K-C3) \\
\did{K-T6} & rec-normal forms & the eta-vs-iota fight on partially applied eliminators & no
counterpart & \kGP & the fight is not modelled at all \\
\did{K-T7} & eight primitives & \texttt{Wtypes.tex}'s $\Sigma$/$W$/\texttt{acc} encoding & not
formalised; only the Sigma-projection shape reused & \kDS & the shape serves \texttt{TrProj},
the reduction does not (K-A8) \\
\did{K-T8} & soundness & a ZFC-plus-inaccessibles model, on paper & no counterpart & \kGP &
correctness is stated relative to the model, not consistency (K-C6) \\
\did{K-T9} & iota vs.\ K-split & \texttt{normalization.tex} splits $K^+$ from $\iota$ & one
uniform iota rule & \kDS & matches the stub's weaker relation, not its split (K-A10) \\
\did{K-T10} & compilation stub & a 25-line fragment, no theorem & native reduction refused
outright & \kOP & no verified-compilation route exists yet (K-A14) \\
\did{K-T11} & inductive blocks & the recursor and iota rules generated from the block's spec &
\texttt{VInductDecl.WF} pins 20 fields directly & \kDS & makes well-formedness decidable, at the
cost of K-A12 \\
\did{K-T12} & standardization & no theorem stated & \texttt{ParRedS.standard}, after Kashima
(2000) & \kDS & beyond the thesis; serves the head-reduction lemmas (K-C7) \\
\did{K-T13} & regularity of reductions & called "an easy induction" &
\texttt{VEnv.WF.patsStrong}, the one open obligation & \kGP & the widest-reaching gap in the
project (K-C2) \\
\bottomrule
\end{tabular}\par\smallskip

\section{What is aligned}
\label{sec:ref-aligned}

The remaining notions line up name for name, with no divergence of kind; each row is a MetaRocq
or thesis element and its lean4lean counterpart.

\par\smallskip\noindent
\begin{tabular}{p{0.23\linewidth}p{0.19\linewidth}p{0.27\linewidth}p{0.20\linewidth}}
\toprule
notion & PCUIC & lean4lean & thesis \\
\midrule
variable lookup & \texttt{type\_Rel} & \texttt{Lookup}, \texttt{IsDefEq.bvar} & the variable rule
\\
sorts & \texttt{type\_Sort} & \texttt{IsDefEq.sortDF} & -- \\
product formation & \texttt{type\_Prod}, \texttt{sort\_of\_product} & \texttt{IsDefEq.forallEDF}
with \texttt{imax} & Pi-formation, the \texttt{imax} clause \\
abstraction & \texttt{type\_Lambda} & \texttt{IsDefEq.lamDF} & -- \\
application & \texttt{type\_App} & \texttt{IsDefEq.appDF} & -- \\
universe-polymorphic constants & \texttt{type\_Const} & \texttt{IsDefEq.constDF} & -- \\
conversion & \texttt{type\_Cumul} & \texttt{IsDefEq.defeqDF} (modulo K-A3) & -- \\
context well-formedness & \texttt{wf\_local} & \texttt{OnCtx}\,$\Gamma\,(\mathrm{IsType})$ &
$\vdash \Gamma$ ok \\
beta & \texttt{cumul\_beta} & \texttt{IsDefEq.beta} & -- \\
delta & \texttt{cumul\_delta} & \texttt{IsDefEq.extra} over \texttt{env.defeqs} & definitions \\
iota & \texttt{cumul\_iota} & \texttt{IsDefEq.pat} over \texttt{env.pats} (modulo K-A10) & the
iota section \\
weakening & \texttt{weakening\_typing} & \texttt{IsDefEq.weakN} & \texttt{thm:weak} \\
substitution & \texttt{substitution} & \texttt{IsDefEq.instN} & \texttt{thm:subst}, item
\texttt{subst\_ty} \\
validity, regularity of types & \texttt{validity} & \texttt{IsDefEq.isType} & \texttt{thm:reg},
item 4 \\
free variables in context & \texttt{PCUICClosed} & \texttt{IsDefEq.closedN} & \texttt{thm:reg},
item 2 \\
context conversion & \texttt{PCUICContextConversion} & \texttt{IsDefEqCtx},
\texttt{IsDefEq.defeqDFC} & -- \\
environment weakening & \texttt{weakening\_env} & \texttt{IsDefEq.mono} along the extension order
& -- \\
universe instantiation & \texttt{typing\_subst\_instance} & \texttt{IsDefEq.instL} &
universe-polymorphic constants \\
simultaneous substitution & \texttt{subslet} & \texttt{Ctx.SubstEq}, \texttt{IsDefEq.substDF} &
-- \\
parallel reduction & \texttt{pred1} & \texttt{ParRed} (\ref{def:parred}) & $\rightsquigarrow_\kappa$
(compatibility direction) \\
complete parallel reduction & \texttt{rho} & \texttt{CParRed} (\ref{def:cparred}) & -- \\
triangle lemma & the triangle property & \texttt{ParRed.triangle} & \texttt{thm:tri} \\
confluence & \texttt{PCUICConfluence.v} & \texttt{ParRedS.church\_rosser} (status differs, K-C1)
& \texttt{thm:church\_rosser} \\
defeq via reduction plus irrelevance & -- & \texttt{IsDefEq.church\_rosser}
(\ref{thm:isdefeq-church-rosser}) & \texttt{thm:ckappa} \\
bidirectional inference & \texttt{Bidirectional/} & \texttt{VEnv.InferType} (unwired, K-B5) & --
\\
head-reduce to a sort or product & \texttt{reduce\_to\_sort}/\texttt{reduce\_to\_prod} &
\texttt{IsDefEq.reduce\_sort}/\texttt{.reduce\_forallE} & -- \\
level valuation semantics & \texttt{Universes.val}/\texttt{satisfies} &
\texttt{VLevel.eval}/\texttt{VLevel.LE} (\ref{ind:vlevel}) & level semantics paragraph \\
declaration admission & \texttt{check\_wf\_decl} & \texttt{VDecl.WF}, \texttt{VEnv.WF'} & \S2.5,
\S2.6, \S2.7.1 \\
mutual blocks & MetaRocq's mutual definitions & \texttt{VEnv.addConsts} then
\texttt{addDefEqs} & -- \\
monotonicity of well-formedness & \texttt{weakening\_env\_decl} & \texttt{VConstant.WF.mono},
\texttt{VDefEq.WF.mono}, \texttt{PatWF.mono} & -- \\
\bottomrule
\end{tabular}\par\smallskip

\section*{Caveats}

\begin{itemize}
\item Every claim above is read off a source file, a blueprint label, or the analysis behind
this chapter; none is inferred from a paper's abstract or from a title alone.
\item \src{CCC} is superseded by \src{JACM} on every shared topic; it is cited here only for its earlier
framing (PCUIC's introduction) or for historical firsts (Sec 3, first sound checker).
\item \src{MR} appears only where lean4lean's own evidence explicitly names it; it establishes no
kernel-verification result and should never be cited for one.
\item A "kind" is read off the current state of the code and the current state of the two
papers; a later proof could move a GAP item into DESIGN without changing what is proved today.
\item Kind counts (FORCED-LEAN 7, FORCED-LEAN4LEAN 2, DESIGN 22, GAP 19, OPEN 2) total all 52
items: K-A--K-E (DESIGN 14, GAP 15, OPEN 1) plus the K-T table of \S\ref{sec:ref-thesis-code}
(DESIGN 8, GAP 4, OPEN 1).
\end{itemize}
